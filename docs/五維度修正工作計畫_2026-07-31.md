# 五維度修正工作計畫 · 2026-07-31

> 依 Codex 第一輪審查 §七 建立。本檔是**五維度重設計的單一權威工作文件** ——
> 血緣稽核的證據在 [`血緣稽核_五維度_2026-07-31.md`](血緣稽核_五維度_2026-07-31.md),
> 本檔負責:**分類問題、定嚴重度、排順序、劃出哪些必須重新預註冊、哪些不得回填 V0**。
>
> **狀態:全部發現一律標 `待獨立覆核` 或 `已獨立覆核`,沒有一項是「最終結論」。**
> 本檔不修改任何策略公式、不執行 OOS、不重建 canonical 面板。
>
> 重現指令:
> ```bash
> python scripts/face_lineage_audit.py --adv 1e6
> python scripts/breakpoint_2019_audit.py
> python scripts/composite_reconcile.py --stocks 60
> ```

---

> **2026-08-09 使用者裁示:凍結解除,V0 可進實盤。** 依據:
> (1) regime 混入——原判定「未拆出來單獨驗」有誤,Gate 1 的 C1 arm 就是「拿掉 regime」
>     的直接檢定,已測完,t=−1.613/−2.113(方向為負、不顯著),不構成部署障礙。詳見
>     `docs/勘誤_本體A無regime描述.md`。
> (2) 資料可得性斷裂(2019 定義斷裂)——已記錄、已理解,是資料本身的限制不是可修的
>     程式錯誤,不構成部署障礙。
> (3) 缺值語意不一致——對應 arm A3,Gate 1 已測(t=1.578/1.253,不顯著),選股訊號
>     這條研究線已正式結案(12 個 arm 全數否決或未過獨立驗證,乾淨樣本已用盡)。
> (4) **研究與 live 路徑不一致——仍未解決**(估值窗 2019 vs 2004、籌碼源
>     `institutional_participation` vs `institutional_flow`、c2 取樣池 ~400 檔粗篩
>     vs 全 ADV 池,見 `docs/現況總表_2026-08-09.md` §9-3)。**凍結解除不等於這個落差
>     已修好**——`app.py`「雙確認精選」分頁已加註揭露此落差(2026-08-09),之後任何
>     部署層工作都要保留這條揭露,不得假裝即時名單逐日重現 H1–H5 的驗證結果。
>
> 本節以下內容(原 2026-07-31 判定)保留為歷史紀錄,不刪除、不回溯改寫。

## 0. V0 的處置(依 Codex §九,先寫在最前面免得被誤讀)

**V0 = `real_composite` Top-20% ∩ `c2` Top-20% @ ADV≥100萬,等權,`exec_ret.fwd_x`。**

| 項 | 處置 |
|---|---|
| `預註冊_雙確認ADV100萬.md` §7.1 的 H1–H5 判定 | **保留、不刪除、不重新命名、不回溯修改** |
| 能不能說「五維度已驗證完成」 | ❌ **不能** |
| 能不能進實盤 SOP / 活體演練 | ❌ **不能**(依 `GPT answer.md` §八,五維度修正與新版 H1–H5 完成前一律暫緩) |
| 新稽核結果能不能回填成「V0 其實是 X」 | ❌ **不能**(見 §7) |

**V0 的唯一正確描述(逐字採用 Codex **第二輪**的正式定位,取代第一輪版本)**:

> V0 的五維 composite＋c2 完整策略包通過既定 H1–H5,但其評分系統存在 **regime 混入、
> 資料可得性斷裂、缺值語意不一致、研究與 live 路徑不一致**等結構問題。
> 因此 V0 **可作為 frozen baseline,不可視為已修好的正式策略,也不可直接進入實盤。**

(第一輪版本的差別:第二輪把「研究與 live 路徑不一致」也列入 —— 那是本輪新查出來的
`washout` 死碼與 `bt_bundle` 覆寫,見 §1-2 與 §6-B5。)

**frozen baseline 的定義(未來所有 candidate 的對照組)**:

| 項 | 凍結值 |
|---|---|
| 計分碼 | `core/scoring_manager.py` + `core/advisor.py` @ commit `d8bb0f4`(v4.4/v4.5 口徑) |
| 五維名目權重 | fund .31 / val .08 / tech .19 / mom .27 / whale .15(`MODES['balanced']['composite_weights']`) |
| 面板 | `data/research_base/realbody_scores_adv100w.parquet`(2026-07-30 建,263,928 列 / 2,139 檔 / 257 月) |
| 報酬線 | `exec_ret.fwd_x` |
| regime / dyn weights | **含**(見 §5) |

---

## 1. 五維度血緣圖

```
TEJ 本機快取 (~/tej_cache)
  └─ beat_0050/realbody/bt_bundle.bt_fetch_history()      ⚠ 兩個研究專用覆寫,見 §1-2
       └─ core.backtest.build_pit_stockdata(bundle, as_of)  ← PIT 切片(PUBLISH_LAG_DAYS=45)
            ├─ core.fundamentals.FundamentalEngine.evaluate()  → f_fund
            │     獲利 .30(roe/net_margin/gross_margin/asset_turnover)
            │     成長 .25(rev_cagr/eps_cagr)
            │     安全 .25(debt_to_asset/current_ratio)
            │     估值 .20(pe_vs_industry ← **與 f_val 同源**)
            ├─ core.valuation.ValuationEngine.evaluate()       → f_val
            │     ① industry_value_percentile(2019-04 起才有)
            │     ② PEG .85 + 歷史位階 .15    ③ 絕對門檻
            └─ core.scoring_manager.ScoringManager.calculate_score()
                  f_tech  均線30 + 週線15 + RSI25 + MACD20 + 布林8 + MA交叉±(6/8)
                          [%B / KD 兩個候選開關 **關閉**;**high52_prox 不在此**]
                  f_mom   (A)中期價格45 + (A2)RS±8 + (B)營收30 + (C)短線確認25
                  f_whale 48 + Σw_n·淨參與率×300 + 土洋同步8 + 連買±12 + 確認±15 + TDCC±8
                          [天期 1/3/5/10/20;無多天期資料 → _legacy_whale_score 舊算法]
                 └─ core.advisor.InvestmentAdvisor.advise()     → real_composite
                      ① washout → chip_bucket = min(100, max(whale,60)+5)
                      ② 估值資料不足 → val_bucket = 50
                      ③ mw = 名目權重 × regime_multipliers(current_regime)
                      ④ 強勢多頭排列 → 估值權重砍 60%(60% 給動能、40% 給籌碼)
                      ⑤ composite = Σ bucket·mw / Σmw
                           └─ build_realbody_scores 只存 real_composite + 五個**原始**面
                              (dyn_weight / regime / data_gaps / 覆寫旗標 **全部沒存**)
```

### 1-1 `high52_prox` 的更正(Codex §五 指示以程式碼為準)

`high52_prox` **不在 production 的 `f_tech` 裡**,它是 `c2` 的獨立腿。
全 repo grep:`core/scoring_manager.py`、`core/advisor.py`、`core/*.py` 完全沒有這個名字,
只出現在 `scripts/lab_paths.add_c2()`、`obs_alpha` 與各 lab。
`GPT answer.md` §四-4 把它列為 f_tech 子因子 —— **以程式碼為準,該說法作廢。**

### 1-2 「真身」不等於「生產」

`bt_bundle.py` 兩個 process-local 覆寫,**未進任何預註冊**:

| 覆寫 | 內容 | 影響 |
|---|---|---|
| 估值窗 | `_PCT_HISTORY_START` 2019 → 2004 | live 鎖 2019 的理由正是「避免口徑漂移」。解鎖後 2005–2018 的 PE/PB 百分位跑在逐年變長的 expanding 窗上。實測 f_val 中位 2005 **56.0** → 2010 **87.5** → 2019 後穩定 **51–52** |
| 籌碼源 | `chip` 改讀 `institutional_flow`(淨額) | `institutional_participation`(buy+sell)以 \|net\| 近似 → 系統性低估 → 影響 f_whale 的 ±15 確認層 |

→ **真身 = 生產計分碼 × 研究改過的資料輸入。** 這是真身面板的第三層落差
(前兩層:c2 取樣池差異、realbody 是快照)。

---

## 2. 2019 三個定義斷裂點 —— ✅ **已獨立覆核**

`scripts/breakpoint_2019_audit.py`,257 個 as_of 全數重算。

### A1/A2 regime:資料缺口是**唯一**原因(同一個函式,只換輸入)

| 輸入 | 列數 / 區間 | 全期分布 | **2019 前 168 個月** |
|---|---|---|---|
| 生產走的 `load_benchmark('0050')`(FinMind 快取) | 1,827 / 2019-01-02 → 2026-07-22 | neutral 183 / bull 57 / bear 17 | **neutral 168(100%)** |
| repo 內 `beat_0050/data/benchmark/0050_raw.parquet` | 5,677 / **2003-06-30** → 2026-07-28 | bull 148 / bear 72 / neutral 37 | **bull 94 / bear 47 / neutral 27** |

兩者一致率:2019 起 **87.6%**,2019 前 **16.1%**。

> ### ⚠ 2026-07-31 修正:上面那個 **87.6% 是被污染的數字**,機制描述也錯了
>
> `scripts/breakpoint_2019_audit.py` 把 `0050_raw.parquet` **未經 `_back_adjust()`**
> 就丟進 `classify_regime`,而生產路徑(`load_benchmark`)**是有跳空回補的**。
> 2025 年 0050 分割在未回補的序列上是一天 **−75%** 的人造崩盤 → 強制 bear。
>
> `scripts/a1a2_isolation_check.py` 用**兩邊都 back-adjust** 重測:
>
> | 量測 | 舊稽核 | **正確做法** |
> |---|---|---|
> | 2019 起一致率 | 87.6%(11 個月不一致,**其中 7 個在 2025**) | **95.4%**(4 個月不一致) |
> | 不一致月份 | 散落 2019 與 2025 | **全部落在 2019-01/02/06/07** |
>
> 而且**「兩條路徑讀不同的 0050」這個機制描述是錯的**:
> - 兩份序列在重疊的 1,827 列上 `close` **逐列相同(max\|Δ\|=0.000000)**;
> - RS 與 regime 讀的是**同一個** `data_cache TaiwanStockPrice/0050`
>   (`read_cached()+_back_adjust()` 與 `load_benchmark()` 輸出逐列相同);
> - `0050_raw.parquet` **不是另一個資料源,是同一條序列的歷史延長**;接縫無跳空
>   (2018-12-28 close 75.50 → 2019-01-02 close 74.05)。
>
> **不變的結論**:2019 前 168 個月全 neutral 的**唯一原因就是資料缺口**
> (`classify_regime` 在 < `ma_long+slope_lookback` = 140 列時強制回 neutral)。
> **變的是**:2019 之後兩者的差異只有 **4 個月**,而且全部是 trailing window
> 跨越 2019-01-02 接縫造成的暖機效應 —— 這批月份的 V0 值是**資料不足的強制預設**,
> 不是真的判定為中性。這 4 個月已寫進預註冊第一批 §4 的「**邊界污染窗**」。

### A3 f_val:路徑①覆蓋率(逐列查 `industry_value_pct`,259,652 列全查)

| 期間 | 路徑①(產業內位階)覆蓋率 |
|---|---|
| 2005 ~ 2019-03 | **0.00%**(逐年皆 0) |
| 2019 全年 | 70.93% |
| 2020 起 | 94–96% |

參考表 `industry_value_ref.parquet`:2,381,356 列 / 1,880 檔 / **2019-04-10** 起。
→ **f_val 在 2019-04 前後走的是兩個不同的因子**,而且退路徑時**不 raise、不記錄**。

### A4 f_mom 的 RS

RS 基準源是 `data_cache TaiwanStockPrice/0050`(2019-01-02 起 1,827 列)。

> **⚠ 2026-07-31 修正**:原文寫「**與 regime 不同源**」是**錯的**。兩者是**同一個資料集**,
> 只是取用路徑不同(RS 走 `read_cached()` + `_back_adjust()`、regime 走 `load_benchmark()`
> → `cached_fetch_history()` → 同一個 `read_cached()` + 同一個 `_back_adjust()`)。
> 實測兩條路徑輸出**逐列相同**(1,827 列,max|Δ|=0.0)。見 `scripts/a1a2_isolation_check.py`。

- `rs_3m` 第一個有值的 as_of:**2019-04-30**
- `rs_6m` 第一個有值的 as_of:**2019-07-31**
- 257 個月裡有 **174 個月** `rs_6m=None` → **(A2) RS ±8 整段不計分**

### A5 落在 H5 六個時代

| 時代 | 月數 | regime 生效 | RS 生效 | f_val 路徑① |
|---|---|---|---|---|
| 05-09 | 60 | ❌ 全 neutral | ❌ | ❌ |
| 10-14 | 60 | ❌ 全 neutral | ❌ | ❌ |
| 15-18 | 48 | ❌ 全 neutral | ❌ | ❌ |
| 19-21 | 36 | ✅ | ✅ | ✅ |
| 2022 | 12 | ✅ | ✅ | ✅ |
| 23-26 | 41 | ✅ | ✅ | ✅ |

**乾淨的 3 : 3 分割。**

### H5 報告語句的修正(逐字採用 Codex §六-A 的要求)

> **H5 判定本身未被回溯修改;但跨時代期間並非完全使用同一套資料可得性與評分定義,
> 因此「跨時代穩健」的解釋需要加上限制。**

具體限制:前三段(05-09 / 10-14 / 15-18)測的是「**無 RS、regime 從未生效、f_val 走 PEG 路徑**」的版本。

---

## 3. 缺值語意 —— 部分已覆核

見 §6 的量測結果。靜態追溯(`build_pit_stockdata` 的預設填值 → 兩個引擎):

| 欄位 | 缺值 → 填什麼 | → 分數 | 語意 |
|---|---|---|---|
| `pe_vs_industry` | `10.0` | bounds 30→0 / 10→100 → **100 分** | ❌ 最好分 |
| `debt_to_asset` | `0.0`(初值) | 外插後 clip → **100 分** | ❌ 最好分 |
| `roe` / `net_margin` / `gross_margin` | `0.0`(初值) | **0 分** | ❌ 最差分 |
| `current_ratio` | `100.0`(初值) | bounds 100→0 → **0 分** | ❌ 最差分 |
| `eps_cagr` | `0.0` | **0 分**,且因「有值」被計入 growth 平均 | ❌ 最差分 |
| 傳 `None` 的欄位 | — | `_avg_present()` 排除;整組皆缺 → 50 | ✅ 中性 |

**實測(直接餵「三大財報全缺 + 無 PE」給 `FundamentalEngine.evaluate()`)**:

```
組分數: profitability 0.0  growth 0.0  safety 50.0  valuation 100.0
total_score = 32.5    is_passed = True          ← 硬門檻照樣放行
(月營收在、rev_cagr=10 → 41.88)
```

**兩個方向相反的錯互相抵銷,產出「看起來正常的中低分」**(全 panel f_fund 中位 46.6)——
缺資料的列在分布上不露馬腳。這是本專案典型的靜默失效。

---

## 4. 量尺與並列(`face_lineage_audit.py` §1/§2/§6,ADV≥100萬 259,652 列)

| 面 | 相異值 | =0% | =100% | 中位 | 月內std | Top-20%並列% | 月間rank自相關 | Top-20%留存率 |
|---|---|---|---|---|---|---|---|---|
| 基本面 | 8,375 | 0.01 | 0.00 | 46.6 | 15.00 | 3.0 | 0.912 | 80.8% |
| 估值 | 10,001 | 1.53 | 1.76 | 61.4 | 29.04 | 0.6 | 0.776 | 67.1% |
| **技術** | **91** | 0.26 | 0.01 | 54.0 | 21.21 | **24.7** | **0.058** | **23.5%** |
| 動能 | 101 | 2.14 | 0.07 | 26.0 | 15.79 | 8.5 | 0.618 | 56.1% |
| **籌碼** | 208,860 | **8.17** | **7.58** | 48.9 | 26.74 | 0.9 | **0.163** | 34.6% |
| real_composite | 7,028 | 0 | 0 | 43.3 | 10.98 | 0.2 | 0.542 | 48.1% |
| c2 | — | — | — | — | — | — | 0.591 | 47.8% |

**名目權重 vs 有效權重(五面未標準化就加權平均)**:

| 面 | 名目 w | 月內 std | w×std | **有效權重** | 與綜合分 rank corr |
|---|---|---|---|---|---|
| 基本面 | 0.31 | 15.00 | 4.650 | 0.24 | 0.543 |
| 估值 | 0.08 | 29.04 | 2.323 | 0.12 | 0.208 |
| 技術 | 0.19 | 21.21 | 4.031 | 0.21 | **0.609** |
| 動能 | 0.27 | 15.79 | 4.263 | 0.22 | **0.635** |
| 籌碼 | 0.15 | 26.74 | 4.011 | 0.21 | 0.527 |

名目 0.31 : 0.08 的 4 倍差,在有效層只剩 2 倍。**兩個 IC 顯著為負的面實際佔 0.42(名目 0.34)。**

---

## 5. dynamic weights / regime 的影響 —— ✅ 已覆核,見 §6

程式碼層(確定):`real_composite` = `advisor.advise()` 之後的 `total_score`,
`score_store.py:206` 明確設 `advisor.current_regime = _regime_at(as_of)`。
→ **regime 乘數、per-stock 動態權重、valuation 覆寫、washout 覆寫四者全部混在 `real_composite` 裡。**

面板層(問題):`build_realbody_scores._score_stock()` 只取 8 欄,
**沒有取 `dyn_weight` / `regime`** —— 而 `score_store.COLUMNS` 這兩欄是有的。
→ 面板存了結果,沒存產生結果的狀態。

---

## 6. real_composite 合成算術重算(抽樣)+ 缺值比例 —— ✅ **已覆核**

> **⚠ 兩個措辭限制(2026-07-31 依 Codex 第二輪審查修正,以下全節一律適用)**
>
> 1. **這不是全 panel 對帳。** 原始面板 263,928 列,本節只查了
>    **60 檔等距抽樣、7,369 stock-months**。所有「100%」一律讀作
>    **「在 60 檔等距抽樣、7,369 stock-months 中,三層對帳 100% 通過」**,
>    **不得**寫成「整份面板已 100% 對帳」。
> 2. **這不是完整的第二條獨立實作。** 它是**獨立的合成算術重算** ——
>    `_indep_composite()` 依規格重寫權重相乘、正規化與 bucket 替換,不呼叫 `advisor.advise()`;
>    但判斷式 `_bull_stack` / `_detect_washout` **共用 advisor 的**。
>    → 可作為**診斷證據**,**不得**宣稱已滿足研究紀律 §3 要求的「第二條獨立實作逐月對帳」。

`scripts/composite_reconcile.py --stocks 60`,60 檔等距抽樣 × 全部月份 = **7,369 stock-months**
(明細 `beat_0050/results/composite_reconcile_60.csv`)。抽樣率 **2.79%**(7,369 / 263,928)。

**為什麼預測器不換**:`build_pit_stockdata` + 五個引擎是**被測對象**,換掉就不是對帳了。

### B1 面板的 `f_*` 是原始分數,不是 bucket

| 比對 | 一致率 |
|---|---|
| 重跑的原始 `f_val` == 面板 `f_val` | **100.00%** |
| 重跑的原始 `f_whale` == 面板 `f_whale` | **100.00%** |

實測 bucket ≠ 原始分的列:valuation 覆寫 **254 列(3.45%)**、washout 覆寫 **0 列(0.00%)**。

### B2 三層對帳 —— **在抽樣的 7,369 列中**全部 100%

| 比對 | \|Δ\|≤0.01 | 中位 | max |
|---|---|---|---|
| 獨立算術 vs 生產 advisor(同一次執行) | **100.00%** | 0.0000 | 0.0000 |
| 生產 advisor **重跑** vs 面板 `real_composite` | **100.00%** | 0.0000 | 0.0000 |
| 獨立算術 vs 面板 `real_composite` | **100.00%** | 0.0000 | 0.0000 |

> 第 2 列的意義:**在抽樣範圍內面板沒有過期**。用今天的碼重跑那 7,369 列,逐列與
> 2026-07-30 建的面板完全相同。這同時反向證明本輪的 import 修復沒有動到那些分數。
> **剩下的 256,559 列(97.2%)未查** —— 全 panel 的對帳要等 Phase 1 的診斷面板落地
> (它會逐列重算,屆時可與 V0 面板做全量比對)。

### B3 殘差歸因 —— 40% 對不上的部分**已被四項完全解釋**

以「靜態名目權重 × 面板五面」為基準,逐步加上四個機制,量每一步的邊際貢獻:

| 來源 | 平均貢獻 | \|貢獻\| 中位 | \|貢獻\|>0.01 的列% |
|---|---|---|---|
| regime 乘數 | −0.255 | 0.000 | **33.42%** |
| dynamic weights | +0.017 | 0.000 | 8.96% |
| valuation 覆寫 | +0.116 | 0.000 | 3.45% |
| washout 覆寫 | +0.000 | 0.000 | **0.00%** |
| **四項合計(explained)** | −0.123 | 0.000 | **40.17%** |
| 殘差總量(面板 − 靜態) | −0.123 | 0.004 | **40.09%** |

**未解釋殘差:\|≤0.01\| 100.00%,max 0.0050(= `round(x,2)` 的量化誤差)。**

→ **回答 Codex §六-B 最後一問:沒有第五個來源。** 主成分是 regime 乘數(33.4% 的列),
其次 dynamic weights(9.0%)與 valuation 覆寫(3.5%)。

### B4 面板沒存、但真的在動的四個狀態

| 狀態 | 分布 |
|---|---|
| `regime` | neutral 66.2% / bull 26.1% / bear 7.7% |
| dynamic weights 啟用 | **9.04%** |
| valuation 覆寫 | 3.45% |
| washout 觸發 | **0.00%** ← 見下 |

### B5 ⚠ 新發現:`washout` 在研究路徑是**死碼**

`advisor._detect_washout()` 的四個條件之一是 `stock.margin_change_pct <= -8.0`,
而 **`build_pit_stockdata()` 從來沒有設定 `margin_change_pct`** ——
它只在 live 的 `data_provider._fetch_full_stock_data():1827` 被填。
PIT 路徑一律吃 `StockData` 的預設值 `0.0` → `retail_exit` 恆為 False → **washout 永不觸發**。

實測:`build_pit_stockdata(bt_fetch_history("2330"), "2024-06-28").margin_change_pct == 0.0`;
7,369 列抽樣中 washout 觸發 **0 次**。

**含意**:這是 live 與研究的**第四層落差**(前三層:bt_bundle 估值窗、bt_bundle 籌碼源、c2 取樣池)。
線上 App 的 composite 有一個「洗盤尾聲把籌碼分拉到 ≥65」的機制,**研究面板完全沒有**。
`GPT answer.md` 說的「H1–H5 通過只代表這個完整策略包通過」——
這裡要再補一句:**那個策略包本身也不完全等於線上跑的那一個。**

### C 缺值比例(只報分布與影響,不修公式)

| 情況 | 列數 | 比例 |
|---|---|---|
| PE 缺失(`stock.pe_ratio is None`) | 370 | **5.02%** |
| 損益表缺失(as_of 當下無已公告列) | 123 | 1.67% |
| 資產負債表缺失 | 123 | 1.67% |
| 現金流量表缺失 | 123 | 1.67% |
| **三大財報同時缺失** | 123 | **1.67%** |
| valuation 面板值 ≠ 合成用值 | 254 | 3.45% |
| whale 面板值 ≠ 合成用值(washout) | 0 | 0.00% |

**兩個缺值有完全相反的時間結構**:

| 年 | 三大財報全缺% | PE 缺失% |
|---|---|---|
| 2005 | **57.21** | 0.47 |
| 2006–2018 | **0.00**(逐年皆 0) | 0.0–7.2 |
| 2019 | 0.00 | **11.25** |
| 2020–2026 | 0.00 | **7.2–10.7** |

- **三大財報全缺完全集中在 2005**(TEJ 財報種子的起點之前),之後歸零。
- **PE 缺失反而在 2019 之後升高到 ~10%** —— 每 10 列就有 1 列的
  `pe_vs_industry` 被填 10.0 → 基本面的估值腿拿 **100 分**(佔 f_fund 的 0.20)。
  這是**現在進行式**,不是歷史殘留。

### C2 缺財報那批列的 `f_fund` 長相

| | 中位 | p10 | p90 |
|---|---|---|---|
| 缺損益表+資產負債表(123 列) | **33.5** | 15.6 | 35.6 |
| 其餘(7,246 列) | **45.8** | 24.9 | 65.4 |

**修正我在血緣稽核 §5-3 的說法**:先前寫「兩個相反的錯互相抵銷成看起來正常的中低分」——
更精確的說法是:**它們被系統性壓低約 12 分並擠在一個很窄的帶(p10–p90 只有 15.6→35.6)**,
但整段落在正常分布的下四分位內(其餘列的 p10 是 24.9),
所以**看分布抓不到、看單列也不覺得異常**,只有拿 `data_gaps` 對照才會現形 —— 而面板沒存那一欄。

→ 直接後果:**2005 年的 f_fund 系統性偏低**(該年 57% 的列缺三大財報),
與 §2 的 2019 斷裂點疊加,使「2005–2018 這三個時代」的分數同時受**兩種**結構性偏誤影響。

---

## 6-A. Phase 2 診斷結果(2026-07-31,全 panel 263,928 列)

> 重現:`python scripts/phase2_diag_analysis.py`(唯讀診斷面板)
> `python scripts/phase2_is_passed_sample.py --stocks 40`(硬門檻通過率,面板沒存,只能抽樣)
>
> **本節完全不碰報酬線** —— 沒有 join `exec_ret`,沒有任何 IC / 績效計算。
> 全部是「分數與狀態的分布」,**不是策略結論**。

### 6-A-1 regime / dynamic weight 的實際有效權重

**六種 (regime × dyn) 組合的正規化後有效權重**:

| regime | dyn | 列數 | 佔比 | 基本面 | 估值 | 技術 | 動能 | 籌碼 |
|---|---|---|---|---|---|---|---|---|
| neutral | False | 157,650 | **59.73%** | 0.310 | 0.080 | 0.190 | 0.270 | 0.150 |
| bull | False | 63,264 | 23.97% | 0.299 | 0.077 | 0.192 | 0.287 | 0.145 |
| **bear** | False | 18,880 | 7.15% | **0.358** | **0.055** | **0.066** | **0.468** | **0.052** |
| neutral | **True** | 15,448 | 5.85% | 0.310 | **0.032** | 0.190 | 0.299 | 0.169 |
| bull | True | 6,730 | 2.55% | 0.299 | 0.031 | 0.192 | 0.314 | 0.163 |
| bear | True | 1,956 | 0.74% | 0.358 | **0.022** | 0.066 | 0.488 | 0.065 |

| 面 | 名目 | 平均 | p5 | p50 | p95 | ≠名目的列% |
|---|---|---|---|---|---|---|
| 基本面 | 0.31 | 0.311 | 0.299 | 0.310 | 0.358 | 34.41% |
| 估值 | 0.08 | 0.073 | **0.032** | 0.080 | 0.080 | **40.27%** |
| 技術 | 0.19 | 0.181 | **0.066** | 0.190 | 0.192 | 34.41% |
| 動能 | 0.27 | 0.293 | 0.270 | 0.270 | **0.468** | **40.27%** |
| 籌碼 | 0.15 | 0.143 | **0.052** | 0.150 | 0.169 | **40.27%** |

> **五面全部等於名目權重的列只有 59.73%。**
> 文件與程式註解裡宣稱的「fund .31 / val .08 / tech .19 / mom .27 / whale .15」
> 適用於**不到六成**的觀測。另外四成用的是另外五組權重之一。

### 6-A-2 缺值與 data_gaps 對五面分數的影響

| 分組 | 列數 | 佔比 | 基本面中位 | 估值中位 | 技術中位 | 動能中位 | 籌碼中位 |
|---|---|---|---|---|---|---|---|
| 全體 | 263,928 | 100% | 46.5 | 61.1 | 54.0 | 26.0 | 48.9 |
| 無任何缺口 | 209,048 | 79.21% | 48.4 | 65.0 | 55.0 | 27.0 | 49.3 |
| 有 `data_gaps` | 53,411 | **20.24%** | 38.8 | 46.2 | 51.0 | 21.0 | 48.0 |
| **PE 缺失** | 8,306 | 3.15% | **51.3** | 6.5 | 55.0 | 27.0 | 48.0 |
| 三大財報全缺 | 5,436 | 2.06% | 34.8 | 60.6 | 51.0 | 23.0 | 48.0 |
| `val_override` | 3,804 | 1.44% | 59.9 | **0.0** | 58.0 | 35.0 | 48.0 |

**PE 缺失的方向是反的**:f_fund 中位 **51.3**(PE 有值 46.3),**高 5.0 分**。
(此為**全 panel** 結果 —— `pe_missing` 與 `f_fund` 都在診斷面板裡。)
機制已確認:`pe_vs_industry` 缺 → 填 10.0 → bounds 30→0/10→100 → **估值腿 100 分**,佔 f_fund 的 0.20。

**哪些結果是全 panel、哪些是抽樣 —— 一覽(Codex 第三輪要求明確區分)**:

| 全 panel 已確認(263,928 列) | 抽樣診斷(40 檔 / 4,307 列) |
|---|---|
| `data_gaps` 20.24% 與逐年分布 | `is_passed` 通過率 |
| `pe_missing` 3.15% 與 f_fund 位移 | 獲利 / 成長 / 安全 / 估值 **組分數** |
| 三大財報全缺 2.06%(2005 集中) | `FundamentalEngine` 引擎信心 |
| `regime` 分布與有效權重 | — |
| `dyn_weight` 9.14% | — |
| `val_override` 1.44% 與 +50.0 分位移 | — |
| `washout_override` 0 / 263,928 | — |
| `valuation_basis` 五種分布 | — |
| 六時代的有效權重 | — |

**硬門檻通過率 —— ⚠ 抽樣診斷,不是全 panel(Codex 第三輪指定的限制)**

> **本小表的每一個數字都只來自 40 檔等距抽樣 / 4,307 stock-months**
> (全 panel 是 2,139 檔 / 263,928 列,抽樣率 **1.63%**)。
> `is_passed` 與四個組分數(獲利 / 成長 / 安全 / 估值)**不在診斷面板裡** ——
> `FundamentalEngine.evaluate()` 的門檻判定與組分數都沒被存下來,要量就得重建 bundle,
> 所以只能抽樣。**這五個欄位的任何結論一律標「抽樣診斷」,不得寫成全 panel 結果。**
> 同表的 `f_fund 中位` 雖然全 panel 有,但此處列的是**抽樣子集**的值,一併適用上述限制。

| 分組(**抽樣 n=4,307**) | 列數 | `is_passed`% | f_fund 中位 | 引擎信心中位 | 獲利 | 成長 | 安全 | 估值 |
|---|---|---|---|---|---|---|---|---|
| 全體 | 4,307 | 96.47% | 47.7 | 100.0 | 29.5 | 36.1 | 66.0 | 70.7 |
| **三大財報全缺** | 97 | **100.00%** | 31.6 | **100.0** | **0.0** | 12.5 | **50.0** | **80.0** |
| PE 缺失 | 212 | 78.77% | 50.0 | 100.0 | 25.0 | 18.0 | 52.9 | **100.0** |
| 有 `data_gaps` | 881 | 89.56% | 40.7 | 100.0 | 22.5 | 12.5 | 54.0 | 95.0 |
| 無任何缺口 | 3,415 | 98.24% | 49.4 | 100.0 | 32.0 | 50.0 | 68.3 | 65.5 |

⚠ 「三大財報全缺」那一列只有 **97 個觀測**,`is_passed = 100.00%` 的信賴區間很寬
(Wilson 95% 下界約 96.2%)。方向可信,**精確值不可引用**。

三件事同時成立(**以下三句都是抽樣診斷**):
1. **三大財報全缺的 `is_passed` 是 100%,比「無任何缺口」的 98.24% 還高。** 硬門檻完全攔不到它。
2. 組分數呈現「獲利 0.0 / 安全 50.0 / **估值 80.0**」的人造形狀 —— 兩個方向相反的填值互相抵銷。
3. **`FundamentalEngine` 的信心分數對這批列是 100.0** —— 因為 `missing_fields` 是空的
   (欄位都「有值」,只是值是捏出來的)。**信心指標完全看不到這件事。**

`data_gaps` 逐年:2005 **63.28%**(三表缺 60.2%),2006–2018 約 9–16%,
**2019 起跳到 35–44%**(2019 38.82 / 2020 35.80 / 2023 **44.05** / 2025 24.83)。
主要成分:「成長率(PEG)」9.27%、「月營收;股權結構」4.20%、「pe」2.38%。
→ **缺口不是歷史殘留,近年反而更嚴重。**

### 6-A-3 washout 在 live 與研究路徑的差異

| | live | 研究 / PIT |
|---|---|---|
| `margin_change_pct` | `data_provider._fetch_full_stock_data():1827` 會填 | **`build_pit_stockdata()` 從未設定** → 恆為 dataclass 預設 `0.0` |
| `retail_exit = margin_change_pct <= -8.0` | 可能為 True | **恆為 False** |
| washout 觸發 | 可能 | **0 / 263,928 列(0.0000%)** |

**影響上界估計**(量落差,不是策略結論):`f_whale < 65` 的列佔 **72.92%**;
若對這些列全部套用 washout,籌碼分平均墊高 **28.4 分**(中位 22.0、最大 65.0),
換算到 composite(籌碼名目權重 0.15)平均 **4.25 分**。
⚠ 這是**上界** —— live 實際觸發率遠低於 100%(要三個條件同時成立),
但研究端的觸發率是**確定的 0%**。真實落差落在 0 與上界之間,**無法從研究面板回推**。

### 6-A-4 原始五面分數 vs 實際合成 bucket

| 面 | b≠f 的列 | 佔比 | 平均 b−f | max\|b−f\| |
|---|---|---|---|---|
| 基本面 | 0 | 0.00% | 0.0000 | 0.00 |
| **估值** | **3,804** | **1.44%** | +0.7207 | **50.00** |
| 技術 | 0 | 0.00% | 0.0000 | 0.00 |
| 動能 | 0 | 0.00% | 0.0000 | 0.00 |
| 籌碼 | 0 | 0.00% | 0.0000 | 0.00 |

估值覆寫的那 3,804 列:面板 `f_val` **100% 都是 0.0**,合成時一律用 **50.0**
→ 差 **+50.0 分**,換算到 composite **+4.00 分**。
籌碼 0 列 —— 因為 washout 從未觸發(6-A-3)。

**`valuation_basis` 全 panel 分布(估值其實有 5 條路徑,不是 3 條)**:

| 路徑 | 列數 | 佔比 |
|---|---|---|
| 產業內位階 | 99,406 | 37.66% |
| **歷史位階(單獨)** | 85,494 | **32.39%** |
| 成長(PEG)+歷史位階 | 72,934 | 27.63% |
| 絕對 | 4,844 | 1.84% |
| 成長(PEG)(單獨) | 1,250 | 0.47% |

> **更正先前的說法**:我在 §1 血緣圖寫「三條互斥路徑」,實際落地是 **5 種 basis** ——
> 路徑② 會因 `peg_score` / `rel_score` 誰有值而分裂成三種不同配方
> (85/15 混合、純位階、純 PEG)。**純位階佔 32.39%,是第二大宗。**
> 而 `valuation.py:149-154` 的註解只論證了 85/15 混合的比例,**沒有論證純位階那一支**。

### 6-A-5 2019 前後與 H5 六時代的有效評分定義

| 時代 | 月 | 列數 | bear% | bull% | dyn% | valOv% | 產業位階% | PE缺% | 三表缺% | w_fund | w_val | w_tech | w_mom | w_whale |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 05-09 | 60 | 48,512 | **0.00** | **0.00** | 13.07 | 0.03 | **0.00** | 1.31 | **11.21** | 0.310 | 0.074 | 0.190 | 0.274 | 0.153 |
| 10-14 | 60 | 56,141 | **0.00** | **0.00** | 7.60 | 0.55 | **0.00** | 1.55 | 0.00 | 0.310 | 0.076 | 0.190 | 0.272 | 0.151 |
| 15-18 | 48 | 51,228 | **0.00** | **0.00** | 6.95 | 2.66 | **0.00** | 3.88 | 0.00 | 0.310 | 0.077 | 0.190 | 0.272 | 0.151 |
| 19-21 | 36 | 40,611 | 14.02 | 61.83 | 9.63 | 2.07 | 87.45 | 4.33 | 0.00 | 0.310 | 0.071 | 0.174 | 0.311 | 0.135 |
| **2022** | 12 | 15,089 | **74.99** | 16.77 | 6.61 | 1.71 | 95.74 | 3.42 | 0.00 | 0.344 | **0.059** | **0.097** | **0.423** | **0.077** |
| 23-26 | 41 | 52,347 | 7.31 | 80.91 | 9.66 | 1.95 | 94.46 | 4.85 | 0.00 | 0.305 | 0.072 | 0.183 | 0.301 | 0.140 |

2019 分界:

| | 列數 | bear% | bull% | 產業位階% | 平均 w_mom | 平均 w_tech |
|---|---|---|---|---|---|---|
| 2019 前 | 155,881 | **0.00** | **0.00** | **0.00** | 0.273 | 0.190 |
| 2019 起 | 108,047 | 19.28 | 64.78 | 92.00 | **0.321** | **0.168** |

加上 RS(不在面板欄位;`breakpoint_2019_audit.py` 量到 `rs_6m` 首見 **2019-07-31**):

> **六個時代不是同一套評分定義在跑。**
> 前三段:regime 恆 neutral(有效權重 ≈ 名目)、RS 不計分、估值 0% 走產業內位階。
> 後三段:regime 逐月切換、RS 生效、估值 87–96% 走產業內位階。
> **2022 那一段的動能有效權重是 0.423、技術 0.097** —— 與名目的 0.27 / 0.19 是兩回事。

---

## 7. 問題分類與嚴重度

| # | 問題 | 類別 | 嚴重度 | 影響誰 | 覆核狀態 |
|---|---|---|---|---|---|
| 1 | 2019 三個定義斷裂點(regime / RS / f_val) | **資料問題** | 🔴 高 | H5 的**解釋**、所有跨 2019 的全期 IC | ✅ 已獨立覆核(§2) |
| 2 | 面板沒存 regime / dyn / 覆寫 / data_gaps | **資料問題** | 🔴 高 | 任何合成層歸因都只能反推 | ✅ 已覆核(§5/§6) |
| 3 | 缺值語意三種相反處置 | **定義問題** | 🔴 高 | f_fund 的尾部與缺財報列 | ✅ 靜態確定 + §6 量測 |
| 4 | 技術面 91 個相異值 / 並列 24.7% / 自相關 0.058 | **定義問題**(量尺) | 🔴 高 | 排序不可重現、換手率 | ✅ 已量測 |
| 5 | 籌碼面 15.75% 撞 clip 邊界 | **定義問題**(量尺) | 🟠 中 | f_whale 尾部 | ✅ 已量測 |
| 6 | 五面未標準化就加權 → 名目≠有效 | **權重問題**(但根因在量尺) | 🟠 中 | 全部 | ✅ 已量測 |
| 7 | f_fund 的估值腿與 f_val 同源 | **定義問題** | 🟠 中 | 五維不正交 | ⏳ 待覆核(未量重疊度) |
| 8 | `bt_bundle` 兩個未預註冊的資料覆寫 | **資料問題** | 🟠 中 | 2019 前的 f_val、全期 f_whale | ⏳ 待覆核(未量影響量) |
| 9 | c2 的 `100−momentum%` 與 f_tech ρ=−0.616 | **定義問題**(假說) | 🟡 待驗 | 「雙確認為何有效」的機制解釋 | ⏳ **高可信度假說,非結論** |
| 10 | `revenue_yoy%` ↔ f_mom 營收腿 ρ=+0.322(真重複) | **定義問題** | 🟡 低 | c2 ∩ composite 的獨立性 | ✅ 已量測 |
| 11 | 產業對照表非 PIT(今天的分類套到 2005) | **資料問題** | 🟡 低 | `is_financial` / `sector_category` | ⏳ 待覆核 |
| 12 | 產業表四層吞錯鏈(已加 strict 閘門) | **資料問題** | 🟠 中 | f_fund 的金融股豁免 | ✅ 已修閘門(見 血緣稽核 §1-4) |
| 13 | **`washout` 在研究路徑是死碼**(`margin_change_pct` 從未被 PIT 路徑設定) | **定義問題** | 🟠 中 | 研究面板的 composite ≠ 線上 composite | ✅ 已覆核(§6-B5) |
| 14 | **PE 缺失在 2019 後升到 ~10%**,每次都讓基本面估值腿拿 100 分 | **資料問題** | 🔴 高 | f_fund(0.20 權重),**現在進行式** | ✅ 已量測(§6-C) |
| 15 | 2005 年 57% 的列缺三大財報 → 該年 f_fund 系統性低 12 分 | **資料問題** | 🟠 中 | 05-09 時代的所有結論 | ✅ 已量測(§6-C2) |

**沒有一項是「權重數字錯了」。** 目前定位到的全部是**資料可得性**與**定義/量尺**層。
這與 `現況盤點_2026-07-29.md` §L4 的「定義病 >> 量尺病」方向一致,但本輪把「量尺病」再拆出了
「**未標準化就加權**」這個獨立成因。

---

## 7-A. Phase 2 的正式問題分類(Codex 第三輪指定的三分類)

> 三分類的判準:
> **資料問題** = 換一份資料就會消失(修法是補資料 / 加閘門,不動公式)。
> **定義問題** = 資料再完整也還在(修法是改因子定義或量尺,**必須新預註冊**)。
> **合成架構問題** = 不在任何單一維度裡,在「五面怎麼變成一個分數」這一層
> (修法是改合成流程,**必須新預註冊**,而且會同時動到所有維度)。

### A. 資料問題(9 項)

| # | 問題 | 全 panel 證據 | 嚴重度 | 修法 |
|---|---|---|---|---|
| D1 | **regime 在 2019 前從未生效** | 2019 前 155,881 列 bear 0.00% / bull 0.00% | 🔴 | 補 0050 歷史(repo 內 `0050_raw.parquet` 有 2003 起) |
| D2 | **RS(f_mom A2 ±8)在 2019-07 前不計分** | `rs_6m` 首見 2019-07-31,174/257 月不計分 | 🔴 | 同 D1(同一份 0050) |
| D3 | **f_val 產業內位階在 2019-04 前 0% 覆蓋** | 2019 前產業位階 0.00%,2019 起 92.00% | 🔴 | 補 `industry_value_ref` 歷史,或接受分段 |
| D4 | **PE 缺失讓基本面估值腿拿 100 分** | 8,306 列(3.15%);f_fund 中位 **高 5.0 分**;抽樣估值組中位 **100.0** | 🔴 | 補 PE 資料 **或** 改缺值填法(後者屬定義問題) |
| D5 | **`data_gaps` 近年更嚴重** | 2019 38.82% / 2023 **44.05%**,全期 20.24% | 🔴 | 補資料;先查「成長率(PEG)」9.27% 為何缺 |
| D6 | 2005 年三大財報缺 60.2% | 05-09 段 11.21%,其餘年 0.00% | 🟠 | 補 2005 財報種子 或 排除該年 |
| D7 | `bt_bundle` 兩個未預註冊的資料覆寫 | 估值窗 2019→2004、籌碼改淨額 | 🟠 | 寫進預註冊的「已知落差」;或還原 |
| D8 | 產業對照表非 PIT(今天的分類套到 2005) | — | 🟡 | 建 PIT 產業表 |
| D9 | 產業表四層吞錯鏈 | — | 🟠 | ✅ **已修**(strict 閘門 + 空表不入快取) |

### B. 定義問題(7 項)

| # | 問題 | 全 panel 證據 | 嚴重度 | 為何是定義問題 |
|---|---|---|---|---|
| F1 | **技術面只有 91 個相異值 / Top-20% 並列 24.7% / 月間自相關 0.058** | — | 🔴 | if/elif 分桶的產物,補資料救不了 |
| F2 | **缺值語意三種相反處置**(缺 PE→100 分、缺資產負債表→100 分、缺損益表→0 分) | 抽樣:三表全缺 `is_passed` **100%**、引擎信心 **100.0** | 🔴 | 填值規則寫在 `build_pit_stockdata`,是定義的一部分 |
| F3 | 籌碼面 15.75% 撞 clip 邊界 | =0 8.17% / =100 7.58% | 🟠 | `48 + ratio×300` 的斜率選擇 |
| F4 | f_fund 的估值腿(0.20)與 f_val 同源 | — | 🟠 | 五維不正交 |
| F5 | **估值其實有 5 種 basis,不是 3 條路徑** | 產業位階 37.66% / **純位階 32.39%** / PEG+位階 27.63% / 絕對 1.84% / 純 PEG 0.47% | 🟠 | `valuation.py:149-154` 只論證了 85/15 混合,**沒論證純位階那一支** |
| F6 | `revenue_yoy%` ↔ f_mom 營收腿 ρ=+0.322(與 c2 真重複) | — | 🟡 | — |
| F7 | c2 的 `100−momentum%` 與 f_tech ρ=−0.616(抵銷假說) | — | 🟡 待驗 | **高可信度假說,非結論** |

### C. 合成架構問題(5 項)—— 本輪新增的第三類

| # | 問題 | 全 panel 證據 | 嚴重度 | 為何是架構問題 |
|---|---|---|---|---|
| **S1** | **名目權重只適用 59.73% 的列** | 六組 (regime×dyn) 權重,估值/動能/籌碼各有 **40.27%** 的列 ≠ 名目 | 🔴 | 不是某個權重數字錯,是「權重是個變數」這件事沒被承認 |
| **S2** | **五面未標準化就加權** | 有效權重被壓成 0.12–0.24;名目 4 倍差只剩 2 倍 | 🔴 | 合成層的量綱問題,與任一維度的定義無關 |
| **S3** | **bucket ≠ 面板存的分數** | 估值 3,804 列(1.44%)差 **+50.0 分** → composite **+4.00 分** | 🟠 | ✅ 診斷面板已解(`b_*` 已存),但**任何用 `f_*` 重建 composite 的舊分析都偏低** |
| **S4** | **washout 在研究路徑是死碼** | 0 / 263,928 列;影響上界 composite 4.25 分 | 🟠 | 研究面板的合成流程 ≠ live 的合成流程 |
| **S5** | **六個時代不是同一套合成定義** | 前三段有效權重≈名目;2022 段 w_mom **0.423** / w_tech **0.097** | 🔴 | D1–D3 的**後果**落在合成層:H5 的跨時代解釋受限 |

---

## 7-B. 需要新預註冊的項目(Codex 第三輪 item 7)

> 判準:**任何會改變 `real_composite` 逐列數值的動作,都要新預註冊。**
> 只增加觀測欄位、只補閘門、只寫文件的,不需要。

### 必須新預註冊(會動到分數)

| # | 工作 | 對應問題 | 為什麼 |
|---|---|---|---|
| P-1 | **補 0050 歷史後讓 regime / RS 在 2005–2018 生效** | D1 / D2 / S5 | 2019 前 155,881 列的權重與 f_mom 會全部改變 = 換了策略 |
| P-2 | **改缺值填法**(`pe_vs_industry` 10.0 / `debt_to_asset` 0.0 / `current_ratio` 100.0 / `eps_cagr` 0.0 → 中性化) | D4 / F2 | 會改變每一列的 f_fund |
| P-3 | **五面標準化 / 百分位化後再加權** | S2 | 改變所有排序 |
| P-4 | **技術面量尺重寫**(if/elif 分桶 → 連續函數) | F1 | 新因子定義 |
| P-5 | **籌碼面去 clip 邊界** | F3 | 同上 |
| P-6 | **動能面四腿拆分後重組** | F6 | 同上 |
| P-7 | **是否保留 `regime_multipliers` 進入選股** | S1 / S5 | 拿掉 = 40.27% 的列換權重 |
| P-8 | **是否保留 `_dynamic_weights`** | S1 | 同上(9.14% 的列) |
| P-9 | **是否把 washout 補進研究路徑**(填 `margin_change_pct`) | S4 | 會讓 72.92% 的列有機會被墊高 |
| P-10 | **拿掉 f_fund 的估值腿** | F4 | 改變基本面定義 |
| P-11 | **估值 basis 統一**(5 種 → 決定要哪幾種) | F5 / D3 | 改變 f_val |
| P-12 | **任何權重配置研究** | — | 多重比較 / DSR,且候選數要事前凍結 |
| P-13 | 還原或正式承認 `bt_bundle` 的兩個覆寫 | D7 | 會改變 2019 前 f_val 與全期 f_whale |

### 不需要新預註冊(不動分數)

| 工作 | 理由 |
|---|---|
| Phase 1 診斷面板 | ✅ 已完成,只增加觀測欄位,`real_composite` 逐列 100% 相同 |
| Phase 2 診斷分析 | ✅ 已完成,只讀面板、不碰報酬線 |
| 子因子 in-sample 診斷(Phase 2 續) | 診斷不是結論;**但診斷數字不得寫成結論句** |
| 補 D5/D6 的**缺失資料**本身(不改填法) | 補資料會改分數 → ⚠ **其實會動分數,要預註冊**;此列僅指「先量清楚缺什麼」 |
| 產業表 strict 閘門 | ✅ 已完成,是防呆不是計分 |
| 修 `fetch_history` 快取代理 | 已證對研究結論零影響(獨立 issue) |

### 建議的預註冊順序(**提案,未凍結**)

1. **先 P-2(缺值中性化)** —— 它是唯一同時是資料問題與定義問題的項目,而且方向明確
   (缺值不該拿滿分也不該拿零分)。改完之後其他診斷才站在乾淨的 f_fund 上。
2. **再 P-1(補 0050)** —— 讓六個時代用同一套定義,H5 的解釋才不用一直加限制句。
3. **再 P-3 / P-4 / P-5(量尺層)** —— 一次一項,每項獨立驗證。
4. **最後 P-7 / P-8 / P-12(合成與權重)** —— 依 Codex §八 Phase 4,權重永遠排最後。

> ⚠ **P-1 與 P-2 目前只是候選假設(Codex 第三輪指定)**:
> **不得**據此修改任何程式、**不得**建立任何新面板。它們要進 `docs/預註冊_FaceRedesignV2.md`
> 走完凍結與審查,才可能變成可執行的工作。
> 「診斷已經證明缺值填法是錯的」**不構成**動手的理由 —— 那正是單發射擊制要防的事。

⚠ 以上**只是順序提案**。哪些進、哪些不進、門檻多少,**一律等 Codex 裁決後寫進新的預註冊文件並凍結**。

### Codex 第四輪裁決後的實際分批(2026-07-31)

草案見 **[`docs/預註冊_FaceRedesignV2_草案.md`](預註冊_FaceRedesignV2_草案.md)**。

| 批次 | 內容 | N |
|---|---|---|
| **第一批(草案已寫)** | A1 補0050→只開RS / A2 補0050→只開regime / A3 缺值中性化 / A4 f_val 配方統一 / C1 拿掉 regime / C3 五面百分位化 | **6** |
| 拆解診斷(不計入 N) | X1 c2/composite/交集三腿分開報 / X2 抵銷假說 | 0 |
| **第二批(另立預註冊)** | B1–B4(公式尚未寫死)、C2(與 C1 高度相關) | 待定 |
| **無法執行** | C4 washout —— **資料不存在**:`~/tej_cache/margin_balance/` 每檔只有 **72 列 / 2026-04-01~2026-07-16**,回測需要 2005–2026 的日資料 | — |

**A1 與 A2 必須一起執行**(Codex 第四輪 §5):不得先跑 A2 再依結果決定 A1 —— 那是用 OOS 縮小搜尋空間。

---

## 8. 下一階段順序(依 Codex §八,**尚未開跑**)

### Phase 1 — 測量層修正(不改任何策略公式)— 🟢 **Codex 第二輪已授權,建置器已就緒**

建置器:**`beat_0050/realbody/build_diag_panel.py`**(新檔,**不動** `build_realbody_scores.py`)。

**隔離(Codex 指定,雙保險)**:
1. 輸出目錄 `data/research_base/diag/` —— `available_realbody_panels()` 的 glob 非遞迴,不會下鑽。
2. 檔名 `diag_scores_adv100w_diag.parquet` —— 不符 `realbody_scores_adv*w.parquet`。

實測 `available_realbody_panels()` 仍只看得到兩份正規面板,`resolve_realbody(1e6)` 仍解析到
`realbody_scores_adv100w.parquet`。**診斷面板結構上不可能被當成正規面板讀走。**

**33 個欄位**(原有 9 + 新增 24,全部是觀測欄位,零個是新分數):

| 群組 | 欄位 |
|---|---|
| 鍵 / 權威分數 | `as_of` `stock_id` `real_composite` `rating` |
| 原始五面(= V0) | `f_fund` `f_val` `f_tech` `f_mom` `f_whale` |
| **實際合成 bucket** | `b_fund` `b_val` `b_tech` `b_mom` `b_whale` |
| **合成狀態** | `regime` `dyn_weight` `val_override` `washout_override` `valuation_basis` |
| **資料缺口** | `data_gaps` `n_raw_gaps` `pe_missing` `income_missing` `balance_missing` `cashflow_missing` |
| **錯誤** | `score_error` |
| **版本** | `mode` `weights_version` `builder_version` `code_commit` `panel_source` `built_at` |
| 自檢 | `composite_indep`(合成算術重算,非權威) |

**驗收閘門(硬性)**:每一列的 `real_composite` 必須與 V0 面板 `|Δ|≤0.01`,不同就 `SystemExit`。
→ 這順帶補上 §6 缺的那一塊:**全 panel(263,928 列)的逐列比對**,不再只是 2.79% 抽樣。

**冒煙測試(8 檔 / 1,731 stock-months / 92s,4 workers)**:

```
覆蓋率 100.0000%   缺 0
real_composite 逐列相同(|Δ|≤0.01):100.0000%   max|Δ| 0.0000
五面逐列相同:{f_fund: True, f_val: True, f_tech: True, f_mom: True, f_whale: True}
合成算術重算 vs advisor(自檢):100.0000%   max 0.0000
```

**單年驗證(`--year 2022`,6 workers,1,514 檔 / 15,089 stock-months / 396s)—— ✅ 全數通過**:

```
產出 15,089 / 期望 15,089   覆蓋率 100.0000%   缺 0
real_composite 逐列相同(|Δ|≤0.01):100.0000%   max|Δ| 0.0000
五面逐列相同:{f_fund: True, f_val: True, f_tech: True, f_mom: True, f_whale: True}
合成算術重算 vs advisor(自檢):100.0000%   max 0.0000
```

產物:`data/research_base/diag/diag_scores_adv100w_diag_partial_year2022.parquet`

**新增觀測欄位在 2022 的分布**:

| 欄 | 值 |
|---|---|
| `regime` | **bear 74.99%** / bull 16.77% / neutral 8.24% |
| `dyn_weight` | 6.61% |
| `val_override` | 1.71% |
| `washout_override` | **0.00%**(印證 §6-B5 的死碼結論) |
| `pe_missing` | 3.42% |
| 三大財報全缺 | 0.00% |
| 有 `data_gaps` | 19.64%(最大宗是「成長率(PEG)」2,332 列,其次「pe;pb;ps;dividend_yield」258 列) |

**⚠ 一個必須記下來的觀測(不是結論)**:2022 有 **10/12 個月**被判為 bear。
bear 乘數把正規化後的**有效名目權重**變成:

| regime | fund | val | tech | mom | whale |
|---|---|---|---|---|---|
| bull | 0.299 | 0.077 | 0.192 | 0.287 | 0.145 |
| neutral | 0.310 | 0.080 | 0.190 | 0.270 | 0.150 |
| **bear** | **0.358** | **0.055** | **0.066** | **0.468** | **0.052** |

→ 2022 有四分之三的列,**動能是 0.468、技術+籌碼合計只有 0.118**。
名目上宣稱的「fund .31 / val .08 / tech .19 / mom .27 / whale .15」在那一年**不是實際在跑的東西**。
H5 的「2022」那一段就是在這個權重下通過的。
**這只是觀測,不下任何策略結論**;要如何處置(是否保留 regime 進入選股)依 §9 必須走新預註冊。

### 全循環 —— ✅ **已完成(2026-07-31,4,772s / 79.5 分,6 workers)**

產物:`data/research_base/diag/diag_scores_adv100w_diag.parquet`(8.3 MB,263,928 列 × 33 欄)

```
產出 263,928 / 期望 263,928   覆蓋率 100.0000%   缺 0
與 V0 面板可比對的列:263,928(未能比對 0)
real_composite 逐列相同(|Δ|≤0.01):100.0000%   max|Δ| 0.0000
五面逐列相同:{f_fund: True, f_val: True, f_tech: True, f_mom: True, f_whale: True}
合成算術重算 vs advisor(自檢):100.0000%   max 0.0000
```

**五道致命條件全數未觸發**(覆蓋率<100% / `score_error`>0 / `real_composite` 不符 /
五面不符 / 有列無法比對)。V0 面板完整性複驗:md5 `6cf06bdee83228eba45ce05fc9462b0b`、
mtime `2026-07-30 02:53:44` —— **與啟動前存證逐位元相同,未被觸碰**。

> **這同時補上 §6 的抽樣缺口**:先前只查了 2.79%(7,369 / 263,928),
> 現在是**全 panel 263,928 列的逐列比對**。
> ⚠ 但**措辭限制不變**:這仍是「獨立的合成算術重算」,
> `_bull_stack` / `_detect_washout` 與預測器都共用生產的,
> **不得**宣稱已滿足研究紀律 §3 的「第二條獨立實作逐月對帳」。

**新增觀測欄位在全 panel 的分布**:

| 欄 | 全 panel | (參考)2022 單年 |
|---|---|---|
| `regime` | neutral **65.59%** / bull **26.52%** / bear **7.89%** | bear 74.99% |
| `dyn_weight` | **9.14%** | 6.61% |
| `val_override` | 1.44% | 1.71% |
| `washout_override` | **0.00%** | 0.00% |
| `pe_missing` | 3.15% | 3.42% |
| 三大財報全缺 | 2.06% | 0.00% |
| 有 `data_gaps` | **20.24%** | 19.64% |

`washout_override` 在**全部 263,928 列**都是 0 —— §6-B5 的「研究路徑死碼」結論
從抽樣升級為**全 panel 確證**。

**這一步只增加觀測欄位,不改分數、不改權重、不下 OOS 結論。**
後續要用這份面板做什麼(Phase 2 子因子診斷)**尚未開始**,等 Codex 第三輪。

### Phase 2 — 維度定義診斷(逐子因子)

| 面 | 拆開量 |
|---|---|
| 技術 | 均線 / RSI / MACD / 布林 / KD / 量價背離 |
| 動能 | 中期價格 / RS / 營收動能 / 短線確認 |
| 籌碼 | 外資 / 投信 / 主力 / 不同期間 / clipping |
| 基本面 | 獲利 / 成長 / 安全(+ 估值腿,見問題 7) |
| 估值 | 產業位階 / PEG / 歷史估值 |

每個子因子都要報:coverage、missing rate、unique values、tie rate、rank IC、Q5−Q1、
**2019 前後分段**、產業內中性化後結果、與 c2 的增量。

### Phase 3 — 新預註冊(先凍結才准看 OOS)

必須先凍結:V0 baseline / 每個 candidate arm / 方向 / 量尺 / 缺值處理 /
是否用 dynamic weights / 是否用 regime / 時間區間 / OOS / 門檻 / 多重比較 / DSR / 失敗處置。

**不得先看新 OOS,再決定哪些 arm 算正式結果。**

### Phase 4 — 最後才處理權重

禁止:看到負 IC 就翻符號 / 看到正 IC 就加重 / 大量 grid search 挑最好 /
用目前 H1–H5 結果重新調到 20–30%。

---

## 9. 哪些工作必須新預註冊

| 工作 | 需要新預註冊? | 理由 |
|---|---|---|
| Phase 1 面板加欄位 | ❌ 不需要 | 純測量,不改分數、不下判定 |
| Phase 2 子因子診斷(in-sample) | ❌ 不需要 | 診斷不是結論(研究紀律 §2)。**但診斷數字不得寫成結論句** |
| 任何維度**定義**變更 | ✅ **必須** | 新因子定義 = 新搜尋空間 |
| 任何**量尺**變更(百分位化 / z-score) | ✅ **必須** | 會改變排序,等同新因子 |
| 缺值處理改中性化 | ✅ **必須** | 同上 |
| 技術/籌碼/動能子因子拆分後的重組 | ✅ **必須** | 同上 |
| 是否保留 dynamic weights / regime | ✅ **必須** | 兩者都在 composite 裡,拿掉是定義變更 |
| 權重配置 | ✅ **必須**,且限制候選數 | 多重比較 / DSR |
| 修 `bt_bundle` 的兩個覆寫 | ✅ **必須** | 會改變 2019 前的 f_val 與全期 f_whale |
| 補 0050 歷史到 2003(解 A1/A4) | ✅ **必須** | 會讓 2005–2018 的 regime 與 RS 從「不生效」變成「生效」= 換了策略 |
| 修 `fetch_history` 快取代理 | ❌(另案) | 已證對研究結論零影響,見獨立 issue |

---

## 10. 不得回填到現有 V0 結論的事(**紅線**)

1. ❌ **不得**用「補了 0050 歷史之後的 regime」重算 H1–H5 再冒充 V0 的結果。
   補歷史 = 換策略,要走新預註冊。
2. ❌ **不得**把「H5 的解釋要加限制」寫成「H5 沒過」或「H5 要重跑」。判定不變,只加限制句。
3. ❌ **不得**把 Phase 1 的新診斷面板當成 V0 的面板 —— V0 的數字綁在
   `realbody_scores_adv100w.parquet`(2026-07-30 建)上。
4. ❌ **不得**因為稽核發現問題,就宣稱 V0 的 H1–H5「其實不成立」。
   稽核發現的是**解釋的邊界**,不是判定的推翻。
5. ❌ **不得**把 §6 的對帳結果解讀成「所以 real_composite 是對的」——
   對帳證明的是**算術一致**,不是**定義正確**。

---

## 10-1. Codex 第二輪明列的暫時禁令(Phase 1 期間一律不得做)

- ❌ 修改五維權重
- ❌ 修改技術、動能、籌碼公式
- ❌ 把五面改成百分位或 z-score
- ❌ **修正缺值填法**(即使已證實 `pe_vs_industry` 缺值拿 100 分是錯的,也不准現在修)
- ❌ 補 0050 歷史後重跑 V0
- ❌ 重跑 H1–H5 / 執行新的 OOS
- ❌ 把診斷結果當成新策略結論

> 第 4 條特別容易踩:§3 與 §6-C 已經把缺值問題查得很清楚,**但「查清楚」不等於「可以修」**。
> 修缺值填法會改變每一列的 f_fund → 等同新因子定義 → 依 §9 必須先走新預註冊。

---

## 11. 待決問題(給 Codex 第三輪)

1. ~~Phase 1 新面板的檔名會不會被當成正規面板撿走?~~ **已實測,答案是不會**:

   | 檔名 | `glob('realbody_scores_adv*w.parquet')` | `_is_canonical_name()` |
   |---|---|---|
   | `realbody_scores.parquet` | False | **True** |
   | `realbody_scores_adv100w.parquet` | **True** | **True** |
   | `realbody_scores_adv100w_v2diag.parquet` | False | False |
   | `realbody_scores_adv100w.EXPLORE.parquet` | False | False |

   glob 要求檔名以 `w.parquet` 結尾,`_v2diag.parquet` 不符 → `available_realbody_panels()`
   撿不到、`resolve_realbody()` 也解析不到。**`realbody_scores_adv100w_v2diag.parquet` 是安全的命名。**
   仍請 Codex 覆核:要不要乾脆讓 Phase 1 面板落在另一個目錄(例如
   `data/research_base/diag/`),把「不可能撿錯」變成結構保證而不是檔名巧合?
2. **補 0050 歷史要不要做**:`0050_raw.parquet` 就在 repo 裡(2003 起)。把它接上
   `load_benchmark` / RS 之後,2005–2018 的 regime 與 RS 會開始生效。這是**改策略**,
   要走新預註冊 —— 但它同時也是「讓五個時代用同一套定義」的唯一方法。
   **是先補資料再做 Phase 2,還是 Phase 2 就分兩段報?**
3. **問題 7(f_fund 估值腿與 f_val 同源)**:要不要在 Phase 2 加量「拿掉估值腿後的 f_fund」?
   基本面是目前唯一廣泛有效的面,動它的風險 vs 正交性的收益如何權衡?
4. **問題 9 的 C1 抵銷假說**:排在 Phase 2 之前(便宜、能省掉大量重設計)還是之後(乾淨、
   避免「看過 in-sample 再設計 arm」)?
