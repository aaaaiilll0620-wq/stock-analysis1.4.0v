# 📊 台股 PIT 多因子回測與個股分析系統 — 開發日誌 (Development Log)

> **專案定位**：以 Point-in-Time (PIT) 嚴格時間軸為地基的台股多因子「個股診斷 + 滾動回測」系統。
> 目標不是預測大盤漲跌，而是建立一套**可驗證、無未來函數、具鑑別力**的個股評級與策略框架。
>
> **文件版本**：v3.0 定稿　｜　**回測區間**：2023-01-31 ~ 2025-12-31（月頻換股）　｜　**基準**：0050
> **核心結論**：三大痛點（恐高症／逆勢偏誤／死抱20天）已解，選股排序力經**市場中性樣本外驗證**，
> 並找到**風險調整後贏過大盤**的可實作配置。

---

## 目錄
1. [系統核心模組架構](#1-️-系統核心模組架構-system-architecture)
2. [開發時間軸總覽](#2--開發時間軸總覽)
3. [v1.0.0 — 防守型價值選股與僵化評級](#3--v100--防守型價值選股與僵化評級-起步期)
4. [v1 實測診斷 — 反向指標的重大轉折](#4-️-v1-實測診斷--反向指標的重大轉折)
5. [v2.0.0 — 四級標籤與動能特赦重構](#5--v200--四級標籤與中性偏積極動能特赦-結構重構)
6. [v2.1 — 依 432 筆關卡診斷的資料驅動校準](#6--v21--依-432-筆關卡診斷的資料驅動校準)
7. [v3.0 — 驗證與生產配置定案](#7--v30--驗證與生產配置定案)
8. [關鍵指標演進總表](#8--關鍵指標演進總表)
9. [未來優化藍圖](#9--未來優化藍圖-next-steps)
10. [附錄：調參入口與檔案地圖](#10--附錄調參入口與檔案地圖)

---

## 1. 🛠️ 系統核心模組架構 (System Architecture)

| 層級 / 模組 | 職責 |
|---|---|
| **Data Layer** (`data_provider`) | 串接 FinMind API，進行 **PIT 嚴格時間軸對齊**：價格/PER/月營收只取 `date ≤ as_of`；季報依約 45 天公告時差過濾，回測日尚未公告的季報一律不用。杜絕未來函數 (Look-ahead bias)。 |
| **ScoringManager** (五維度評分大腦) | 將個股訊號量化為五大維度分數（見下）。 |
| **ValuationEngine** | 三重視角估值：PEG 成長調整 / 河流圖歷史百分位 / 絕對門檻；並交叉驗證「成長溢價 vs 昂貴泡泡」。 |
| **FundamentalEngine** | 硬門檻安全檢查 + 獲利品質一致性（增收是否增利）+ 現金流健康（OCF/FCF）。 |
| **InvestmentAdvisor** (決策導師) | 讀取五維度分數與各維度 Gate（閘門），套用動態權重與雙軌評級邏輯，輸出個股最終評級與可執行建議。 |
| **Backtester** (回測引擎) | PIT 滾動回測，支援固定持有 (Horizon) 與**路徑相依動態出場** (dynamic_stop)；產出分桶報酬、多空價差、權益曲線、市場中性檢驗、參數網格搜尋。 |

### 五維度評分 (ScoringManager)

| 維度 | 內容 |
|---|---|
| 🏢 **基本面 (Fundamental)** | 營收 / EPS / 三率、負債比、流動比、現金流健康。 |
| 💰 **估值面 (Valuation)** | PER / PBR 歷史百分位、PEG、殖利率。 |
| 📈 **技術面 (Technical)** | 均線多頭排列、週線趨勢、RSI 位階、MACD、布林、乖離率。 |
| 🐋 **籌碼面 (Whale)** | 千張大戶持股週變化、法人連續買超力道、近期淨流向、投信吸籌比。 |
| 🚀 **動能面 (Momentum)** | 漲跌幅 / 量能爆發 / 乖離健康度、月營收年增與加速度。 |

---

## 2. 📅 開發時間軸總覽

| 版本 | 主題 | 關鍵動作 | 結果 |
|---|---|---|---|
| **v1.0.0** | 防守型價值選股 | 3 級評級、嚴格估值/RSI/乖離閘門、固定持有 20 天 | 反向指標：多空價差 **−2.83%** |
| **v2.0.0** | 四級 + 動能特赦 | 新增【強勢買進】軌道、逆勢救援、dynamic_stop 預設、權重重配 | 多空價差轉正 **+0.44%**、贏家進買進級 |
| **v2.1** | 資料驅動校準 | 依 432 筆關卡診斷修正**致命排序 bug**、動能三選一、土洋同買點火、門檻下修 | 強勢買進可入選 **25→78 (3×)** |
| **v3.0** | 驗證與定案 | 市場中性樣本外驗證、排序配置實驗、生產配置選定 | 市場中性 test **+3.23%**、生產配置 **+109% / −9.8% 回檔** |

---

## 3. 🧱 v1.0.0 — 防守型價值選股與僵化評級 (起步期)

**設計思維**：個股分析極度注重安全性。`InvestmentAdvisor` 設立了極其嚴格的過濾閘門，
必須**同時**通過才給【強烈推薦】：

- `g_估值OK`：估值必須「偏低」或「合理」。
- `g_RSI不過熱`：`rsi < rsi_extreme`。
- `g_不追高`：`ma20_bias ≤ bias_chase`。
- 外加品質、分數門檻、籌碼不弱。

**評級桶（硬編碼 3 級）**：`["強烈推薦", "觀望追蹤", "謹慎避開"]`
**回測設定**：固定持有 20 天 (Horizon Exit)。

> **埋下的種子**：這三道閘門要求「便宜 + 不過熱 + 不追高」**同時成立**——
> 而任何一檔漲上去的主流飆股，本質上就是「貴 + RSI 高 + 乖離大」。
> 換言之，**恐高與逆勢被寫進了評級邏輯本身**。

---

## 4. ⚠️ v1 實測診斷 — 反向指標的重大轉折

### 📉 回測整體數據
- 策略總報酬 **+81.7%**（年化 +22.7%），期勝率 63%，但嚴重落後大盤 0050 的 **+119.9%**（超額報酬 **−38.2%**）。
- 【強烈推薦】分桶平均報酬為負 **−0.58%**，多空價差 **−2.83%** — 系統呈現嚴重的**「反向指標」偏誤**。

### 🔍 個股分析盲點診斷

**① 主流飆股的「恐高症」錯失**

| 個股 | 回測期平均報酬 | 被系統判定 | 問題 |
|---|---|---|---|
| **1519 華城** | **+8.76%** | 35 期中 **26 次【觀望追蹤】** | 漲高→估值爆表、RSI 過熱，飆速段完全沒吃到 |
| **2330 台積電** | +3.59% | 【強勢/強推】次數近乎 0 | 因「不夠便宜」長期卡【觀望】 |
| **3017 奇鋐 / 3324 雙鴻 / 3661 世芯-KY** | +2~6% | 多數【觀望】 | AI／權值核心飆股被估值門檻擋下 |

**② 弱勢反彈股的「反向打臉」**

| 個股 | 回測期平均報酬 | 被系統判定 | 問題 |
|---|---|---|---|
| **2408 南亞科** | **+4.67%** | **31 次【謹慎避開】** | 基本面差被瘋狂避開，卻因底部籌碼流入 + 技術低檔反彈大漲 |
| **2891 中信金** | +1.42% | **33 次【謹慎避開】** | 避開桶反而是正報酬 |

### 🩺 診斷結論
1. 個股分析過於死守「防守閘門」→ 牛市中投資組合全是**死魚股**。
2. 固定持有 20 天 → 強勢股利潤集體回吐。
3. 避開桶藏著贏家 → **多空價差為負**，評級與未來報酬呈反向關係。

---

## 5. 🚀 v2.0.0 — 四級標籤與中性偏積極動能特赦 (結構重構)

為一次解決「回測落後」與「個股分析失真」，進行大刀闊斧重構。

### A. 個股分析模組 (InvestmentAdvisor) 邏輯翻修

**1. 新增【強勢買進】判定軌道（雙軌制）**
- 保留【強烈推薦】給傳統「低估值、安全基期、基本面好」的長線價值股。
- 新增【強勢買進】(第 4 級) 專收「順勢動能飆股」：當動能強、主力法人狂吸、技術面多頭排列時，
  採用**【中性偏積極的條件式特赦】**，放寬估值百分位與 RSI/乖離限制，
  正式將台積電、華城等主流股納入最高評級。
- **泡沫與衰竭保護**：本益比歷史高檔且成長跟不上（昂貴泡泡）、或價漲量縮 + 主力同步出貨（衰竭）→ 不特赦。

**2. 修正【謹慎避開】的逆勢盲點（逆勢救援）**
- 若個股因基本面差被歸類為避開，但觸發「技術面翻多/突破壓力」且「籌碼面顯著流入」→
  強制提升為【觀望追蹤】，把南亞科型反彈股移出避開桶，消除反向指標。

**3. 動態權重 (Dynamic Weighting)**
- 當個股處於強勢多頭（多頭排列 + 正乖離 + RSI 偏多）時，
  自動把**估值權重砍 60%** 轉給動能與籌碼 → 分數由順勢動能主導，不再被「便宜與否」拖累主流股。

### B. 回測與統計結構重構
1. **硬編碼評級擴充為 4 級**：`{"謹慎避開":0, "觀望追蹤":1, "強烈推薦":2, "強勢買進":3}`。
   全面同步修改 `summarize()`、`equity_curve()`、`track_rating_changes()`、`_bucket_spread()` 的統計/繪圖/排序邏輯。
2. **多空價差修正**：公式改為 **(強勢買進平均報酬 − 謹慎避開平均報酬)**。
3. **全面匹配動態出場**：`run()` 預設 `exit_mode="dynamic_stop"` —— 收盤實質跌破量價成本區 (POC/Support)
   或觸發 σ 移動停利 (Chandelier) 立刻提早出場，**拒絕死抱 20 天**（合成回測平均持有降至約 11 天）。
4. **五維度權重重配（中性偏積極）**：技術/動能/籌碼合計拉高至約 70%，估值退居安全邊際確認。

### 📊 v2.0.0 實測
| 指標 | v1 | v2.0.0 |
|---|---|---|
| 多空價差 | −2.83% | **+0.44%**（轉正） |
| 強勢買進桶報酬 | 不存在 | **+2.21%（最高桶，67 樣本）** |
| 主流飆股歸類 | 全卡觀望 | 台積電 14 次強勢、聯發科 12、廣達 9、世芯 5 |
| 權益曲線 (集中買進級) | +81.7% / −13.6% | **+109.1% / −9.8% 回檔** |

> **殘留問題**：分桶報酬非單調——【謹慎避開 +1.77%】仍高於【觀望 +0.69%】。避開桶仍藏贏家。
> 但這是下一步 v3 驗證的關鍵切入點。

---

## 6. 🔬 v2.1 — 依 432 筆關卡診斷的資料驅動校準

啟用**每次都印的兩軌關卡未過率診斷**後，用 432 筆真實樣本定位瓶頸，逐一對症下藥。

### 發現的四個問題與修正

| # | 問題（實測瓶頸） | 修正 |
|---|---|---|
| **1（最關鍵）** | **致命排序 bug**：「估值偏高 + 綜合分<50」的軟避開被排在強勢買進判定**之前**。主流飆股綜合分常被估值拖到 50 以下 → 還沒輪到強勢買進就被錯殺成避開。 | 改判定順序為：**硬避開（基本面/現金流/破線）→ 強勢買進 → 估值型軟避開**。 |
| **2** | 動能夠強 69.9% 未過：`momentum_score` 是單日快照、太噪。 | 動能改**三選一**：價量動能分 **或** 技術結構分 (多頭排列·MACD·週線) **或** 營收動能。 |
| **3** | 籌碼點火 79.2% 未過：`whale_score` 計分天生偏低（需連續買超才拉得高）。 | 加**替代點火路徑**：有實質流入且（whale 達標 **或 土洋同步連買 或 法人成交占比≥40%**）。 |
| **4** | 多頭排列 61.1% 未過：`ma5≥ma20` 在健康回檔會短暫下彎，錯殺回測到月線的主流股。 | 多頭排列改以「**站上月線 + 站穩週線 + 中期未死叉**」認定，不綁噪動的 5 日線。 |

外加門檻下修：`whale_hot 45→42`、`mom_hot 50→46`、`min_score`（balanced 62→58）。

### 校準成效
| 強勢買進軌道 | 校準前 | 校準後 |
|---|---|---|
| 通過全部關卡 | 25 / 432 | **78 / 432（3×）** |
| 籌碼點火 未過率 | 79.2% | **26.9%** |
| 動能夠強 未過率 | 69.9% | **25.2%** |

---

## 7. ✅ v3.0 — 驗證與生產配置定案

### 7.1 決定性證據：市場中性樣本外驗證

用 `--validate` 把「絕對報酬」與「市場中性（剃除大盤 beta）」兩條路攤開對比：

| 檢驗 | train | test | 判定 |
|---|---|---|---|
| 絕對報酬（只買強推） | −0.58% | +1.21% | ❌ 未同時為正 |
| 絕對報酬（強推+觀望） | +0.24% | −1.57% | ❌ 未同時為正 |
| **市場中性（同日 前⅓ − 後⅓）** | **+0.96%** | **+3.23%** | ✅ **通過（且樣本外放大）** |

> **關鍵洞察**：系統**會「排序」（哪些強、哪些弱），但不會「擇時」（大盤何時漲跌）**。
> 當初的「反向指標」本質是**把「不會擇時」誤當成「選股選反了」**——
> 剃掉大盤 beta 後，高分股確實贏低分股（test **+3.23%**），且 out-of-sample 站得住。
> **重構在數據上被驗證成功。**

市場中性對沖曲線與大盤相關性 **+0.07**（近乎零）→ 確認是純粹的排序 α，不靠大盤。

### 7.2 配置實驗：集中 vs 分散（反直覺結論）

既然排序力已驗證，實測各種「怎麼用它」的配置（同區間、同基準）：

| 配置 | 總報酬 | 最大回檔 | Calmar | 判讀 |
|---|---|---|---|---|
| **買 強勢+強推（分散，等權）** | **+109.1%** | **−9.8%** | **2.66** | ✅ **最佳** |
| 純做多前⅓ (market-neutral 系列) | +113.2% | −24.8% | 1.19 | 報酬相近、回檔大 |
| 排序 top5（分數加權） | +96.8% | −23.6% | 1.11 | 集中反而變差 |
| 排序 top5（等權） | +85.0% | −19.3% | 1.22 | |
| 排序 top3（分數加權） | +61.5% | −23.6% | 0.76 | 越集中越差 |
| 多空對沖（前⅓−後⅓） | +60.0% | −24.3% | 0.72 | 市場中性、corr 0.07 |
| 大盤 0050 | +119.9% | −16.2% | 1.91 | benchmark |

> **反直覺結論：集中化（排序 top-N）全面變差**——報酬更低、回檔更大，越集中越糟。
>
> **系統真正的超能力不是「排序」，是「沒好貨時自動空手」**：排序法每期強迫買前 N 名，
> 大盤修正時仍滿倉吃回檔；而分散買買進級的版本，當沒幾檔通過「多頭排列＋籌碼＋基本面」時
> 自然少買/空手 → 修正時自動降曝險 → 回檔壓到大盤的一半（−9.8% vs −16.2%）。

### 7.3 ✅ 生產配置定案

```bash
python tests\run_backtest.py           # 買 強勢買進+強烈推薦、等權、dynamic_stop 出場
```

- **總報酬 +109.1%、最大回檔 −9.8%、Calmar 2.66（風險調整後勝 0050 的 1.91）。**
- `--ranked` / `--neutral` 保留為研究工具，不作主策略。

---

## 8. 📈 關鍵指標演進總表

| 指標 | v1.0.0 (起步) | v2.0.0 (四級) | v3.0 (定案) |
|---|---|---|---|
| 評級級數 | 3 級 | 4 級 | 4 級（雙軌） |
| 出場方式 | 固定 20 天 | dynamic_stop | dynamic_stop |
| 多空價差 | **−2.83%** | +0.44% | 市場中性 test **+3.23%** ✅ |
| 主流飆股 | 全卡觀望 | 進買進級 | 進買進級 |
| 策略總報酬 | +81.7% | +109.1% | **+109.1%** |
| 最大回檔 | −13.6% | −9.8% | **−9.8%** |
| 對 0050 (風險調整) | 落後 | 追平/略勝 | **Calmar 2.66 > 1.91 ✅** |
| 樣本外選股力 | 反向 | — | **驗證通過 (+3.23%)** |

---

## 9. 🔮 未來優化藍圖 (Next Steps)

1. **資料層完整度稽核（最高優先）**：綜合分中位僅 ~41、分數門檻 86% 未過，代表 composite 系統性偏低。
   需回 `data_provider` / `build_pit_stockdata` 檢查**法人買賣天數、週線 MA、月營收**等訊號在 PIT 重建時是否有缺漏——
   這比再調 advisor 門檻更根本，是下一個真正能擠出 α 的地方。
2. **轉折點防禦驗證**：觀察 4 級架構在大盤高檔拉回時，特赦機制與「沒訊號就空手」特性的防禦表現。
3. **多市場週期穩健性**：把回測區間延伸到 2021–2022 空頭段，確認排序力不是只在 2023–2025 多頭有效（避免過度配適）。
4. **交易成本與流動性**：加入手續費/稅/滑價與最小成交量過濾，讓回測更貼近可交易性。
5. **分項因子歸因**：拆解五維度各自對市場中性價差的邊際貢獻，砍掉無效因子、強化有效因子。

---

## 10. 🧰 附錄：調參入口與檔案地圖

### 主要調參入口
| 檔案 | 參數 | 作用 |
|---|---|---|
| `core/advisor.py` | `mom_hot / whale_hot / rev_hot / tech_hot` | 強勢買進的動能/籌碼/營收/技術門檻 |
| `core/advisor.py` | `trend_bias_min / lead_rsi_min` | 動態權重觸發條件 |
| `core/advisor.py` | `_dynamic_weights` 的 `cut = v*0.6` | 估值權重讓渡比例 |
| `core/scoring_manager.py` | `MODES` 的 `composite_weights / min_score` | 五維度綜合權重與分數門檻 |

### 回測指令速查
| 指令 | 用途 |
|---|---|
| `python tests\run_backtest.py` | **生產配置**：分桶報酬 + 權益曲線（買買進級） |
| `python tests\run_backtest.py --validate` | 絕對報酬 vs 市場中性樣本外驗證 |
| `python tests\run_backtest.py --neutral` | 市場中性權益曲線（買前⅓放空後⅓） |
| `python tests\run_backtest.py --ranked --top-n 5` | 排序配置研究工具 |
| `python tests\run_backtest.py --optimize` | 參數網格搜尋 + train/test 驗證 |
| `python tests\run_backtest.py --track` | 評級變動追蹤 |

### 核心檔案
`core/advisor.py`（評級邏輯）、`core/scoring_manager.py`（五維度權重）、
`core/backtest.py`（回測引擎/統計）、`tests/run_backtest.py`（執行器）、
`core/fundamentals.py`、`core/valuation.py`、`dynamic_stop_engine.py`（動態出場核心）。

---

## 11. 🔬 2026-07-30 — 回測基礎設施稽核（Codex 靜態盤點 → 五輪對抗式修正）

> **定位**：這一節記錄的是**量測管線的稽核與加固**，不是任何假設（H1–H5）的判定。
> 起因是審查官（Codex）對 `beat_0050/` 研究層做全面靜態盤點，提出 7 項「可能讓回測結論無效」
> 的風險，經五輪對抗式往返逐一封死。**目的：確保 §7 之後填進去的數字站得住。**

### 稽核結論一句話
發現並修掉一個**真的在動數字**的量測偏誤（尾月時鐘不對齊，四個受測策略各虛高
**0.35–0.50pp CAGR / 0.02 夏普**），以及六項「目前沒出事、但遲早會靜默出事」的紀律漏洞。
**沒有任何研究結論被翻盤**（`dual100 --part h1` 前置閘修正後仍過：真身雙確認 @100萬 夏普 0.89 > 0050 的 0.68）。

### 逐項處置（7 點原始盤點 → 五輪收斂）
| 風險 | 實測量級 | 處置 |
|---|---|---|
| 0050 基準時位（開盤 vs 收盤錨） | **僅 0.1pp/年**（逐月差平均 +0.010pp, t≈0） | 量化澄清；`0050_tr` 有 `adj_open` 就自動改用；兩路徑一致 |
| 尾月共同時鐘不對齊 | **0.35–0.50pp CAGR / 0.02 夏普** | `Panel._truncate_to_bench` / `Engine._align_clock` / `met_vs` 硬性同時鐘 |
| 靜默砍母體 | 兩份面板實測 **0 缺、100.0000%** | 建置對帳報告 + 覆蓋率閘門（預設 1.0）+ `no_data`/`score_error` 分類 |
| Merge 複製列 / 計數膨脹 | 四份面板重複鍵 **全 0** | `assert_unique` / `assert_no_row_growth` 上鎖六個 merge 點；輸入母體 `assert_obs_unique` |
| try/except 繞過缺檔硬規則 | — | `REAL_COMP` 改 property 延遲 raise；`score_row(strict=True)` 分流 |
| Import 副作用（FinMind 登入） | — | `lab_paths` 去除匯入副作用；建置器 lazy import（網路封鎖下可匯入） |
| 研究生命線測試缺口 | — | 新增 `tests/test_research_lifelines.py`，**25 項** |

### 正規面板隔離規則（防污染）
- 只有**完美建置**（cov=1.0 且 0 個 score_error）能佔正規檔名。
- 任何瑕疵 → `.EXPLORE`（過放寬門檻）或 `.INCOMPLETE`（未過/有 bug），`resolve_realbody()` 撿不到。
- `--year/--limit` 局部跑強制 `_partial`；正規 parquet 原子寫入（`.tmp → os.replace`）。
- 覆蓋率門檻降低（`DUAL100_COV_MIN` / `--min-coverage`）一律印進 run log（provenance），<1.0 印探索橫幅。

### 驗證紀錄（保留）
- **研究 lifeline 測試：25 passed / 0 failed。**
- 完整 `tests/`：**132 passed / 10 failed**。
- 那 10 個失敗（`test_scoring_manager` 的 `test_bias_bands` / `test_full_momentum_breakout`、
  `test_fundamentals`）是 **`core` 計分口徑與測試期望漂移的既有 baseline**，
  經 `git stash` 全部改動後複跑仍為同樣 10 failed / 51 passed，**與本次 diff 無交集**。
- 兩條獨立路徑（矩陣 `high52_lab.Panel` vs 持股字典 `honest_backtest.Engine`）逐月對帳 **0.0000pp**。

### 動到的檔案
`scripts/lab_paths.py`、`beat_0050/strategies/high52_lab.py`、`beat_0050/strategies/dual100_lab.py`、
`beat_0050/honest_backtest.py`、`beat_0050/realbody/build_realbody_scores.py`、`core/score_store.py`
（`strict` 分流）、`scripts/{alpha_gate,portfolio_simulator,basket_dispersion}_lab.py`（`ensure_base()`）、
`tests/test_research_lifelines.py`（新）、`tests/conftest.py`。

### 範圍外（已列，未動）
- `core/data_provider.py` 的 class-body `DataLoader()`（全 App 既有登入副作用，牽涉 app/main/portfolio）。
- `scripts/tej_funnel_test.py` 收集階段中止（硬編碼外部路徑）。
- `core` 計分口徑漂移導致的 10 個 baseline 失敗（`real_composite` 正是這套引擎算的，值得另開一輪查）。

---

## 12. 🎯 2026-07-30 — 雙確認@ADV≥100萬 預註冊驗證：H1–H5 全過(觸發 §4 出口 1)

預註冊:`docs/預註冊_雙確認ADV100萬.md`(面板落地前凍結)。門檻取自該文件 §4。
執行版本:稽核後的碼(§11 / commit `9d80dd3`),runner `beat_0050/strategies/dual100_lab.py`。
面板:`realbody_scores_adv100w.parquet`(01:45 建置、02:53 落地、cov=1.0 乾淨檔名)。

### H1 前置閘 — ✅ 通過(見 §7.0,稽核後複核)
全期夏普 **0.89** > 0050 的 0.68;①選股階 **+9.30pp/年**;兩路徑逐月對帳 **0.0000pp**。

### H2 walk-forward(主假設,high52 死在這關)— ✅ 通過;H2b 亦通過
`--part h2`,2026-07-30。協定:訓練窗 ≥60 月、每 12 月重估、只用訓練窗夏普從
{2000萬,500萬,100萬} 挑一層。三層真身覆蓋率均 **100.0%**,全數納入(搜尋空間=3)。

| 段(2010-01~2026-03,195 月同時鐘) | CAGR% | 夏普 | MDD% |
|---|---|---|---|
| 協定 walk-forward(H2) | **22.79** | **1.20** | −28.5 |
| 固定 100萬 層(H2b) | 22.79 | 1.20 | −28.5 |
| 0050(同段) | 16.52 | 0.86 | −29.4 |

門檻「OOS 夏普**且** CAGR 皆勝同段 0050」:1.20>0.86 且 22.79>16.52 → **H2 通過**;H2b 同。

**誠實揭露**:走查 **17/17 個重選窗全部挑到 100萬**(訓練夏普 0.42→0.86),故 H2 與 H2b
淨值序列完全相同 —— 通過的是「即時可複製地挑到 100萬」(high52 正是死在此),**不是**
「動態選池優於固定池」。

**nan 已排除為顯示層瑕疵(Codex 覆核點)**:`dual100_lab.py:125` 的 `show()` 換手欄是獨立
位置參數 `turn`(預設 `np.nan`),run_h2 的策略腿(:259/:261)未傳該值故印 nan;CAGR/夏普
由 `met_vs()` 自淨值序列獨立算出,判定式(:264-267)只讀 `sharpe`/`cagr`。**不影響 H2 判定**。
換手計算函式 `turnover()` 本身正常(H1 於 :169 已正確使用)。→ H4 前無需重跑或改策略邏輯。

### H3 三組虛無 + DSR — ✅ 全過
`--part h3 --reps 500`,2026-07-30(42s)。觀測本體全期 CAGR 20.80% / 夏普 0.89。
- **H3-1** 對齊 footprint 隨機:策略 0.89 vs 隨機 p95 0.30 → p=**0.000**(門檻<0.01)✅
- **H3-2** 報酬逐月置換:置換夏普 p99 0.33 → p=**0.0000**(門檻<0.01)✅
- **H3-3** 配置置換 + DSR:觀測 0.888 > 置換 max p95 0.357 ✅;解析 DSR(N=3,Var(SR)=0.0264,
  偏態 −0.27、峰態 4.17)DSR=0.9998 → p=**0.0002**(門檻<0.05)✅。
  ⚠ §3 已揭露:雙確認規則本身是先前工作產物,N=3 **低估**真實多重比較 —— 只主張「宣告空間內未見選擇性偏誤」。

### H4 滑價敏感度 — ✅ 通過
`--part h4`,2026-07-30。來回滑價 0.60% → 夏普 **0.73** > 0.68 ✅;損益兩平 ≈0.71%(替身時代 0.70%);
0.80% 才跌破(0.64)。⚠ **全期 MDD 在 0.25–0.60% 滑價下為 −68.7% ~ −70.4%** —— 這是**無 regime
overlay、固定曝險的策略本體**真實尾端風險。零股撮合溢折價無法回測(§5-3),本項是代理不是證明。

### H5 六時代穩健 — ✅ 通過
`--part h5`,2026-07-30。勝等權母體 **6/6**(門檻≥4);夏普勝 0050 **4/6**(門檻≥3;輸的是
2019-21、2023-26 兩段強多頭,0050 集中大型股更猛)。①選股階六時代全正(+1.08 ~ +15.29pp)。

### 判定狀態與 §4 出口 1 處置
**H1/H2/H2b/H3/H4/H5 全部通過** —— 本專案史上第一個走完完整預註冊的策略(high52 死在 H1,
本案六關全過)。觸發 §4 出口 1:雙確認 @ADV≥100萬(**策略本體**)進**實盤候選**,走
`操作手冊_實盤執行SOP.md` → **先小額活體演練**(零股撮合風險無法回測,不直接全額部署)。
完整判定表與三項誠實保留見預註冊 §7.1。

> **Codex 架構關切(`GPT answer.md`)已受理,另立文件處置。** market regime 在 `core/advisor.py`
> (`:104-113` 改五維權重、`:507-509` 改 bear 段評級門檻 → 介入**選股**)與
> `core/regime_exposure.py`(0→1 曝險階梯 + 3 日遲滯 → **風控 overlay**)兩處都介入。
> 已查證:`build_realbody_scores.py` 與 `core/scoring_manager.py` **均無 regime 參照**,
> `real_composite` 是在 `current_regime=None` 下建的 → **本案 dual100 確為無 regime 的策略本體**,
> H2 的 22.79%/1.20 是本體 OOS 成績,H4 的 −70% MDD 是本體真實尾端。低 MDD 的漂亮數字來自
> overlay,**不屬本案已驗範圍**。策略本體 / overlay / 三階基準的分離與待預註冊清單見
> `docs/架構分離_StrategyBodyVsOverlay.md`。

---

## 13. 🔒 2026-07-31 — P-Overlay-C 預註冊凍結(Codex 最終審查通過)

`docs/預註冊_ExposureOverlay.md` 於 **2026-07-31 凍結**,**§7 結果欄空白**(A@100萬 × overlay-only
的 OOS 數字尚未計算)。**凍結後方可實作並執行 overlay runner。**

**凍結內容**:A(dual100 月頻 ~48 檔,H1–H5 已驗)× C(`regime_exposure` MA50/100/200 + 遲滯3/3)。
**部署模擬主測 = 每日限速路徑**(`cap=0.20/日`,MDD 在**日 NAV** 上量);月頻未限速為比較組。零自由參數。

| 假設 | 門檻 |
|---|---|
| **HO1**(主) | OOS 日 NAV `|MDD| ≤ 40%` **且** 扣成本 OOS CAGR ≥ 20% |
| HO2 | vs 同均曝常數:`dd_flat − dd_AC ≥ 5pp` 且 `CAGR_AC ≥ CAGR_flat − 1pp`(確定性) |
| HO3 | **相位對齊虛無**:固定 `e_lim`/成本、只旋轉 `r_A(d)`;`p<0.05`(B=1000, seed=20260730) |
| **HO4a**(硬) | 滑價 0.60% 下 HO1 兩條仍成立 → 失敗即**不可部署** |
| HO4b(軟) | 六時代 ≥5/6 段 `|MDD|≤40%` 且 2008/2022 均達標 → 風險旗標 |

**凍結前完成的實證(細節見該檔 §8)**:
- **§8-1a 純度**:發現**部分污染** —— `dual_riskoverlay.py` 的「雙確認 + bear→現金30%」(夏普 0.80 /
  MDD −46.6%)已被觀測,但屬**不同 overlay**;本案確切組態未算過 → 定位為**部分預註冊**。
- **§8-1b 對帳**:日 `r_A(d)` 加性分解 vs `fwd_x`,單股 296,050 檔月最大 **5.7e-13pp**;**完整組合層**
  (含選股換手成本、股利、權重時點)vs `r_A(t)` 最大 **1.67e-06pp**;正式 OOS 194 月 **9.4e-07pp**。
- **§3-A-D-⏱ 時間對齊**:close(t) 算訊號 → open(t+1) 執行 → `e_lim(d)←sig(d−1)` 加權 open(d)→open(d+1);
  訊號變更日範例證**無 look-ahead**。
- **§3-A-D-💰 股利模型**:均勻每日加性 `δ=dy12/n`(**非**除息日現金流,資料未提供)。
  **加性月度對帳 ≠ 日 NAV 複利**(舊稿誤稱「恆等」,已更正):日 NAV 複利 vs `r_A(t)` 最大月差 **0.055pp**;
  **MDD 對股利分配方式敏感度 ≤0.10pp**(均勻 −33.72% / 首日 −33.61% / 末日 −33.66%),距 −40% 餘裕
  6.3pp 僅佔 1.6% → 列**揭露項**。對照:不計股利 −35.64%、官方月頻 −28.50%(日vs月觀測頻率差 5.2pp)。
- **§8-0 策略身分**:查出 SOP §B + `PortfolioSimulatorLab` 的**季度/K=8** 與 dual100(**月頻/~48**)
  是**兩個不同策略**。使用者裁定**採 dual100 月頻 ~48 檔**;季度8 保留為另一未驗證策略。SOP 已加頂部
  校準 banner + §0/§4 inline 標記 + §6 待辦列(**僅標記**;操作步驟改寫須另案預註冊,依 SOP §6 後設規則)。

> ⚠ **本案凍結 = overlay 研究預註冊,不等於可上線。** 正式月頻 dual100 的 A/B 實盤執行 SOP
> **仍須另案預註冊**;即使 HO 全過,§4 出口 1 仍要求先小額活體演練(零股撮合風險無法回測)。

---

## 14. ❌ 2026-07-31 — P-Overlay-C 執行結果:**部署硬門檻未過**(否定結果照登)

`python beat_0050/strategies/overlay_lab.py --part all`(exit 0,11s)。
stdout 原樣保存於 `beat_0050/results/overlay_all_2026-07-31.log`;runner sha256 `ef172d73…7726b1`
(Codex 審查通過後未再改動)。26 項凍結常數自檢通過;preflight(P1/P2/P3)全過才進入判定。

| 假設 | 觀測 | 判定 |
|---|---|---|
| **HO1**(主) | `\|MDD\| 20.56%` ✅ / **CAGR 15.61% < 20%** ❌ | **❌ 否定** |
| HO2 | gap **+3.11pp < 5pp** ❌ / ΔCAGR +0.13pp ✅ | **❌ 否定** |
| HO3 相位對齊虛無 | **p = 0.0160 < 0.05** | ✅ 通過 |
| **HO4a**(硬) | 滑價0.60%:CAGR **13.20%** ❌ | **❌ 否定** |
| HO4b 六時代 | **6/6** ≤40%,壓力段皆達標 | ✅ 通過 |

> ⛔ **部署硬門檻(HO1 AND HO4a)未過 → 依 §4 出口 2 + 5:此 overlay 與此本體不相容,不可正式部署。**

**核心數字(OOS 2010-02~2026-03,3,959 交易日;ē=0.6736)**:

| 序列 | CAGR% | 夏普 | MDD% |
|---|---|---|---|
| A×C(日限速,主測) | **15.61** | 1.13 | **−20.56** |
| 本體 A(無 overlay) | 22.48 | 1.11 | −33.73 |
| A×ē(同均曝常數) | 15.49 | 1.11 | −23.67 |
| A×C @滑價0.60% | 13.20 | 0.94 | −21.77 |
| 比較組:月頻未限速 | 17.46 | 1.29 | −13.40 |

**結論**:overlay **確實壓低回撤**(−33.73% → −20.56%,+13.2pp),**−40% 的 MDD 門檻輕鬆達標**;
但**報酬掉了 6.87pp(22.48% → 15.61%),跌破 20% 的 CAGR 硬線** —— 卡住的是報酬不是回撤。

**HO2 否定 vs HO3 通過不矛盾**:HO3(p=0.016)證明「相位對齊不是運氣」;HO2 卻顯示相對
**同均曝常數線**只多換到 3.11pp 回撤改善 → **MDD 改善的絕大部分來自「平均只持有 67.4%」而非擇時**。
依 §4 出口 3(凍結規則),HO2 失敗即**不得宣稱擇時 alpha**,本案僅記為「有效的減碼」。

**方法論驗證**:月頻未限速比較組(17.46 / −13.40)在報酬與回撤上都比日限速主測好看 ——
印證 §3-B「月頻低估部署 MDD(少報 7.2pp)且高估報酬」;**採日路徑為主測是對的**
(若以月頻為主測,CAGR 仍未達 20%,結論不變)。

**揭露**:(1) 本案是**部分預註冊**(§8-1a:不同 overlay 的 0.80/−46.6% 已被觀測);
(2) 日 NAV 複利 vs 月度加性最大月差 0.05618pp(非門檻);
(3) 252 日年化**高估** CAGR 約 +0.4pp,**偏誤對本案有利而仍失敗 → 結論穩健**;
(4) D1 落點/算術 pre-run 凍結(`entry_day`+`additive_return`),本體 A −33.73% 與凍結公布的
−33.72% 差 0.01pp,即附錄 D1 §4 預告的二階差,未回改凍結數字。

⚠ **本案未通過,不得作為實盤依據**;月頻 dual100 的 A/B 實盤 SOP 亦仍未完成另案預註冊。
若要主張「更輕的 overlay 可同時守住 MDD 與 CAGR」,**須另行預註冊**,不得在本案調參重跑。

---

## 15. 🔍 2026-07-31 — 五維度血緣稽核第一輪(靜態,不動權重)

**觸發**:`GPT answer.md` 重置為「先處理五維度評分系統的基礎問題,再重新驗證策略」的指令。
本輪只交付 §9 的七項,**不改權重、不跑 OOS、不動任何預註冊門檻**。
完整交付物:**`docs/血緣稽核_五維度_2026-07-31.md`**;重現指令 `python scripts/face_lineage_audit.py --adv 1e6`。

**A. 基礎缺陷修復**:`core/data_provider.py` class-body `_api = DataLoader()` 改為延遲建立
(`_get_api()`)。實測確認連網發生在 **`DataLoader()` 建構子**而非 SDK import ——
原 ISSUE 文件對此存疑,現定案。`import core.data_provider` 4.27s→**0.48s 且不再登入**。
新增 `tests/test_import_hygiene.py`(socket 封死後 import,連網即炸)。
測試:研究 lifeline 25 passed;完整 tests **164 passed / 0 failed**(修前 159/0;+5 為新測試)。
**計分碼一行未動 → `_weights_version` 不變 → realbody 面板未過期**;
`dual100 --part verify` 覆核與 `預註冊_雙確認ADV100萬` §7.0 逐項相同。

**B. 「10 個 core 測試失敗」的正確敘述**(2026-07-31 依 Codex 審查 §二 修正措辭):
那 10 個失敗是**舊測試與現行 v4.4 計分口徑不一致**;處置是**更新測試**(`d8bb0f4`,**先於本輪**,
`core/` 零改動)使完整測試恢復通過。**這不是本輪的 scoring code 修正,本輪沒有動任何計分邏輯**;
本輪新增的 import hygiene 測試**另外**增加 5 個通過案例。`GPT answer.md` 的「目前 10 個失敗」敘述已過期。

**C. 五個必須記住的稽核發現**(細節見交付文件):

1. **三個定義斷裂點全部落在 2019** —— `f_val` 的產業內位階參考表起 2019-04-10;`f_mom` 的 RS 疊加
   要 0050 快取(只有 2019-01-02 起,實測 2018 年查詢回 None);`regime_multipliers` 同樣依賴 0050,
   **2005–2018 的 168 個月 `classify_regime()` 一律回 `neutral`,regime 層從未生效**。
   → H5「六時代穩健」的判定未翻盤,但**語意**須修正:前三段測的不是同一個定義。
2. **五面未標準化就直接加權** —— 有效權重(w×月內std 佔比)被壓成 0.12–0.24 一團,
   名目 0.31:0.08 的 4 倍差在有效層只剩 2 倍;與綜合分 rank corr 最高的是技術 0.609 / 動能 0.635。
   **問題不在權重數字,在加權前沒有統一量尺。**
3. **缺值語意在同一個引擎裡有三種相反處置** —— 缺 PE→`pe_vs_industry=10.0`→**100 分**;
   無資產負債表→`debt_to_asset=0.0`→**100 分**;無損益表→`roe/net_margin/gross_margin=0.0`→**0 分**。
   實測三大財報全缺時 `f_fund = 32.5`、`is_passed = True` —— 兩個相反的錯**互相抵銷成看起來正常的中低分**,
   在分布上完全不露馬腳。
4. **`real_composite` 確含 regime + per-stock 動態權重 + 兩個 bucket 覆寫**(估值資料不足用 50、
   洗盤把籌碼拉到 ≥65),但 `build_realbody_scores` **沒有把 `dyn_weight`/`regime` 存進面板**
   (`score_store.COLUMNS` 有這兩欄)。ADV≥100萬 殘差:僅 **59.7%** 的列 `|real_composite − Σw·face| ≤ 0.01`,
   3.9% 的列差 >5 分,極值 −23.0 ~ +17.9。
5. **c2 的 `100−momentum%` 與 f_tech 的 rank corr = −0.616** —— c2 在系統性反選技術面高分股,
   而 f_tech 正是 IC 顯著為負的那一面。**「雙確認有效是因為 c2 抵銷了 f_tech 的負貢獻」**
   成為一個可檢驗的廉價假說(交付文件 §6-4 arm C1)。
   另:`revenue_yoy%` ↔ f_mom 的營收動能腿 ρ=+0.322 是**真的重複計**;
   c2 的 momentum 腿與 f_mom **不是重複,是反向**(ρ=−0.377)。

**D. 額外發現**:`bt_bundle.py` 對 live 資料層做了兩個**研究專用覆寫**(估值百分位窗 2019→2004、
籌碼源改淨額),只寫在程式註解裡、未進任何預註冊。→ 「真身」= 生產計分碼 × **研究改過的資料輸入**。

**E. Codex 第一輪審查回覆(2026-07-31)**:匯入副作用修復**通過初步驗收**(獨立複跑 25 / 164 passed,
確認五維權重未改、無新 OOS、未重建面板)。兩項後續:
  · §二 措辭修正 —— 已照辦(見上 B 段)。
  · §三-1 `_ensure_login()` 例外語意 —— 追查後發現一條**三層連續吞錯**的鏈:登入失敗吞 →
    `TaiwanStockInfo` 抓取失敗吞 → **空表照樣寫進 30 天快取** → `build_pit_stockdata` 產業分流吞,
    結果 `is_financial=False` 對全市場成立、`FundamentalEngine` 三個金融股豁免全失效,
    **`f_fund` 被靜默壓低且面板無跡可循**。實測 `data/industry_map.json` 3,115 檔 / age 28.0 天 ——
    本次面板未受影響,但**距 30 天到期只剩 2 天**,重建面板(P1)正好落在風險窗。細節與建議見
    交付文件 §1-4。**本輪未改任何行為**,等 Codex §三 的完整指示(原文在此處被截斷)。

**狀態**:交付完畢,**停在此處等 Codex 審查**(問題清單 11 條見交付文件 §7)。
不進入權重修改或 OOS 執行。

---

## 16. 🔬 2026-07-31 — Codex 第一輪審查回覆的執行(獨立覆核,仍不改策略)

**輸入**:Codex 第一輪審查 —— 匯入副作用修復**通過初步驗收**;§十 給了七項停止條件。
**產出**:`docs/五維度修正工作計畫_2026-07-31.md`(§七 指定的正式工作文件)。
本輪**沒有**改 MODES 權重 / REGIME_MULTIPLIERS / _dynamic_weights / 任一面的計分公式 / c2,
**沒有**重建 canonical 面板,**沒有**跑任何 OOS 或 H1–H5。

**A. `_ensure_login()` 三種錯誤語意定案 + strict 模式**(§三-1)

| 錯誤 | `strict=False`(線上 App 容錯) | `strict=True`(研究建置) |
|---|---|---|
| `_get_api()` 建構失敗 | **raise** | **raise** |
| 缺 token / 登入失敗 | 降級匿名 | **raise** |
| 快取代理安裝失敗 | 降級直連 | **raise** |

`_get_api()` 移出 try(舊版會把「SDK 裝不起來」誤報成「登入失敗」再吞成匿名)。
匿名降級**明文標註為線上 App 的容錯,不是研究建置模式**。
研究入口 fail-closed:`build_realbody_scores._init_worker()` 顯式呼叫
`DataProvider._ensure_industry_map(strict=True)`;新增 `_check_industry_map()` ——
**空表或「有 3000 檔但金融股 0 檔」一律 raise**(後者筆數看起來正常但三個金融股豁免已全失效)。
另修:**產業表為空一律不寫入 30 天快取**(舊行為會讓一次網路失敗鎖死 30 天)。

**B. app.py `_saved_api` 還原行為**(§三-2):確認 lazy 化後 `_saved_api` 可能是 `None`,
還原後由 `_get_api()` 乾淨重建、不會指回前一位訪客的 loader。**token 隔離架構未動**,只補 2 個 regression test。

**C. 兩個獨立 issue 已開單、未修**(§四):
`docs/ISSUE_fetch_history_繞過快取代理.md`(量測結論:**對研究結論零影響**,研究走 bt_bundle → TEJ;
影響僅止於 live 的 API 次數與不落地)、`docs/ISSUE_core_config_模組級DataLoader.md`
(**更正**先前「無 importer」的說法 —— 漏掉相對匯入,實際有 `core/market_sentiment.py:4`,
而該模組自己無 importer → 兩者是孤立死鏈。**不刪除**)。

**D. 三項獨立覆核結果**(`scripts/breakpoint_2019_audit.py` / `scripts/composite_reconcile.py`):

1. **2019 斷裂點 —— 資料缺口是唯一原因,已證。** 同一個 `classify_regime()`、同一組 257 個 as_of,
   只換基準序列:生產走的 FinMind 0050 快取(1,827 列 / 2019-01-02 起)→ 2019 前 **168 個月全 neutral**;
   repo 內 `0050_raw.parquet`(5,677 列 / **2003-06-30** 起)→ 2019 前 **bull 94 / bear 47 / neutral 27**。
   兩者一致率 2019 起 87.6%、2019 前 **16.1%**。
   ⚠ **2026-07-31 修正(見 §21)**:87.6% 是被污染的數字 —— 稽核腳本沒對 `0050_raw.parquet`
   套 `_back_adjust()`,2025 年 0050 分割成了人造 −75% 崩盤。兩邊都回補後是 **95.4%**,
   4 個不一致月份全部落在 2019-01/02/06/07。另,「兩條路徑讀不同的 0050」的說法**是錯的**:
   兩份序列重疊 1,827 列 `close` **逐列相同**,RS 與 regime 讀的是**同一個**資料集。
   **「2019 前全 neutral 唯一因為資料缺口」這個結論不變。**
   f_val 路徑①覆蓋率:2019-03 前逐年 **0.00%**,2020 起 94–96%。
   RS:`rs_3m` 首見 2019-04-30、`rs_6m` 首見 2019-07-31,**257 個月裡 174 個月不計分**。
   落在 H5 是乾淨的 **3:3** 分割。→ **H5 判定不改寫**,但報告須加限制句:
   **「跨時代期間並非完全使用同一套資料可得性與評分定義」**。
2. **real_composite 合成算術重算(抽樣)—— 三層 100%**:獨立算術 vs 生產 advisor、
   生產重跑 vs 面板、獨立算術 vs 面板,`|Δ|≤0.01` 皆 **100.00%**、max 0.0000。
   ⚠ **兩個措辭限制(Codex 第二輪指定)**:(a) 這**不是全 panel 對帳** —— 只查了
   **60 檔等距抽樣、7,369 stock-months**,原始面板 263,928 列(抽樣率 2.79%),
   不得寫成「整份面板已 100% 對帳」;(b) 它是**獨立的合成算術重算**,
   `_bull_stack` / `_detect_washout` 仍共用 advisor,**不得**宣稱已滿足研究紀律 §3 的
   「第二條獨立實作逐月對帳」。**在抽樣範圍內**面板沒有過期。
   殘差歸因:regime 33.4% 的列 / dyn weights 9.0% / valuation 覆寫 3.5% / washout **0.0%**,
   四項合計 40.17%,**未解釋殘差 max 0.0050(= round 量化誤差)→ 沒有第五個來源**。
3. **缺值比例**:PE 缺失 5.02%(**2019 後升到 7–11%,現在進行式**,每次讓基本面估值腿拿 100 分);
   三大財報全缺 1.67%,**完全集中在 2005(該年 57.21%)**;valuation 覆寫 3.45%;washout 0%。
   缺財報列的 f_fund 中位 **33.5** vs 其餘 **45.8** —— 被系統性壓低約 12 分並擠在窄帶,
   但整段落在正常分布的下四分位內,**看分布抓不到、看單列不覺得異常**。

**E. 兩個新發現**:
- **`washout` 在研究路徑是死碼** —— `_detect_washout()` 需要 `margin_change_pct <= -8`,
  而 `build_pit_stockdata()` **從未設定該欄**(只在 live 的 `_fetch_full_stock_data:1827` 填),
  PIT 路徑恆為預設 0.0 → 7,369 列觸發 0 次。這是 live 與研究的**第四層落差**。
- `high52_prox` **確認不在 production 的 f_tech 內**(Codex §五 已裁決以程式碼為準),
  它是 c2 的獨立腿;`GPT answer.md` §四-4 的說法作廢。

**測試**:研究 lifeline **25 passed**;完整 `tests/` **172 passed / 0 failed**
(上一輪 164 → +8:`_ensure_login` 三種語意 ×4、產業表 strict 閘門 ×1、app 換入還原 ×2、缺 token 語意 ×1)。

**狀態**:Codex §十 的七項停止條件全部完成,**停在此處等第二輪審查**。
四個待決問題見工作計畫 §11。

---

## 17. 🧪 2026-07-31 — Codex 第二輪審查通過(附限制)+ Phase 1 診斷面板建置器

**Codex 第二輪結論**:第一/二輪的基礎工作**通過**(獨立複跑 25 / 172 passed、未改權重與公式、
未重建 canonical、未跑 OOS)。授權進入 **Phase 1 測量層修正**,但明列七條暫時禁令
(不得改權重/公式、不得百分位或 z-score、**不得修缺值填法**、不得補 0050 後重跑 V0、
不得重跑 H1–H5、不得跑 OOS、不得把診斷結果當新策略結論)。

**A. 兩處措辭已依指示修正**(工作計畫 §6、本日誌 §16、`scripts/composite_reconcile.py`):

1. 對帳**不是全 panel** —— 只查了 **60 檔等距抽樣、7,369 stock-months**,原始面板 263,928 列
   (抽樣率 **2.79%**)。一律寫成「在 60 檔等距抽樣、7,369 stock-months 中,三層對帳 100% 通過」。
2. 它是**獨立的合成算術重算**,不是研究紀律 §3 的「第二條獨立實作」——
   `_bull_stack` / `_detect_washout` 仍共用 advisor。可當診斷證據,**不得**宣稱已滿足該紀律。

**B. V0 正式定位換成第二輪版本**(多了「研究與 live 路徑不一致」這一項):

> V0 的五維 composite＋c2 完整策略包通過既定 H1–H5,但其評分系統存在 regime 混入、
> 資料可得性斷裂、缺值語意不一致、研究與 live 路徑不一致等結構問題。因此 V0 可作為
> **frozen baseline,不可視為已修好的正式策略,也不可直接進入實盤**。

**C. Phase 1 建置器**:新檔 `beat_0050/realbody/build_diag_panel.py`(**不動** `build_realbody_scores.py`)。

- **隔離雙保險**:輸出目錄 `data/research_base/diag/`(`available_realbody_panels()` 的 glob 非遞迴,
  不會下鑽)+ 檔名 `diag_scores_*_diag.parquet`(不符 `realbody_scores_adv*w.parquet`)。
  實測 `resolve_realbody(1e6)` 仍解析到 V0 面板,診斷面板**結構上不可能**被當正規面板讀走。
- **33 欄**(原有 9 + 新增 24,全部是觀測欄位,零個新分數):實際合成 bucket `b_*`×5、
  `regime`、`dyn_weight`、`val_override`、`washout_override`、`valuation_basis`、
  `data_gaps` / `n_raw_gaps` / 四個缺失旗標、`score_error`、版本六欄
  (`mode` / `weights_version` / `builder_version` / `code_commit` / `panel_source` / `built_at`)。
- **權威 `real_composite` 仍由生產的 `advisor.advise()` 產出**;`composite_indep` 只是自檢欄。
- **硬驗收閘門**:每列 `real_composite` 必須與 V0 面板 `|Δ|≤0.01`,不同即 `SystemExit`。
  全循環跑完即等於補上 **263,928 列的逐列比對**(不再只是 2.79% 抽樣)。
- 同樣掛上 `_ensure_industry_map(strict=True)` 的 fail-closed 閘門。

**D. 單年驗證(`--year 2022`,1,514 檔 / 15,089 stock-months / 396s)—— 全數通過**:
覆蓋率 100.0000%、`real_composite` 逐列相同 **100.0000%**(max|Δ| 0.0000)、五面逐列相同、自檢 100%。

新欄位在 2022 的分布:`regime` **bear 74.99%** / bull 16.77% / neutral 8.24%;
`dyn_weight` 6.61%;`val_override` 1.71%;`washout_override` **0.00%**(印證死碼結論);
`pe_missing` 3.42%;三大財報全缺 0%;有 `data_gaps` 19.64%。

**⚠ 觀測(非結論)**:2022 有 **10/12 個月**被判 bear。bear 乘數讓正規化後的有效名目權重變成
**fund .358 / val .055 / tech .066 / mom .468 / whale .052** ——
2022 有四分之三的列,**動能 0.468、技術+籌碼合計僅 0.118**。
名目宣稱的「.31/.08/.19/.27/.15」在那一年不是實際在跑的東西;H5 的「2022」段就是在此權重下通過的。
**如何處置(是否保留 regime 進入選股)依工作計畫 §9 必須走新預註冊。**

**測試**:研究 lifeline 25 passed;完整 `tests/` **172 passed / 0 failed**(與上一輪相同,本輪未加測試)。

**E. 全循環已完成(2026-07-31,4,772s / 79.5 分,6 workers)**:
`data/research_base/diag/diag_scores_adv100w_diag.parquet`,**263,928 列 × 33 欄**。

啟動前補上第五道閘門(原本只有對帳會停、覆蓋率不足只會印):現為**五道致命條件** ——
覆蓋率<100% / `score_error`>0 / `real_composite` 與 V0 不符 / 五面不符 / 有列無法比對;
任一觸發即落 `.INCOMPLETE.parquet` 並非零退出。

驗收:覆蓋率 **100.0000%**(缺 0)、可比對 263,928 列(未能比對 0)、
`real_composite` 逐列相同 **100.0000%**(max|Δ| **0.0000**)、五面逐列相同、自檢 100%。
**五道條件全數未觸發。**
V0 面板完整性複驗:md5 `6cf06bdee83228eba45ce05fc9462b0b`、mtime `2026-07-30 02:53:44`,
**與啟動前存證逐位元相同**;`data/research_base/` 根目錄未新增任何檔案。

→ **補上了 §16 的抽樣缺口**:從 2.79%(7,369 列)升級為**全 panel 263,928 列逐列比對**。
⚠ 措辭限制不變:仍是「獨立的合成算術重算」,**不得**宣稱已滿足研究紀律 §3。

新增觀測欄位在全 panel 的分布:`regime` neutral **65.59%** / bull **26.52%** / bear **7.89%**;
`dyn_weight` **9.14%**;`val_override` 1.44%;`washout_override` **0.00%**;
`pe_missing` 3.15%;三大財報全缺 2.06%;有 `data_gaps` **20.24%**。

`washout_override` 在**全部 263,928 列**皆為 0 —— §16-E 的「washout 在研究路徑是死碼」
從抽樣結論升級為**全 panel 確證**。

**狀態**:Phase 1 完成。**未進入 Phase 2**(子因子診斷),未跑 OOS / H1–H5 / 新預註冊,
未改任何五維公式、權重或 c2。依 Codex 指示**暫不整批 commit**。

---

## 18. 🔎 2026-07-31 — Phase 2 診斷(全 panel,仍不改策略)

**授權**:Codex 第三輪 —— Phase 1 全 panel 驗收通過,進入 Phase 2,**只做診斷**。
**產出**:`scripts/phase2_diag_analysis.py`(唯讀診斷面板)、
`scripts/phase2_is_passed_sample.py`(硬門檻通過率,面板沒存只能抽樣)。
工作計畫新增 §6-A(五項分析)、§7-A(三分類)、§7-B(預註冊清單)。

**本節完全不碰報酬線** —— 沒有 join `exec_ret`,沒有任何 IC / 績效計算;
全部是「分數與狀態的分布」,不是策略結論。

### 五項分析結果

1. **有效權重**:名目權重(fund .31/val .08/tech .19/mom .27/whale .15)**只適用 59.73% 的列**。
   六種 (regime×dyn) 組合:neutral/F 59.73%、bull/F 23.97%、**bear/F 7.15%**、
   neutral/T 5.85%、bull/T 2.55%、bear/T 0.74%。
   bear 的有效權重是 **fund .358 / val .055 / tech .066 / mom .468 / whale .052**;
   dyn 啟用時估值被砍到 **.032**。估值/動能/籌碼各有 **40.27%** 的列 ≠ 名目。

2. **缺值**:有 `data_gaps` **20.24%**;PE 缺失 3.15%;三大財報全缺 2.06%(全在 2005,該年 60.2%)。
   **PE 缺失的方向是反的** —— 那批列 f_fund 中位 **51.3** vs 有值 46.3(**高 5.0 分**)。
   抽樣 40 檔 / 4,307 列量硬門檻:**三大財報全缺的 `is_passed` = 100.00%**,
   高於「無任何缺口」的 98.24%;組分數呈「獲利 0.0 / 安全 50.0 / **估值 80.0**」的人造形狀;
   **`FundamentalEngine` 信心中位仍是 100.0**(欄位都「有值」,只是值是捏的)→ 信心指標看不到。
   `data_gaps` **近年更嚴重**:2019 38.82% / 2023 **44.05%**,不是歷史殘留。

3. **washout**:全 panel **0 / 263,928** 觸發(研究路徑死碼,已確證)。
   影響上界:`f_whale<65` 佔 **72.92%**,全套用會讓籌碼分平均墊高 28.4 分 → composite **4.25 分**。
   真實落差落在 0 與上界之間,**無法從研究面板回推**。

4. **bucket vs 原始面**:只有估值不同 —— **3,804 列(1.44%)**,`f_val` 100% 是 0.0、
   合成用 50.0,差 **+50.0 分** → composite **+4.00 分**。籌碼 0 列(washout 從未觸發)。
   **更正 §1 血緣圖**:估值不是 3 條路徑而是 **5 種 basis**(產業位階 37.66% /
   **純歷史位階 32.39%** / PEG+位階 27.63% / 絕對 1.84% / 純 PEG 0.47%)——
   `valuation.py:149-154` 只論證了 85/15 混合,**沒論證純位階那一支**。

5. **六時代**:前三段 bear/bull **0.00%**、產業位階 **0.00%**、有效權重 ≈ 名目;
   後三段 regime 逐月切換、產業位階 87–96%。**2022 段 w_mom 0.423 / w_tech 0.097**。
   2019 前後平均 w_mom 0.273 → 0.321、w_tech 0.190 → 0.168。
   → **六個時代不是同一套評分定義在跑**(判定不改寫,只限制解釋)。

### 三分類(Codex 指定)

- **資料問題 9 項**(D1–D9):regime/RS/產業位階的 2019 斷裂、PE 缺失、近年 gaps 上升、
  2005 財報缺、`bt_bundle` 覆寫、產業表非 PIT、四層吞錯鏈(已修)。
- **定義問題 7 項**(F1–F7):技術面 91 個相異值、缺值三種相反語意、籌碼 clip、
  f_fund 估值腿與 f_val 同源、估值 5 種 basis、營收腿與 c2 重複、抵銷假說。
- **合成架構問題 5 項**(S1–S5,**本輪新增的第三類**):名目權重只適用 59.73%、
  五面未標準化就加權、bucket≠面板值、washout 死碼、六時代非同一套合成定義。

### 需要新預註冊:13 項(P-1 ~ P-13)

判準:**任何會改變 `real_composite` 逐列數值的動作都要**。含補 0050 後讓 regime/RS 生效、
改缺值填法、五面標準化、技術/籌碼量尺重寫、動能拆腿、是否保留 regime、是否保留 dyn weights、
是否把 washout 補進研究路徑、拿掉 f_fund 估值腿、估值 basis 統一、權重配置、`bt_bundle` 覆寫處置。
建議順序(**提案未凍結**):P-2 缺值中性化 → P-1 補 0050 → 量尺層 → 合成與權重。

**本階段禁止且未做**:改五維公式 / 改權重 / 改 c2 / 改缺值填法 / 補 0050 重建 V0 /
跑 OOS 或 H1–H5 / 把診斷數字當策略結論。

**狀態**:Phase 2 診斷完成。

### 18-1 Codex 第三輪回覆的三處補正(2026-07-31)

1. **抽樣 vs 全 panel 的界線已標明**:`is_passed` 與四個組分數(獲利/成長/安全/估值)、
   `FundamentalEngine` 信心 —— 這五項**只來自 40 檔 / 4,307 stock-months 抽樣**
   (抽樣率 **1.63%**),因為它們不在診斷面板裡,要量就得重建 bundle。
   工作計畫 §6-A-2 已加警語與「全 panel vs 抽樣」對照表;
   並註明「三大財報全缺」那一格只有 **97 個觀測**,`is_passed=100%` 的 Wilson 95% 下界約 96.2%,
   **方向可信、精確值不可引用**。
2. **全 panel 已確認的結果保留**:`data_gaps`、PE 缺失、三表全缺、`regime`、`dyn_weight`、
   `val_override`、washout 死碼、`valuation_basis`、六時代有效權重。
3. **P-1(補 0050)與 P-2(缺值中性化)降級為候選假設** —— 工作計畫 §7-B 已加註:
   不得據此改程式、不得建新面板;「診斷已證明填法是錯的」**不構成**動手的理由。

**未整批 commit**(依指示)。

---

## 19. 📝 2026-07-31 — Phase 3:新預註冊**草案**(只寫設計,未執行任何 OOS)

**產出**:`docs/預註冊_FaceRedesignV2_草案.md`。
**狀態**:**草案,尚未凍結**。已執行 OOS **0 個**、新數字 **0 個**、改程式 **0 行**、新面板 **0 份**。
凍結需經 Codex 審查 + 使用者明確指示。

**四層分離(Codex 第三輪 §4 指定)**,每個 arm **只有一類主要變更**(§5 指定):

| 層 | arms | 內容 |
|---|---|---|
| **A 資料定義** | A1 A2 A3 A4 | A1 補 0050→**只**開 RS;A2 補 0050→**只**開 regime;A3 缺值中性化;A4 f_val basis 統一 |
| **B 維度定義** | B1 B2 B3 B4 | B1 技術面分桶→連續;B2 籌碼面去 clip;B3 動能面移除與 c2 重複的營收腿;B4 基本面移除估值腿 |
| **C 合成架構** | C1 C2 C3 C4 | C1 拿掉 regime;C2 拿掉 dynamic weights;C3 五面百分位化後加權;C4 washout 補進研究路徑 |
| **D c2 角色** | D1 D2 | D1 c2 / composite / 交集**三腿分開評估**;D2 抵銷假說檢定 |

A1 與 A2 刻意拆開:同一份 0050 資料,但作用在不同層(f_mom 定義 vs 合成層權重),
合在一起就分不出是哪個在起作用。C3 只改「權重乘在什麼量綱上」、**不改權重數字**,故歸合成架構。

**已凍結的規格(草案值)**:V0 baseline(含面板 md5)、資料區間
(Train 2005-01~2014-12 / Validation 2015-01~2018-12 / **OOS 2019-01~2026-03**)、
走查協定為**主協定**(訓練窗≥60 月、每 12 月重估,把「選哪個 arm」放進 OOS)、
報酬線 `exec_ret.fwd_x`、基準階梯三階、成本 0.72%、
五道 Gate(排序器層 IC t>2.0 + 產業內中性化後仍>0 / 策略層 CAGR≥20% 且 MDD 不得比 V0 本體差 >5pp /
穩健六時代≥4 段 + 滑價 0.60% / 多重比較 DSR **N=14** p<0.05 /
繼承 FaceRedesign 的「打不過 c2 一半即降級」出口)、五個失敗出口 E1–E5。

**事前揭露的缺陷**:Train+Validation 全在「定義 α」、OOS 全在「定義 β」(2019 斷裂點)。
這個不對稱是**刻意保留**的 —— 它讓「斷裂是否重要」變成可檢定的;代價是 B/C/D 層的 OOS 帶雜訊,
判定時必須一併報告。另揭露 14 個 arm 是**看過 Phase 1/2 診斷後**設計的
(診斷未 join 報酬線,故未用報酬資訊挑 arm,但「哪些問題值得修」仍受影響)→
**DSR 的 N=14 低估真實多重比較**,不主張已充分校正。

**草案留了 5 個問題給 Codex 裁決**(§9):切分是否接受、14 個 arm 是否太多、
CAGR≥20% 是否適用於「排序器研究」(composite alone 在 V0 是 12.07%)、
MDD 相對門檻是否太寬、A1/A2 要不要先只做 A2。

**本階段禁止且未做**:改五維公式 / 改權重 / 重建 V0 / 跑 OOS / 重跑 H1–H5 /
把診斷數字寫成策略結論。

**狀態**:草案完成,**停止等待 Codex 審查**。未整批 commit。

---

## 20. 📐 2026-07-31 — 預註冊草案 V2 依 Codex 第四輪裁決改寫(仍未凍結、未執行)

**Codex 第四輪**對草案的五題給了裁決,另指出三個凍結前必須修的規格問題。全部照辦。

### 五項裁決的落實

| # | 裁決 | 落實 |
|---|---|---|
| 1 | 切分接受,但**重新命名** | 正式名稱改為「**2019 定義斷裂壓力測試**」,明寫「**不是**同一資料定義下的純 OOS」。六時代**降為描述性診斷**,Gate 3 的時代條款改成**只看 OOS 內的後三段**(19-21 / 2022 / 23-26) |
| 2 | 第一批只凍結 **6 個 arm** | A1–A4 + C1 + C3。D1/D2 → 拆解診斷 X1/X2,**不計入 N**;B1–B4 公式未寫死 → 第二批;C2 → 第二批。**DSR 改寫 N=6** |
| 3 | CAGR 20% **拆兩層** | Gate 2-A(`composite alone`):IC + 產業中性化 IC + **相對** V0 的 CAGR/Sharpe,**不設絕對 20%**;Gate 2-B(`composite ∩ c2`):才適用 CAGR≥20% 與 Sharpe 勝 0050 |
| 4 | MDD **保留相對門檻但修正基準** | 改成 `candidate OOS MDD ≥ V0 同一 OOS 區間 MDD − 5pp`,並先把 V0 基準固定成單一數字(見下)。絕對 MDD **僅作風險揭露,不作新增 Gate** |
| 5 | **A1 與 A2 必須一起**預註冊、一起執行 | 已寫死;並明列「**不得在看到 A2 結果後刪除 A1**」;資源不足只能整體延後到第二批 |

### V0 分段基準值已固定(`scripts/v0_oos_window_baseline.py`)

**這不是執行新 OOS**:量的對象是 V0 baseline 本身(H1–H5 已落記),此刻不存在任何 candidate。

| 區間 | 月 | CAGR% | Sharpe | MDD% | 換手% |
|---|---|---|---|---|---|
| Train 2005-01~2014-12 | 120 | 16.23 | 0.58 | **−68.72** | 73.4 |
| Validation 2015-01~2018-12 | 48 | 19.73 | 1.20 | −19.81 | 72.9 |
| **OOS 2019-01~2026-03** | 87 | **28.02** | **1.38** | **−17.01** | 67.8 |
| 全期 2005-01~2026-03 | 255 | 20.80 | 0.89 | −68.72 | 71.1 |

→ Gate 2-B 的 MDD 門檻 = **≥ −22.01%**。

**兩個必須記住的發現**:
1. V0 全期 MDD −68.72% **全部來自 Train 段**;OOS 區間只有 −17.01%。
   先前引用的「本體 MDD −68.7%~−70.4%」是**全期**值,拿它當 OOS 基準是錯的(Codex §4 已指正)。
2. **V0 在 OOS 區間的 CAGR 已是 28.02%** → Gate 2-B 的絕對 20% **在這個區間幾乎沒有鑑別力**
   (一個比 V0 差 7pp 的 arm 照樣過關)。已列入 §9 待決問題 1,**未自行凍結**。

### 三個凍結前規格修正

- **A3 硬門檻語意(已寫死)**:只改 `build_pit_stockdata` 的五個填值為 `None`,
  `FundamentalEngine` 一行不改。實測必然後果:三大財報全缺的列
  `is_passed` **True→False**、`total_score` **32.5→50.0**、`confidence` **88.0→4.0**;
  只缺 PE 的列 `total_score` 64.60→54.60(估值組 100→50)。
  **三件事前承認**:(a) A3 對缺財報列是**調高 +17.5 分**不是調低,與缺 PE 的 −10 分方向相反,
  **淨效果方向未知**,不得事後說「早就知道」;(b) `is_passed` 翻轉**不影響選股**
  (選股只看 composite∩c2 百分位),須在結果中複核;(c) `confidence` 崩到 4.0,
  目前無 confidence 篩選故不影響,但未來若加就會全被砍。
- **A4 多重 fallback(已寫死)**:**承認第一版寫錯** —— 關掉 `USE_INDUSTRY_RELATIVE` 並不會
  統一成一種配方,路徑② 仍會依哪一腿有值分裂成三種(85/15 混合 27.63% / **純位階 32.39%** /
  純 PEG 0.47%),還會再退路徑③。A4 改為:**兩腿都有值才用固定 0.85/0.15,任一腿缺就直接判
  「估值資料不足」→ bucket 50,不做權重重分配、不退路徑③**。事前承認 `val_override`
  會從 1.44% 大幅上升(上界約 72%),並事前寫死出口 E6:若因此失去意義,那本身就是結論。
- **C4 不是「尚無管線」是「尚無資料」**:實測 `~/tej_cache/margin_balance/` 有 1,859 檔,
  但每檔只有 **72 列 / 2026-04-01 ~ 2026-07-16**(約 3.5 個月)。回測需要 2005–2026 日資料 →
  **在補到歷史之前不可能執行**,已移出第一批並留紀錄。

### 狀態

草案 **仍未凍結**。已執行 candidate OOS **0 個**、改程式 **0 行**、新面板 **0 份**;
唯一的新數字是 §1-1 的 V0 baseline 切片(Codex 明確要求)。
新增 5 個待決問題(§9),其中最重要的是**絕對 20% CAGR 的鑑別力**與
**Gate 2-A 的 V0 `composite alone` 基準尚未計算**。

**停止等待 Codex 第五輪審查。** 未整批 commit。

---

## 21. 🔧 2026-07-31 — Codex 第五輪:修 A1/A2 污染 + 補 Gate 2-A 基準 + 第二批 B 層寫死

**仍未凍結、仍未執行任何 candidate OOS。改的是規格文件與量測腳本,不是策略。**

### A. Codex 指出的必修項:A1/A2 不是單一變更

前一版用「把另一個通道**全期**關掉」來隔離 —— A1 寫 `current_regime` 全期 `None`、
A2 寫 `USE_RS_OVERLAY` 全期關閉。但 V0 在 2019 之後 regime 是逐月切換的、RS 是生效的,
所以 **A1 實際上 = 補 RS + C1**、**A2 實際上 = 補 regime + 拿掉 RS**,兩個都是雙變更,
`N=6` 的 DSR 失去意義。

**修法(`scripts/a1a2_isolation_check.py` 實測後寫死)**:改成
**拼接資料源 + 單一消費端注入**,生產計分碼一行不改、旗標一個不翻。

| 量測 | 結果 |
|---|---|
| 兩份 0050 重疊 1,827 列的 `close` | **逐列相同,max\|Δ\|=0.000000** |
| 接縫 | 2018-12-28 → 2019-01-02 close 75.50 → 74.05,**無跳空** |
| `_back_adjust` 斷點數 | 三個序列都是 **1 處**(2025 分割);回補後 2019 起價格 max\|Δ\|=0 |
| RS vs regime 的基準源 | **同一個** `TaiwanStockPrice/0050`,兩條取用路徑輸出逐列相同 |

注入點各只有一處:A1 → `core.backtest._rs_bench_bundle`;
A2 → `core.score_store._bench_state`。另一個消費端**完全不動**。

**邊界污染窗(必然存在,已寫死並列出確切月份)**:trailing window 跨接縫 →
- **A1**:2019-01~06 共 **6 個** as_of 的 `rs_6m` 由 `None`→有值(`rs_3m` 是前 3 個)。
  **沒有任何原本有值的 RS 被改成不同的值。**
- **A2**:**4 個**月標籤改變(2019-01/02 neutral→bear、2019-06/07 neutral→bull)。
  原本的 neutral 是**資料不足的強制預設**,不是真的判定中性。
- **2019-08 起兩者都與 V0 逐月完全相同。**

→ 佔 OOS 87 月的 8.0%。已寫死:**兩組數字都報(全 87 月 / 剔除後 80 月),
不一致時以 80 月版為準**(新出口 **E8**)。

### B. 修正兩個先前的錯誤說法(§16-D-1 已加註)

1. **「2019 起一致率 87.6%」是被污染的數字。** 舊稽核腳本沒對 `0050_raw.parquet` 套
   `_back_adjust()`,而生產路徑有。2025 年 0050 分割在未回補的序列上是一天 **−75%**
   的人造崩盤 → 強制 bear。11 個不一致月份**有 7 個落在 2025**。
   **兩邊都回補後:一致率 95.4%,4 個不一致月份全部落在 2019 上半年。**
2. **「兩條路徑讀不同的 0050」的機制描述是錯的。** 同一個資料集、重疊區間逐列相同;
   `0050_raw.parquet` 是**同一條序列的歷史延長**,不是另一個資料源。
   → **「2019 前全 neutral 唯一因為資料缺口」這個結論不變**,錯的只是機制描述。

### C. Gate 2-A 基準已補算,**與 Codex 獨立重算逐位相同**

`scripts/v0_composite_alone_baseline.py`(V0 的 `composite alone`,少 c2 腿,其餘全同):

| 區間 | 月 | CAGR% | Sharpe | MDD% | 換手% | 持股 |
|---|---|---|---|---|---|---|
| Train 2005-01~2014-12 | 120 | 10.70 | 0.38 | −66.47 | 55.1 | 172 |
| Validation 2015-01~2018-12 | 48 | 10.09 | 0.59 | −21.10 | 54.0 | 211 |
| **OOS 2019-01~2026-03** | **87** | **24.56** | **1.13** | **−22.51** | 49.0 | 240 |
| 全期 2005-01~2026-03 | 255 | 15.12 | 0.63 | −66.47 | 52.4 | 202 |

Codex 第五輪 §2 給的三個數字是 24.56 / 1.13 / −22.51 —— **兩條各自實作、逐位相同**。
腳本內建硬性自檢:把 c2 腿加回去必須與凍結的 `dual_confirm_mask()` **逐格相同**
(255×2,148 格全同),不同即 raise。

**兩個觀察(不是結論)**:`composite alone` 在 Train/Val 只有 10.70/10.09% CAGR、
OOS 卻是 24.56%,同一個排序器前後差 14pp;c2 那一腿在 OOS 只加 3.46pp CAGR,
但把 MDD 從 −22.51% 收到 −17.01%,而在 Train 段加了 5.53pp → **c2 的貢獻不均勻**。

### D. 門檻凍結(依 Codex 第五輪)

- **Gate 2-B 移除絕對 CAGR 20%**,改嚴格非劣性:**CAGR ≥28.02% / Sharpe ≥1.38 / MDD ≥−22.01%**。
- **Gate 2-A**:CAGR ≥24.56% / Sharpe ≥1.13 / MDD ≥−27.51%(MDD 的 −5pp 條款是我加的,已標明)。
- **Gate 3** 保留 ≥2/3,加寫「三段一律全報」。
- **A4 加可測性門檻**:paired 覆蓋率逐 OOS 時代 **≥50%**,未達 → 標「**不可測**」(≠失敗,新出口 **E7**)。
  事前揭露:paired 覆蓋率目前**只知界 [27.63%, 65.29%]**(V0 的路徑① 37.66% 那批從未走到路徑②,
  無從得知兩腿是否都有值)→ **50% 門檻可能兩邊都落**。
- **C3 的規格補寫**(原本一行敘述有歧義):百分位化的對象是 **advisor 實際加權的五個 bucket**,
  不是 `score_result` 上的五面原始分 —— 否則會同時繞過 bucket 覆寫,變成雙變更。

### E. 第二批(B 層 + C2)已同日寫死,暫不執行

`docs/預註冊_FaceRedesignV2_第二批_B層草案.md`。**`N_total = 6 + 5 = 11`**;
第一批 Gate 4 的通過條件仍用 N=6,**N=11 的 DSR 列為必報的次要數字**。

**baseline 改成 `V0-C3`(V0 + C3 的百分位量尺),現在就凍結、與 C1/C3 的第一批結果無關。**
理由:在未標準化的量尺上,任何改變維度**分數尺度**的動作同時就是一次有效權重變更 ——
「B3 是否改善排序力」與「momentum 有效權重是否下降」**無法分離**。百分位量尺讓單調
尺度變換成為 no-op,每個 B arm 才真的只有一個變更。**這是設計決定,不是結果決定。**

| arm | 唯一變更 | 新自由參數 |
|---|---|---|
| **B1** | `f_tech` 均線腿(ATR 標準化斜坡)+ RSI 腿(桶中點分段線性)連續化 —— **104 分中的 70 分** | **1**(斜坡半寬 1.0 ATR) |
| **B2** | `f_whale` 去掉最外層 `min(max(·,0),100)`;內部 cap 全保留 | **0** |
| **B3** | `f_mom` 移除營收腿(30 分);**不重標** | **0** |
| **B4** | `f_fund` 移除估值組,三組重標到和 1(0.375 / 0.3125 / 0.3125) | **0** |
| **C2** | `_dynamic_weights` 一律回 `(base, False)` | **0** |

**B 層要修的東西已全 panel 量出(不 join 報酬線)**:`f_tech` **只有 91 個相異值、
層內並列率 94.6%**;`f_mom` 101 值 / 93.1%;`f_whale` **15.68% 的列被夾在邊界**
(8.13% 在 0、7.55% 在 100)—— 那批列在維度內部完全沒有排序資訊。
百分位化**不能**解決分桶並列,所以 B1 是必要的。

**C2 的排除理由原本是結果相依的**(「留待與 C1 的結果一起評估」)→ 已改成無條件寫死進第二批。
**留紀錄不得事後塞入**:B5(動能面連續化)、B6(`f_whale` 內部 cap)、C1+C2 同時拿掉、
其他量尺方案、權重數字本身。

### F. 本輪沒做

沒改任何五維公式 / 權重 / c2 / 缺值填法;沒建新面板;沒跑 OOS 或 H1–H5;
V0 面板未被觸碰。新增的三個腳本都不 join 報酬線之外的東西:
`v0_composite_alone_baseline.py`(V0 baseline 切片)、`a1a2_isolation_check.py`
(regime 標籤與資料可得性,**零報酬、零 IC**)。

新增 5 個待決問題(第一批 §9-A),最重要的兩個:
**Gate 2-B 的嚴格非劣性沒有容許帶是否為 Codex 本意**、
**要不要直接把 OOS 定義成 2019-08 起的 80 月**(代價是已核可的 V0 基準值要重算)。

**停止等待 Codex 第六輪審查。** 未整批 commit。

---

## 22. ⏱ 2026-07-31 — Codex 第六輪:80 月共同時鐘 + bootstrap 門檻 + N=12,並量出 Gate 檢定力不足

**仍未凍結、仍未執行任何 candidate OOS。V0 面板 md5 未變。**

### A. OOS 主時鐘改成 80 月(2019-08~2026-03),所有 V0 baseline 重算

Codex 第六輪 §3:六個 arm 統一時鐘,87 月版只作 robustness 描述,
**不得**「衝突時選 80 月」(前一版出口 **E8 已刪除** —— 那仍留了結果相依的裁量縫)。

A1/A2 的邊界污染窗(2019-01~07)剛好完整落在 Validation 結束與新 OOS 開始之間
→ 成為**不屬於任何段的 7 個月空窗**,不進任何指標、不進走查訓練窗與量測窗。
月數會計:255 − 168(Train+Val)− 7(空窗)= **80**;走查在 248 月序列上運行。

| 腿 | 87 月(舊) | **80 月(主)** | Δ |
|---|---|---|---|
| `∩c2`(Gate 2-B) | 28.02 / 1.38 / −17.01 | **28.00 / 1.34 / −17.01** | CAGR −0.02pp / Sharpe −0.04 / MDD 0 |
| `composite alone`(Gate 2-A) | 24.56 / 1.13 / −22.51 | **24.61 / 1.09 / −22.51** | CAGR +0.05pp / Sharpe −0.04 / MDD 0 |

→ **換窗不是在挑對 V0 有利或不利的區間**,被剔除的 7 個月對 V0 幾乎無影響。

Gate 3 的三段與主時鐘取交集後變成 **29 / 12 / 39 月**(19-21 段被截短;
`honest_backtest.ERAS` 一行不改,截短是取用約定)。V0 分段值:
`∩c2` 34.60 / 8.57 / 29.70;`alone` 28.48 / **−3.47** / 31.76。
→ **2022 那年 `alone` 是 −3.47%、`∩c2` 是 +8.57%,12pp 全部來自 c2 那一腿。**

### B. Gate 2 改 paired block bootstrap —— 並在凍結前量出**它的檢定力不足**

凍結參數(Codex 第六輪 §1 要求「現在寫死」):**paired circular moving-block bootstrap**,
`L=12` 月 / `B=10,000` / `seed=20260731` / 雙尾 95%(下界第 2.5 百分位);
`Δ = met(cand) − met(V0)`,CAGR 與 Sharpe 的 CI 下界皆須 ≥ 0。
參考實作 [gate2b_bootstrap.py](scripts/gate2b_bootstrap.py),**candidate runner 必須 import 它**。
退化檢查已過:同一條腿對自己,`Δ` 恆 0、CI 恰為 `[0,0]` → paired 索引確實同時套用兩腿。

**但檢定力探測的結果必須在凍結前講**:拿 V0 自己的兩條腿當對照
(相差 **+3.39pp CAGR / +0.244 Sharpe**,= 整條 c2 腿的貢獻):

| | 觀測 Δ | 95% CI | 下界 ≥ 0? |
|---|---|---|---|
| ΔCAGR | +3.39pp | **[−5.61, +10.43]** | ❌ |
| ΔSharpe | +0.244 | **[−0.107, +0.559]** | ❌ |

**連整條 c2 腿都判不出顯著。而且不是區塊長度選錯**:

| `L` | 1 | 3 | 6 | 12 | 24 |
|---|---|---|---|---|---|
| ΔCAGR CI 下界 | −3.60 | −3.94 | −4.81 | −5.63 | −5.14 |

`L=1`(i.i.d.,檢定力上限)仍 −3.60pp;單尾 95% @L=12 仍 −4.29pp。
→ **這是 80 個月觀測值的樣本量極限,不是參數問題。**
必然後果已寫死成出口 **E9**:六個 arm 幾乎必然全部止於 E4,而那是**事前已知**的,
**不得**事後放寬 §5-1 的任何參數或延長 OOS。

三個處置選項留給第七輪(§9-A 待決 1):**(a) 改成非劣性檢定**(CI 下界 ≥ −δ,`δ` 事前凍結)、
**(b) 維持現狀並接受全部 E4**、**(c) 主判定移到 Gate 1**(rank IC 有約 2,000 檔 × 80 月的
橫斷面觀測,檢定力高一個量級)。**建議 (a) 或 (c),但沒有自行選。**

### C. 其餘裁決

- **Gate 2-A 移除 MDD gate**(Codex 第六輪 §2)—— 我上一版自加的 −27.51% 已刪,MDD 改描述統計。
  CAGR/Sharpe 一併改用同一個 bootstrap(**這是我的延伸**,已標明可改回)。
- **DSR 主校正改 `N=12`**(必修正點):`N=6` 降級為「第一批 interim-only」分析,不是通過條件。
  前一版「通過用 N=6、N=11 只作次要」被判定為兩面討好。
- **MDD 刻意不 bootstrap**:它是路徑相依統計量,block bootstrap 打散路徑後分布無現實意義;
  維持點估計的 −5pp 非劣性界線(≥ −22.01%)。

### D. 第二批:加入 B5、改無條件執行、`N_total=12`

**B5(`f_mom` 分桶連續化)已加入並寫死完整公式**。為避免十幾道階梯各自變成任意選擇,
先立了一條**確定性的機械規則 R**(§0-3):有界桶取中點為節點、開放端桶取
「有限邊 ± 相鄰有界桶寬度的一半」、節點間線性內插、節點外平延;
**單一門檻的 on/off 項不適用規則 R**(沒有相鄰有界桶可推寬度)→ 一律維持 V0 原樣。

規則 R **零自由參數**(節點位置完全由 V0 自己的門檻決定),所以 B5 的八張節點表
(`mom_6m` / `mom_3m` / RS / `revenue_accel` / `revenue_cum_yoy` / `streak` /
`volume_spike` / `ma20_bias`)全部是機械推導的結果,已逐張列出。
連續化覆蓋 108 名目正分中的 **103 分**;不變的是動能衰竭 −8、量價背離 ±5、`kd_j>100` −3、
`volume≥500` 閘。B1 的 RSI 節點表也**改成依規則 R 重推**(25/35/45/60/72.5/77.5),與 B5 一致。

**B5 與 B3 不得合併**:B3 問「營收資訊該不該在 f_mom 裡」(移除腿)、
B5 問「分桶粒度是不是問題」(保留全部腿只換映射)—— 方向相反不是矛盾(出口 E15)。

**第二批改為無條件執行**(Codex 第六輪 §4):第一批完成後即執行,不論結果如何。
算力承諾 6 個 arm × 各一份面板。替代方案(拆成完全獨立的後續 prereg)會讓 `N_total`
不能寫 12、必須承認分批降低懲罰 —— **已選前者**。
第二批的 Gate 2 基準是 **V0-C3**,其數值需先建 C3 面板才能算,已標明「未填入前 Gate 不成立」。

### E. 本輪沒做

沒改任何五維公式 / 權重 / c2 / 缺值填法;沒建新面板;沒跑任何 candidate OOS 或 H1–H5。
新增的唯一腳本 `gate2b_bootstrap.py` 只跑 V0 自己的兩條腿(**不存在 candidate**)。
`tests/` 172 passed / 0 failed。V0 面板 md5 仍是 `6cf06bde…`。

**停止等待 Codex 第七輪審查 —— 關鍵待決是 §9-A 待決 1 的檢定力處置,
在裁決之前不得執行任何 arm**(否則就會變成「跑完看到全部 E4,再回來放寬門檻」)。未整批 commit。

---

## 23. 🎯 2026-07-31 — Codex 第七輪:Gate 1 升為主判定 + 12-arm max-t,並量出 V0 自己的 IC t = 0.88

**仍未凍結、仍未執行任何 candidate OOS。V0 面板 md5 未變。**

### A. Codex 選了 (c):Gate 1 作主要效能判定,Gate 2 降為風險/描述性揭露

- **Gate 1** 現在是主判定,且 `t > 2.0` **已被撤除** —— Codex 指出固定 2.0 不能對 12 個 arm
  各自套用。改成**事前凍結的 12-arm max-t 置換校正**(§5-2):
  逐 as_of 打散 `fwd_x` / 2,000 次 / seed 20260731 / 單尾 α=0.05 /
  **同一次置換的同一組打散順序必須同時套用到全部 12 個 arm**(否則 max-t 退化成 Bonferroni)/
  門檻 `T*` = 12 個 arm 的 t 取 max 後的虛無 p95。單步 max-T,**不是** Westfall-Young step-down。
  G1-a(raw IC)與 G1-c(產業內中性化 IC)**各一套 `T*`**。
- **G1-b(基準階梯①)標成「篩網不是檢定」** —— 對每個 arm 重跑完整回測 × 2,000 次置換
  算力不可行,此限制已事前揭露(待決 2)。
- **Gate 2 的 CAGR / Sharpe 通過條件全部移除**,只報 §5-1 bootstrap 的 CI。
  Gate 2-A **完全沒有通過條件**;Gate 2-B 只保留 **MDD ≥ −22.01%** 作為風險護欄
  (這是我對「風險/描述性揭露」的解讀,已標明可刪 —— 待決 3)。
- Gate 3 維持三段 29/12/39 不合併、7 個污染月繼續完全排除 → 我上一輪的待決 3、4 **已撤回**。

### B. `T*` 會落在哪裡:已量

`scripts/gate1_maxt_power_check.py`。單 arm 置換虛無(主時鐘、2,000 次):
`mean −0.047 / sd 1.011 / p95 1.569 / p99 2.422` —— 與標準常態幾乎一致,t 的尺度正常。
12-arm max-t 門檻隨 arm 間相關 ρ 變化:

| ρ | 0.00 | 0.50 | 0.80 | 0.90 | 0.95 | 0.99 | 1.00 |
|---|---|---|---|---|---|---|---|
| max-t p95 | 2.628 | 2.504 | 2.261 | 2.104 | 1.982 | 1.804 | 1.654 |

對照:單 arm 單尾 95% = 1.645、**Bonferroni(0.05/12)= 2.638**。
12 個 arm 都是 V0 的單點變體 → ρ 預期很高 → **`T*` ≈ 1.8–2.1**。
**用 max-t 而不是 Bonferroni 不是為了放寬** —— Bonferroni 假設獨立,對高度相關的族系是錯的校正。
ρ 必須實測,不得事前假設。

### C. ⚠ 凍結前的關鍵發現:**V0 自己在判定窗內的 rank IC t = 0.88**

| 區間 | 月 | rank IC | **t** | 產業內中性化 IC | **t** | 月勝率% |
|---|---|---|---|---|---|---|
| Train 2005-01~2014-12 | 120 | 0.0207 | 2.32 | 0.0099 | 1.28 | 58.3 |
| Validation 2015-01~2018-12 | 48 | 0.0255 | 2.10 | 0.0168 | 1.56 | 64.6 |
| **OOS(主,判定窗)** | **80** | **0.0092** | **0.88** | **0.0020** | **0.26** | 55.0 |
| OOS(robust,87 月) | 87 | 0.0104 | 1.03 | 0.0033 | 0.44 | 54.0 |
| 全期 | 255 | 0.0181 | 3.07 | 0.0089 | 1.83 | 58.0 |
| 19-21(截短) | 29 | 0.0019 | 0.10 | −0.0066 | −0.42 | 51.7 |
| 2022 | 12 | −0.0262 | −1.25 | −0.0161 | −1.03 | 41.7 |
| 23-26 | 39 | 0.0255 | 1.73 | 0.0140 | 1.43 | 61.5 |

V0 的置換 p 值 **0.179**。三件事:

1. **判定窗內 V0 的 composite 沒有統計上可偵測的橫斷面排序力**;而 Gate 1 是**絕對**檢定
   (`T*` ≈ 1.8–2.1),**不是**與 V0 比較 → **這個門檻是 V0 自己在這個窗內達不到的。**
2. **IC 從 Train/Val 的 0.021–0.026 腰斬到 OOS 的 0.0092**;產業內中性化後從
   0.010–0.017 掉到 **0.0020**(幾乎歸零)。判定窗恰好是 V0 排序力最弱的一段。
3. **同一個窗裡 `∩c2` 是 CAGR 28.00% / Sharpe 1.34** → V0 在 OOS 的績效**不是**來自
   composite 的全橫斷面單調排序力。來自哪裡(c2 腿?Top-20% 尾端?母體本身?)
   由 X1 拆解診斷回答,**此處不下結論**。

**這個量測不能推出任何 arm 會不會通過** —— 效果量未知,結果一律按事前門檻判定(出口 E10)。

四個處置選項留給第八輪(§9-A 待決 1):**(a) 維持絕對門檻**(否定的對象會包含 V0 自己,
那本身是有價值的結論)、**(b) 改成對 V0 的配對 ΔIC 檢定**(arm 與 V0 共用同一組橫斷面,
`ΔIC` 的月度變異遠小於各自的 IC → 檢定力可能高得多;但我**無法事前量它**,那需要至少一個
arm 存在)、**(c) 換判定窗**(87 月只把 t 從 0.88 拉到 1.03,幫助有限;全期則不再是樣本外
—— **不建議**)、**(d) 改用 Top-20% 尾端統計量**(需要新的凍結與新的置換設計)。
**建議 (b),必要時搭配 (a) 作必報的次要數字。**

### D. 依 Codex §5 修正措辭

§5-1a 與 E9 原本寫「六個 arm 幾乎必然全部 E4」—— 那是把**尚未執行的結果預先寫死**。
已改成「能推出 / 不能推出」兩段:**能**推出「V0 兩條腿那個量級(+3.39pp / +0.244)的差異
在此設定下判不出顯著,候選若效果小於該對照差異可能無法被此 Gate 區分」;
**不能**推出任何 arm 的去留。新增出口 **E10**(Gate 1 全未過的處置),
E9 改成「Gate 2 的描述性 CI 橫跨 0」——它**不再是出局條件**。

### E. 修掉自己兩個 bug

`gate1_maxt_power_check.py` 初版的產業內中性化寫錯(組內 rank 的均值 = `(n_g+1)/2`,
是組別常數而非產業去均值),另有一個 print 的 `.replace` 造成 `2.64.2.645/12` 的亂碼。
**兩者都在報告數字之前修掉**;上表的產業內中性化欄位是修正後的值
(修正前 OOS 是 0.0084/t 0.80,修正後 0.0020/t 0.26 —— 差別很大,幸好在報出去之前抓到)。

### F. 本輪沒做

沒改任何五維公式 / 權重 / c2 / 缺值填法;沒建新面板;沒跑任何 candidate OOS 或 H1–H5。
新腳本 `gate1_maxt_power_check.py` 只量 V0 與純虛無分布(**不存在 candidate**)。
`tests/` 172 passed / 0 failed。V0 面板 md5 仍是 `6cf06bde…`。

**停止等待 Codex 第八輪審查 —— 關鍵待決是 §9-A 待決 1:
主判定要用絕對 IC 檢定還是對 V0 的配對 ΔIC 檢定。裁決之前不得執行任何 arm。** 未整批 commit。

---

## 24. 🧬 2026-08-01 — Codex 第八輪:主統計量改配對 ΔIC + 12-arm joint max-t 實作與 synthetic 驗證

**仍未凍結、仍未執行任何 candidate OOS。V0 面板 md5 未變。**

### A. 主統計量改成對 V0 的配對 ΔIC(第八輪 §1 選 (b))

上一輪量出「Gate 1 的絕對門檻是 V0 自己達不到的」(主窗 rank IC t=0.88、
產業內中性化 t=0.26)。Codex 據此選 (b):統計量改成
**`ΔIC(t) = IC_arm(t) − IC_V0(t)`** 的配對檢定 —— 直接回答「修完之後排序力有沒有改善」。

**凍結規格已寫死**(預註冊 §5-2),實作 [gate1_delta_ic_maxt.py](scripts/gate1_delta_ic_maxt.py),
**arm runner 必須 import,不得另寫一份**:

- **共同股票集 `I(t)`** = `fwd_x` + V0 分數 + **全部 12 個 arm 分數**皆非缺的交集;
- **族系共同月份集 `M*`** = `|I(t)| ≥ 30` 的 as_of;**12 個 arm 與 V0 一律同一組 `M*`/`I(t)`**;
- 缺值**一律剔除**,不補中性值、不補 0;必報各 arm 因交集被剔除的股票數;
- 置換:**逐 as_of 打散 `fwd_x` rank**,**同一次置換的同一組順序同時套用到 V0 與全部 12 個 arm**
  —— 這同時保住配對結構與 arm 間相關結構;2,000 次 / seed 20260731 / 單尾 α=0.05;
- `T*` = 每次置換取 12 個 `t(ΔIC)` 的 max → p95。**單步 max-T,不是 step-down。**

### B. 一個必須寫進凍結規格的守衛:`EPS_SD = 1e-12`

T1 退化檢查(12 個 arm 全設成 = V0)**第一次是失敗的**:
`std(ΔIC) ≈ 4e-17`(機器誤差級),`mean/std` 把浮點殘渣放大成
**假 t ≈ 0.56、虛無 max-t ≈ 2.76** —— 一個「什麼都沒改」的 arm 憑空得到 t 值。

→ 凍結:`std(ΔIC) < EPS_SD` 時**一律記 t = 0** 並報告成「與 V0 無差異」。
**這不是防禦性程式碼**:真實 arm 也會踩到(例如某變更實際命中 0 列,
像 washout 那種在研究路徑觸發 0 次的情形)。

### C. 12-arm synthetic 驗證(第八輪 §4 要求),四項全過

| 驗證 | 結果 | 判定 |
|---|---|---|
| **T1** 退化(12 arm 全 = V0) | 觀測 max\|t\| = 0.000、虛無 max-t = 0.000 | ✅ |
| **T2-A** FWER,偏離互相獨立 | ρ̂ **0.025** → `T*` **2.686**;獨立虛無拒絕率 **4.60%** | ✅ |
| **T2-B** FWER,偏離高度相關 | ρ̂ **0.903** → `T*` **2.226**;拒絕率 **4.25%** | ✅ |
| **T4** MDI | `2.686 × 0.00910 / √80` = **+0.00273 IC**(= V0 IC 0.0092 的 **+30%**) | — |

**ρ̂ 上升 → `T*` 下降,方向與理論一致** ⇒ joint max-t **確實在使用**相關結構,
不是退化成 Bonferroni(0.05/12 ≈ 2.638)。族系 A 的 12 個純雜訊 arm 觀測 t 在
`−1.25 ~ +1.24`,超過 `T*` 的 **0 個**。

**T3 檢定力曲線**(12 個 arm 中只有 1 個注入已知改善):

| 實測 ΔIC | +0.0013 | +0.0022 | +0.0031 | +0.0048 | +0.0093 |
|---|---|---|---|---|---|
| 相對 V0 IC | +14% | +24% | +33% | +53% | +101% |
| **偵測率** | 8% | 25% | **67%** | **100%** | 100% |

**這是與上一輪最重要的差別**:Gate 2 的 bootstrap **連整條 c2 腿都判不出顯著**;
配對 ΔIC 在 **+30% 相對改善**處已有約 2/3 檢定力、**+50% 以上接近 100%**。
→ **Codex 選 (b) 是對的,而且現在有量化依據。**

**三個一起說的限制**:(1) `T*` 2.686/2.226 與 MDI +0.00273 **都是 synthetic 校準值,
不是真實門檻** —— 真實值必須由同一凍結演算法在 12 個真 arm 的共同資料上產生;
(2) synthetic arm 的偏離是逐 stock-month **i.i.d.** 雜訊,真實 arm 的偏離是**結構性**的,
i.i.d. 會放大 `ΔIC` 的月度變異 → 此處的 MDI 偏保守、檢定力偏低估,但**不得事前假設**;
(3) ρ̂=0.903 那一列只證明機制對相關結構有反應,**不得**當成真實 arm 的 ρ̂。

### D. 其餘三項裁決

- **G1-b 降為純描述,不再是 Gate 1 通過條件**(§2):「若 `>0` 會造成 arm 通過/失敗,
  它就不是篩網而是第三個檢定,必須納入校正」。→ Gate 1 的通過條件**只剩 G1-a 與 G1-c**。
- **MDD 風險護欄保留**(§3):Gate 2-B 維持 `MDD ≥ V0 − 5pp`(= ≥ −22.01%),
  Gate 2-A 不設 MDD gate。我上一輪的 hedge 措辭已移除。
- **`T*` 不必事前寫死數字**(§4)已接受;同時依 Codex 最後一段,
  把「ρ≈0.9 → `T*`≈1.8–2.1」全部改成**預測範圍**,不再寫成已知事實。
  舊的 `gate1_maxt_power_check.py` 已在檔頭標明**降級**:它的等相關模擬不是 12-arm 驗證,
  現在只用來量 V0 的絕對 IC 水準(§5-2b 那張表)。

### E. 本輪沒做

沒改任何五維公式 / 權重 / c2 / 缺值填法;沒建新面板;沒跑任何 candidate OOS 或 H1–H5。
`gate1_delta_ic_maxt.py` 的 synthetic arm **不是 candidate**(T3/T4 刻意含前視,
只為校準操作特性)。`tests/` 172 passed / 0 failed。V0 面板 md5 仍是 `6cf06bde…`。

**三個新待決(§9-A)**:`T*_ind`(G1-c)尚未做 synthetic 驗證、
G1-a 與 G1-c 是 AND 還是只看 G1-a、要不要把 T1/T2 縮小規模放進 `tests/` 當回歸測試。
**停止等待 Codex 第九輪審查。** 未整批 commit。

---

## 25. 🧪 2026-08-01 — Codex 第九輪:補 G1-c 實作與驗證、消除 warning、加回歸測試

**仍未凍結、仍未執行任何 candidate OOS。V0 面板 md5 未變。**

### A. 兩個必修點

**1. `rho_hat` 在退化族系全 NaN → `RuntimeWarning`。** 原本用 `nanmean` 吞掉全 NaN 相關矩陣,
會噴 `Mean of empty slice / All-NaN slice`。已改成**明確回報**:
`rho_hat=None` + `rho_status="degenerate:所有 arm 的 t 恆定(與 V0 無差異)→ 相關係數無定義"`;
只有部分 arm 恆定時排除後估計並標 `partial`。
`python -W error::RuntimeWarning scripts/gate1_delta_ic_maxt.py` **乾淨跑完**。

**2. 第八輪那版完全沒有 G1-c 的產業中性化實作** —— Codex 指出「不能說 `T*_ind` 已沿用完成」,
這是對的。已補 `build_month_blocks(..., neutral_by=)`,並加 `assert_same_months()`
硬性複核 G1-a 與 G1-c 跑在同一組 `M*`。

**凍結的中性化定義**:分數的**全體 rank 減掉該股所屬產業的全體 rank 平均**,再標準化;
產業欄 `tej_ind_name`(`fillna('未分類')`);**報酬那一腿不中性化**。
與 `gate1_maxt_power_check._demean_by` 逐字相同,所以 §5-2b 的 V0 數字可直接對照。

### B. G1-c 的 T1–T4 已補跑(第九輪 §1)

| 驗證 | G1-a(raw ΔIC) | **G1-c(ΔIC_ind)** |
|---|---|---|
| T1 退化 | ✅ t=0 / `T*`=0 / ρ̂「無法估計」/ 無 warning | ✅ 同 |
| T2-A(偏離獨立) | ρ̂ 0.025 → `T*` **2.686**;FWER **4.60%** | ρ̂ 0.026 → `T*_ind` **2.690**;FWER **5.15%** |
| T2-B(偏離高度相關) | ρ̂ 0.903 → `T*` **2.226**;FWER 4.25% | ρ̂ 0.903 → `T*_ind` **2.229**;FWER 4.10% |
| T4 MDI | **+0.00273 IC** | **+0.00255 IC** |
| V0 同窗基準 IC | 0.0092 | **0.0020** |
| **需要的相對改善** | **+30%** | **+128%** |
| `M*` | 80 月 | 80 月(**一致 ✅**) |

T3 檢定力(G1-c):+0.0015 → 17% / +0.0023 → 33% / **+0.0031 → 67%** / +0.0047 → 100%。

### C. AND 已確認,而且量出「**G1-c 才是綁定約束**」

Codex 第九輪 §2 確認 **G1-a `AND` G1-c**。量出來的必然後果:
`T*_ind`(2.690)與 `T*`(2.686)幾乎相同、`MDI_ind`(+0.00255)甚至**略小**,
但 V0 的產業內中性化 IC 只有 **0.0020** → 同樣的絕對改善換算成相對幅度是
**+128% vs +30%**。→ **實際卡住 arm 的會是 G1-c**,arm 必須讓產業內排序力**翻倍以上**。

這不是門檻設得太嚴,而是反映 §5-2b 的事實:**V0 的排序力在產業內中性化後幾乎歸零**。
從幾乎零的基準做出「統計上可偵測」的改善,相對幅度必然很大。**已事前揭露,不得事後放寬。**

### D. 回歸測試已加(第九輪 §3)

[tests/test_gate1_delta_ic.py](tests/test_gate1_delta_ic.py) —— **11 項 / 0.7 秒**,
用自造的 12 月 × 50 檔小面板,**不依賴研究面板存在**。涵蓋 Codex 指定的四項:
退化守衛(t=0 / `T*`=0)、固定 seed 可重現、**同一置換索引確實同時套用全 12 arm**、
退化不噴 RuntimeWarning;另附缺值規則與 G1-a/G1-c 的 `M*` 一致性。

**依 Codex 指示,T2 的 FWER 區間與 T3 檢定力刻意不進單元測試**(小樣本拒絕率本身有
抽樣誤差,當固定數字斷言會變成脆弱測試),留在慢的 synthetic validation。

**另做 mutation 檢查**,確認測試真的抓得到迴歸而不是形式通過:

| 注入的錯誤 | 結果 |
|---|---|
| 關掉 `EPS_SD` 守衛(改成 `sd < 0.0`) | **4 項失敗** |
| 改成每個 arm 各自抽一組置換(破壞 joint 結構) | `test_identical_arms_get_identical_null_t_per_permutation` **失敗** |

兩個 mutation 還原後 11 項全過。

### E. 測試與狀態

focused:`tests/test_gate1_delta_ic.py` **11 passed**;
研究 lifeline **25 passed**;完整 `tests/` **183 passed / 0 failed**(172 → +11)。
V0 面板 md5 仍是 `6cf06bde…`。沒改任何五維公式 / 權重 / c2 / 缺值填法;
沒建新面板;沒跑任何 candidate OOS 或 H1–H5。

**一個新待決(§9-A)**:AND 之下 G1-c 需要 +128% 相對改善 —— 這是否符合意圖?
若是則現狀不動並事前接受「多數 arm 止於 G1-c」;若 G1-c 應該是**否決條款而非等權主判定**
(例如改成「`ΔIC_ind` 點估計不得為負」),那需要新裁決 —— **我不自行放寬已凍結的門檻。**

**停止等待 Codex 第十輪審查。** 未整批 commit。

---

## 26. 🔒 2026-08-01 — Codex 第十輪:Gate 1 統計實作凍結 + 一致性掃描,並發現一個結構性衝突

**仍未執行任何 candidate OOS。V0 面板 md5 未變。**

### A. 措辭修正(第十輪 §2)—— `+128%` 不是門檻

Codex 指正:`+128%` 只是因為 V0 的中性化 IC 基準接近零而產生的相對值,**不是實際門檻**。
真正的量級是**絕對 MDI**:raw **+0.00273 IC** / industry-neutral **+0.00255 IC**
—— 兩者幾乎相同,所以就絕對可偵測量而言 **G1-c 並沒有比 G1-a 更嚴**(`MDI_ind` 甚至略小)。

已刪掉「候選必須讓中性化排序力翻倍」這種寫法。`+30%` / `+128%` 全部改標為
**「僅供基準解讀,不是判定條件」**。AND 的正式條件仍然只有一句:
**兩個 `t(ΔIC)` 各自超過自己的 joint max-t 門檻。**
上一輪的待決 1(「AND 是否過嚴」)因此**結案** —— 那個問題建立在錯的前提上。

### B. Gate 1 統計實作已凍結

範圍:`delta_ic_t()` / `joint_maxt_null()` / `build_month_blocks()`(含 `neutral_by`)/
`assert_same_months()`,以及 `MIN_N`=30 / `N_PERM`=2000 / `SEED`=20260731 /
`ALPHA`=0.05 單尾 / `EPS_SD`=1e-12 / 單步 max-T / AND。
凍結後改動必須先過 `tests/test_gate1_delta_ic.py`(11 項),參數變更要新預註冊。
**不含 `T*` 的數值** —— 它由演算法在 12 個真 arm 上產生。

### C. 一致性掃描完成(工作計畫寫進預註冊 §10-A)

**可凍結的 14 項全部核對過**:V0 兩條 baseline(80 月)、Gate 3 三段、時間切分月數會計
(255−168−7=80)、A1/A2 隔離與邊界污染窗、A3/A4 規格、C1/C3 規格、Gate 1–4、
`N_total`=12、出口編號。

順手清掉三處不一致:**E3 併入 E10**(第七輪後 Gate 1 才是主判定,E3 是同一情境的舊寫法)、
**E10 補上 AND 語意**、第二批的「基準值尚未計算」註解改指向新的 §9-B。

**另外確認一件先前掛著的事**:完整診斷面板其實**已經跑完並通過驗收** ——
`diag_scores_adv100w_diag.parquet`,**263,928 / 263,928 列、`v0_match_rate 1.0`、
`max|Δ| 0.0`、五面逐列相同、`score_errors 0`、耗時 4,772 秒**。
這補上了先前只有 2.79% 抽樣對帳的缺口(§16 的措辭限制可據此升級,但**判定不變**)。

### D. ⚠ 發現的結構性衝突:`V0-C3` ≡ 第一批的 `C3` arm

Codex 第十輪要求「填妥第二批 `V0-C3` 的 Gate 2 baseline」。**這在不執行 C3 的前提下做不到**:

| | 定義 |
|---|---|
| 第一批 `C3` arm | V0 + 五個 bucket 層內百分位化後再加權 |
| 第二批 `V0-C3` baseline | V0 + 第一批 C3 的層內百分位量尺 |

**同一個 composite。** 算出 V0-C3 在 80 月的 CAGR/Sharpe/MDD **就是**算出 C3 的 Gate 2 數字。

而且這不是資料沒建好:診斷面板已存 `b_*` / `regime` / `dyn_weight`,
**C3 與 V0-C3 都只是純算術後處理,技術上隨時可算 —— 正因如此才必須先問。**

**建議 R1**(未自行採用):第二批 Gate 2-B 的 MDD 護欄改用 **V0 的 −22.01%**。
理由:第二批 Gate 2 在第七/八輪之後只剩 MDD 一個通過條件;MDD 是風險底線,
本來就該是可部署策略的絕對性質,不該相對於中間構造物;用 V0 的值可讓兩批
在同一條風險底線上直接可比;且第二批今天即可完全凍結。
V0-C3 的數字仍要報,但只作**描述性 CI 的比較對象**,在 C3 結果解封時填入。

**替代 R2**:維持相對 V0-C3,但第二批的凍結時點延後到第一批執行完成之後
(「無條件執行」的承諾改寫成三步:完成第一批 → 填 baseline → 凍結 → 執行)。

**兩者都會動到已凍結的條款,所以我不自行選。**

### E. 狀態

`tests/` **183 passed / 0 failed**;V0 面板 md5 仍是 `6cf06bde…`。
沒改任何五維公式 / 權重 / c2 / 缺值填法;沒建新面板;沒跑任何 candidate OOS 或 H1–H5。

**擋住 candidate OOS 的只剩一項**:§9-B 的 R1/R2 裁決。
其餘 14 項一致性檢查已全部通過(§10-A)。

**停止等待 Codex 第十一輪審查。** 未整批 commit。

---

## 27. ✅ 2026-08-01 — Codex 第十一輪採 R1:第二批的最後一個未填項解決,舊條款已清除

**仍未執行任何 candidate OOS。V0 面板 md5 未變。**

### A. R1 的四條已全部落實

Codex 第十一輪確認一致性掃描的結論(`V0-C3` ≡ 第一批的 `C3`,不能在第一批 OOS 前
填入它的 CAGR/Sharpe/MDD),並**裁決採 R1、不採 R2**:

| # | R1 | 落實位置 |
|---|---|---|
| 1 | 第二批維持現在凍結、**無條件執行**(不延後凍結時點) | 第二批 §9(執行前提只剩「第一批完成」一項) |
| 2 | Gate 2-B 的 MDD 護欄改用已凍結的 V0 門檻 **`MDD ≥ −22.01%`** | 第二批 §7 Gate 2-B |
| 3 | `V0-C3` 的 CAGR/Sharpe/MDD **仍必須報告**,但 **C3 執行後才填、僅作描述性比較** | 第二批 §7 R1 說明 + §10 |
| 4 | `V0-C3` 仍是第二批 Gate 1 的**固定 score scaffold**;不得提前算它的 OOS 績效、不得用來挑 baseline | 第二批 §0-1 + §7 |

**R1 之下必須守住的區分(寫進兩份文件)**:
> **用 `V0-C3` 的分數 ✅(Gate 1 的 ΔIC 配對基準)/ 算 `V0-C3` 的績效 ❌(直到 C3 執行)。**

### B. 舊條款已清除(Codex 明示「避免 R1 與舊條款並存」)

逐處改寫:第二批 §7 的 **Gate 2-B 表格**(`V0-C3 − 5pp` → `≥ −22.01%`)、
**Gate 2-A**(對 V0-C3 的 CI 只能在 C3 執行後算,在那之前只報對 V0 的 CI)、
**Gate 2-B'**(明寫這一組在第二批執行時就可以算)、**E13**(限定只涉及 Gate 1,
並註明 Gate 2-B 的 MDD 是 V0 的絕對門檻、不存在「兩個尺度」問題)、
**§9 執行前提第 2 條**(刪除「算出並填入 V0-C3 基準」這個前提)、
**§0-1 的報告義務**(加上 R1 的時序限制)、**§7 標題**與**文件頭版本說明**。

實測 `grep`:`MDD ≥ V0-C3 − 5pp` 這個活躍條款**已不存在於任何一處**
(剩下的命中全是「**不是** V0-C3 − 5pp」「**已全部刪除**」這類否定敘述)。

### C. 預註冊 §10-A 的擋點欄位:**已清空**

前一版寫「仍未解決、擋住 candidate OOS 的部分:1 項」。現在是 **無**:

| 項 | 狀態 |
|---|---|
| 第二批 Gate 2 MDD 護欄 | ✅ `≥ −22.01%`(沿用 V0,已凍結) |
| 第二批其餘通過條件 | ✅ 無未填項 |
| R1 與舊條款並存 | ✅ 已清除 |

### D. 狀態

`tests/` **183 passed / 0 failed**;V0 面板 md5 仍是 `6cf06bde…`。
沒改任何五維公式 / 權重 / c2 / 缺值填法;沒建新面板;**沒跑任何 candidate OOS 或 H1–H5**。
Gate 1 的統計實作已於本日凍結(第一批 §5-2);第一批與第二批的門檻**全部無未填項**。

**兩份預註冊草案現在都處於「可進入最終凍結」的狀態,等使用者明確指示凍結。**
依研究紀律 §2,凍結之後不得改門檻、不得換母體、不得換報酬線、不得事後追加 arm。

---

## 28. 🔒🔒 2026-08-01 — **FaceRedesignV2 兩份預註冊正式凍結**(使用者授權)

**凍結時已執行的 candidate OOS:0 個。凍結本身不啟動任何 arm。**

### 凍結記錄

| 項 | 值 |
|---|---|
| 凍結日期 | **2026-08-01** |
| 凍結授權 | 使用者明確指示(依研究紀律 §2;Codex 第十一輪確認「唯一尚未做的是正式凍結」) |
| **凍結基準 commit** | **`4164993`**「研究基礎設施:import 副作用修復、五維度血緣稽核、Gate 1 判定演算法」 |
| 凍結的文件 | `docs/預註冊_FaceRedesignV2_草案.md`(第一批 A1–A4 + C1 + C3)<br>`docs/預註冊_FaceRedesignV2_第二批_B層草案.md`(第二批 B1–B5 + C2) |
| V0 面板 | `realbody_scores_adv100w.parquet` md5 **`6cf06bdee83228eba45ce05fc9462b0b`** |
| 診斷面板 | `diag_scores_adv100w_diag.parquet` 263,928 列 / `v0_match_rate 1.0` / `max\|Δ\| 0.0` / `score_errors 0` |
| `weights_version` | `aa082fa054` |
| 測試 | `tests/` **183 passed / 0 failed**;研究 lifeline 25 passed |
| `N_total` | **12**(第一批 6 + 第二批 6)= Gate 4 的主校正 |
| Codex 對抗式審查 | **11 輪** |

### 被凍結的內容(摘要)

| 層 | 內容 |
|---|---|
| **時鐘** | Train 2005-01~2014-12(120)/ Validation 2015-01~2018-12(48)/ **空窗 2019-01~07(7,不屬任何段)**/ **OOS 2019-08~2026-03(80,判定的唯一依據)**;走查在 248 月序列上運行 |
| **V0 baseline** | `∩c2` **28.00 / 1.34 / −17.01**;`composite alone` **24.61 / 1.09 / −22.51**(皆 80 月) |
| **Gate 1(主判定)** | 對 V0 的 paired **ΔIC** + **12-arm joint max-t**;**G1-a `AND` G1-c**;`MIN_N`=30 / `N_PERM`=2000 / `SEED`=20260731 / 單尾 α=0.05 / `EPS_SD`=1e-12 / 單步 max-T;G1-b 純描述 |
| **Gate 2** | CAGR / Sharpe **只報 CI**(paired block bootstrap,`L`=12 / `B`=10000 / seed 20260731);**唯一通過條件 `MDD ≥ −22.01%`**(兩批共用) |
| **Gate 3** | OOS 內三段 **29 / 12 / 39** 月,**≥2/3 且三段全報**;滑價 0.60%;兩條獨立路徑逐月對帳 |
| **Gate 4** | DSR 主校正 **N=12**(N=6 僅 interim);置換掃描對 12-arm 空間;虛無對照 ≥500 次 |
| **出口** | E1–E10(第一批)、E12–E15(第二批);E3 已併入 E10、E8 已刪 |

### 凍結後的硬規則

**不得**改門檻、換母體、換報酬線、事後追加 arm、用 OOS 結果反推該保留哪些 arm、
在看到 A2 結果後刪除 A1、改 §5-2 的任何統計參數
(改動須先過 `tests/test_gate1_delta_ic.py` 且另立新預註冊)。
第二批**不得**因第一批的結果而改 baseline 或不執行。

### 下一步

**執行時機另候指示。** 凍結不啟動 arm。第一批 6 個 arm 各需一份獨立面板
(A1/A2 需重建 0050 拼接後的面板),粗估 1–2 天機器時間。
執行順序見第一批 §8:建 runner → 各建面板 → Train+Val 定義層診斷(in-sample,不是結論)
→ Validation 初選 → 走查協定 → **OOS 只跑一次** → 結果不論正負寫進 DevLog 與 §10。

---

## 29. 🔒 2026-08-02 — **Gate 1 執行完畢(只有 C3 通過)+ §8 流程偏差正式裁定**

本節嚴格區分四件事:**(A) Gate 1 排序層結果**、**(B) 流程偏差**、
**(C) 未執行的績效 / Gate 2**、**(D) 未來另案前瞻驗證**。四者不得互相推論。

### (A) Gate 1 排序層結果 —— 已執行

12 個 arm 面板於 2026-08-01~02 建置完成,全部通過 builder 驗收 + 獨立完整性稽核
(263,928 列 / 覆蓋率 100% / 重複鍵 0 / score_error 0 / 每個 arm 只動一個面)。
provenance 以 `MANIFEST.json` + Gate 1 overlay 兩層固定,`--check` 53 項逐檔重算相符。

**per-arm baseline 勘誤**:第一批 §5-2 凍結的是全族單一 baseline(V0),
第二批 §0-1 卻要求 B/C2 對 `V0-C3`,而 §7 要求 12 arms 同族算 `T*` ——
三條凍結條款無法並存。Codex 2026-08-02 裁決採 per-arm baseline,
勘誤見 `docs/勘誤_Gate1_PerArmBaseline.md`;凍結函式最小擴充
(`baseline_idx` 選用參數,省略時與勘誤前逐位元相同),既有 11 項測試一字未改照樣全過。

**執行**(`GATE1_EXECUTION_MANIFEST.json`,`sha256=4ed3b2c9…`,
`N_PERM=2000` / `seed=20260731` / `M*=80`):

| 版本 | `T*`(95% joint max-t 臨界值) | 通過 arm |
|---|---:|---|
| G1-a(raw) | **2.455044** | **C3**(觀測 t = 3.737509) |
| G1-c(產業內中性化) | **2.419922** | **C3**(觀測 t = 5.650046) |

**AND 判定:12 個 arm 中只有 C3 通過。** 逐 arm 數字見預註冊 §10-1。

兩點必須隨結果一起讀:

- **A1/A2 的 t 恰為 0 是設計使然**(只補 2005–2018,主時鐘 2019-08 起與 V0 相同),
  結論寫作「**無法在此窗檢定**」,不是「檢定後未通過」。
  它們仍在每次置換的 12-arm `max(t)` 內,**族系維持預註冊的 12 arms**;
  `ρ̂` 估計時排除恆定欄只影響相關係數,不影響 max 運算。
- **B1–B5/C2 的凍結措辭**:「在此 80 月窗、各自凍結 baseline 下的觀測配對 ΔIC t 為負,
  且未通過預註冊的優越性 Gate。」Gate 1 是**單尾優越性檢定**,**不是劣勢檢定** ——
  不得寫「顯著較差」或「有害」。

**attempt 1 失敗並已封存**:首次執行(18:24 之前)因 `main()` 在 `preflight_passed`
被設定之前讀取它而 `KeyError` 中止於步驟 5。**統計未擊發**(`run_gate1` 未被呼叫、
無 EXECUTION_MANIFEST),依單發射擊制保留 STARTED、另寫 FAILURE、封存於
`beat_0050/results/gate1/archive/attempt1_preflight_keyerror/`(含 ARCHIVE_MANIFEST)。
根因已修(判定改由 `preflight()` 自身產出),並補上「合成 rep 不得比真實回傳更完整」
的契約測試 —— 那個假物件正是遮蔽缺陷的原因。

### (B) 流程偏差 —— §8 執行順序

§8 的**第 4 步**(Train+Validation 定義層診斷)與**第 5 步**(Validation 初選 → 走查協定)
**無可稽核產物**,而**第 6 步**(OOS 只跑一次)已執行。
`beat_0050/results/diag_*.log` 是診斷面板建置器的 log,不是定義層診斷;
`scripts/` 與 `beat_0050/` 下不存在任何走查協定實作。

**正式裁定**:`docs/裁定_FaceRedesignV2_S8偏差.md`(2026-08-02,使用者裁示 + Codex 逐輪審查)。
事實記載:`docs/協定偏差_FaceRedesignV2_S8.md`。

- **本案不落入 §6 所列 E1–E10 的任一完整結果狀態**,以另案裁定處置;
  **不新增 E11**、**不宣稱適用 E2**(E2 前件為「走查失敗」,本案為「走查未執行」)。
- 處置:**(a) 現案降級宣稱並封存 + (c) 未來另立 C3 前瞻預註冊**;第 4/5 步**不補跑**。
- **走查協定的盲測性不可事後修復** —— Gate 1 已公開全窗答案,
  此後任何走查實作都在知道答案的狀態下進行。
- `ordering_violation=true` 是**永久歷史事實**,裁定不將其翻轉,
  不新增例外放行欄位,不修改 runner。

**最遠可宣稱邊界**:「在 2019-08~2026-03 的 80 月主時鐘、`exec_ret.fwd_x` 報酬線、
`real_composite` 分數下,12 個預註冊 arm 中只有 C3 同時滿足
`t(ΔIC) > T*` 與 `t(ΔIC_ind) > T*_ind`。」C3 通過的是**排序層**的配對 ΔIC 檢定。

**禁止宣稱**:走完 E1 路徑、C3 經盲走查選中、C3 進入實盤候選、
C3 可採用或納入生產計分、C3 績效優於 V0、B/C2「顯著較差／有害」、
§8 已依序完成、Gate 2 已通過。

### (C) 績效 / Gate 2 —— **未執行**

**本階段未執行任何 CAGR、Sharpe、MDD、累積報酬、bootstrap、投組建構或 Gate 1 以外的
candidate OOS 分析。** 亦未觸及第二批 R1 禁止的「算 V0-C3 績效」。

`scripts/gate2_c3_runner.py` 已建立但**執行路徑 fail-closed**;
`--preflight-only` 因 §8 違序判 `preflight_passed=false`(exit 3)。
preflight 綁定 C3 / V0 / Gate 1 execution manifest / provenance overlay 四個 hash,
並以 `load_real_panel(drop_na_ret=True, min_coverage=1.0)` 建立唯一合法報酬母體:
canonical 96,956 鍵 / 80 月,C3 覆蓋 100%,另有 999 列有分數但無可執行 `fwd_x`,
**明記為不具報酬資格而排除**(C3 原始面板時鐘內的 97,955 列**不是**可用樣本)。

**原 Gate 2 在任何情況下都不得產生 `passed` 狀態。** 若日後另有明示授權要做風險側檢查,
只能另立 `post_deviation` 描述性規格,不得冒充原 Gate 2。

### (D) 未來另案前瞻驗證

C3 的前瞻驗證另立新預註冊,對**未來**資料執行。約束條件見裁定文件 §4,重點:

- 報酬線精確定義為 `open(本月訊號日後第一個交易日) → open(下月訊號日後第一個交易日)`,
  **不是固定 20 交易日**(固定 T+20 是本專案已禁止的漏日報酬線);
- 前瞻硬條件:`formation_as_of`、`entry_at`、`exit_at` 三者皆須 > `frozen_at`,
  `exit_at` 依交易日曆推導;
- 凍結時同時記錄 `frozen_at` 與由其推導的精確 `CLOCK_LO`,runner 須驗證兩者一致,
  **`CLOCK_LO` 不得隨執行日期往後滑**;
- 2026-04/05 的分數已在既有面板內、2026-06/07 雖未建入面板但**已經發生** ——
  兩者皆不得作前瞻起點;面板 sha256 只能證明「凍結當下不在這些面板」,
  **不能單獨證明從未被觀察**;
- V0 的**實現數字不可事前凍結**(未來報酬尚未實現);事前凍結的是定義、母體、時計、
  成本、公式、門檻、開封時間與單發程序,V0 與 C3 的實現數字只能在完整前瞻窗結束後
  由**同一次**已授權執行共同計算;
- **C3 在該前瞻驗證完成之前不得進入生產計分碼。**

---

*本日誌記錄從「防守型 3 級反向指標」到「四級雙軌 + 市場中性驗證通過」的完整重構歷程。*
*核心成果：三大痛點（恐高／逆勢／死抱）已解，選股排序力經樣本外驗證，並定案風險調整後勝過大盤的可實作配置。*
