# L3 v1.38 · A-3 衍生 · valuation store 的定義域 · 選項書（2026-08-31）

**狀態：`OPTIONS ONLY — NOT ADJUDICATED`**

本文件之外未變更任何 code、master prereg、data。所有選項保留。**「建議」是建議，不是裁決。**

九項選項書的 A-3 建議「甲 + 丙，**並把『月底制是否規範』列為獨立小裁決**」，
外部覆核同意並重述「另立『月底制是否規範』小裁決」。兩方都說要立，本文件即是。
**本項不屬於九項序列，也不是第十項**；它是 A-3 的衍生子裁決，A-3 落地前必須先答。

## 0 · 撰稿者揭露

未觀測任何 L3 決策、名單、NAV、報酬或基準比較。判準為條款層與涵蓋分佈層。

## 1 · 事實（2026-08-31 實測）

**⚠ 先更正 A-3 的一項事實描述。** 九項選項書 A-3 的〈事實〉寫：
「該 store 在 `artifacts/valuation_lineage_audit` 下**只有月底**，止於 2026-07-31」。
**「只有月底」不精確。** 實測：

```
twse session 共 180 個，2016-01-30 .. 2026-07-31，橫跨 127 個月
53 個月有 2 個以上 session，74 個月只有 1 個

2026-07  ['2026-07-30', '2026-07-31']      2026-05  ['2026-05-29']
2026-06  ['2026-06-29', '2026-06-30']      2026-02  ['2026-02-26']
2026-04  ['2026-04-29', '2026-04-30']      2026-01  ['2026-01-30']
2025-09  ['2025-09-26', '2025-09-30']      2025-11  ['2025-11-28']
```

`2026-06-29` 不是月底；`2025-09-26` 也不是、且不是 `2025-09-30` 的前一個 session。
止於 2026-07-31 是對的；`twse_2026-08-28.json` / `tpex_2026-08-28.json` 未 harvest 也是對的。

**⚠ 更正（同日，本文件初版）。** 初版在此寫「它沒有可陳述的規則」。**那句是錯的**，
在追查 harvester 時發現並在此更正 —— store 不是沒有規則，是**同時materialise了兩條規則**：

```
harvest_official_pbr.decision_sessions()    月末當天或之前最後一個 session（涵蓋稽核用）  123
harvest_official_pbr.route_as_of_sessions() 月末之前最嚴格的前一個 session（§6.6，路線用）  87
                                                                              聯集  173
實際 store                                                                          180
聯集 \ store = 0      store \ 聯集 = 7（全部在 window_end 2026-03-31 之後）
```

`route_as_of_sessions` 的 docstring 自陳兩者「同一日期當月末不是 session、差一個 session 當它是
—— 141 個決策月裡有 85 個」。**store 完整包含兩個規則集**，另加 7 個凍結窗之後、
沿同樣兩兩成對模式手工補的 session。所以本項的問題不是「定義域不存在」，
而是**定義域被定義了兩次，因為有兩個不同的問題在問它**。

**L3 實際讀哪一天：** §6 的 stop rule 把 `as_of` 綁在**決策日之前最後一個完成的 session**
（`research/b0_l3/l3_snapshot.py:176-178`，且 `run_l3_prospective.py:1163` 註明
「§6.6 can move as_of back across a month boundary」）。
⇒ 決策日若是月底，**L3 要的是月底的前一個 session，不是月底本身**。
Month 1（decision 2026-09-30）要的是 **2026-09-29**。

**成本：** harvester 直接向 TWSE/TPEx 取，任一 session 皆可回抓、不會過期（C-48/C-49 禁止 TEJ 替代）。

## 2 · 選項

**甲 · 裁定定義域為「每個決策期的 `as_of` session」。**
即決策日前最後一個完成的 session —— L3 真正讀的那一個。
- 後果：定義域與讀取點有因果關係，不是慣例。每期只需一個 session。
- 注意：這在九項選項書裡被寫成 A-3-乙，並以「改變 store 的定義域，屬於來源語義變更」為由未採。
  **§1 的量測改變了那個理由**：沒有既存定義域可被「改變」，本項是第一次定義它。

**乙 · 裁定定義域為「每月月底 session」，並要求 L3 的 `as_of` 對齊月底。**
- 後果：store 的形狀與名目一致。
- 代價：**要改的是 §6 的 stop rule，不是 store。** 那動的是決策時點語義
  （`as_of` 從「決策日前最後一個 session」變成「決策日當天」），影響每一個 PIT 邊界。
  **成本與風險遠高於本項的標的，應標示為「這不是資料裁決，是決策語義裁決」。**

**丙 · 不定義域，改立即時規則：每次執行前檢查該期 `as_of` 是否在 store 內，不在就 harvest。**
- 後果：不必回填歷史，Month 1 也能過。
- 代價：「store 裡有什麼」永遠不是可預測的事實，只能靠執行時查詢；
  且它把一個資料完整性問題轉成每期的操作步驟，而操作步驟會被漏做。

**丁 · 維持不裁決。**
- **應標示為實質選項而非拖延**：下一個 decision session 會再撞一次同一件事，
  且每撞一次都要臨時判斷該不該補、補哪一天。

## 3 · 建議

**甲。** 理由經 §1 的更正後改變，但結論不變：兩條規則裡只有 `route_as_of_sessions`
對應 L3 實際讀取的 session（§6.6），另一條服務的是涵蓋稽核，是另一個問題。
所以本項要裁的是**哪一條是規範**，而不是「發明一個定義域」，也不是「變更來源語義」——
九項選項書 A-3 拒絕此選項時用的正是後面那個理由，該理由在量測後不成立。
採甲之後必須一併說明：稽核那條規則產出的 123 個 session 是什麼地位
（`route_as_of_sessions` 的 docstring 已警告「把已封存面板建在稽核集上，
會把凍結輸入綁到一個路線從不讀取的 session」）。
乙 應明文記為「它要改的是 §6 而不是 store」，以免日後被當成同級選項。
丁 應明文記為會週期性復發。

## 4 · 本文件未涵蓋

- **回填範圍**：採甲之後，是把 2016 以來每期的 `as_of` 都補齊，
  還是只從 L3 前瞻線的第一期開始？兩者對既有 180 個 session 的處置不同
  （前者要補很多，後者要說明既有 session 的地位是什麼）。這題採甲則必答。
- Month 1 的 `as_of` 是 **2026-09-29，尚未發生**，今天無法 harvest。
  今天能做的只有補 `2026-08-28`（A-3-甲 的觸發點）與 `2026-08-31`（A-3-丙 的月末連續）。
- 既有 53 個「一個月有兩個 session」的月份，第二個 session 是誰要的、為何存在，本文件未追查。

## 5 · A-3 的 harvest 現在跑不了（2026-08-31 實測）

先前把「harvest 2026-08-28」歸類為「不需裁決、現在就能做」。**那個歸類是錯的，在此更正。**
`research/b0_valuation_lineage_audit/harvest_official_pbr.py` 有兩道邊界，兩道都來自凍結物：

```
_sessions()   讀 data/b0/trading_calendar.csv        止於 2026-08-17
WINDOW_TO     = frozen_spec("window_end")[:7]        = 2026-03   （window_end 2026-03-31）
```

2026-08-28 **既不在那份日曆裡**（雖然它確實是交易日 —— `股價0817-0828.zip` 有它的價格列），
**也在凍結窗之後**。兩個 session 列舉函式都由這兩道邊界導出，所以 harvester 照現狀不會提供它。
`data/b0/` 由 R-W1-1 凍結，日曆不能就地延長；繞過 session 導出去硬塞一個日期，
正是該檔案自己警告的 C-55 形狀（「寫成字面值會產生第二個真相來源，它一直吻合，直到不吻合的那天」）。

⇒ **A-3 的補資料與 Month 1 的價格阻塞卡在同一道邊界上**，不是兩個獨立問題。
這也與九項選項書 §0.1 的根因一致：整套時間裁決建在日曆上。

**未解的觀察**：store 裡那 7 個 `window_end` 之後的 session（2026-04-29 … 2026-07-31）
是怎麼取得的？script 的環境變數沒有可以移動窗口的旋鈕。本文件不推測，
但**採任何選項之前應先問清楚**，因為那正是「下一期要怎麼補」的既有先例。

**端點本身沒有問題**：TWSE `BWIBBU_d?date=` 與 TPEx `peQryDate?date=` 都吃日期參數，
A-3 寫的「任一 session 皆可回抓、不會過期」對這個 harvester 成立
（`scripts/market_snapshot_collector.py` docstring 講的「端點不支援歷史日期」是另一組 openapi 端點，
兩者不衝突）。**擋住的是 session 列舉，不是取數能力。**
