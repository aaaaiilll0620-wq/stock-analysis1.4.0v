# L3 前瞻線 v1.38 · 工作單與覆核紀錄

**狀態：`REVIEW COMPLETE — ALL CODE FINDINGS CLOSED — NOT RATIFIED, NOT SEALED`**
**最後更新：2026-08-31**

Codex 交接點。記錄誰做了什麼、哪些關閉、哪些開著、每項的檔案與行號。
**沒有任何版本升級、freeze 變更、lineage capture、route seal、decision observation 或績效量發生。**

分支 `codex/l3-september-readiness`，基底 `216d62db`（Frozen B0 Master v1.37）。

```
0ff279e6  R1  三個致命缺陷 + adapter 層的第二份認證
9c63a5ba      本文件
3b6e8bd4  R2  兩份新匯出的宣告、四道閘門、CA 洞、順序測試
12cb39b2      本文件更新
e13c512e  R3a S-1 S-2 S-4 S-5 S-6 A-4 S-7
5dbe47f2  R3b S-8 S-9
```

---

## 0 · 角色、覆核狀態、以及一次性對調

`AGENTS.md` 定 Claude = 執行官、Codex = 審查官。**這三輪反過來**：decision-intent/execution
拆分由 Codex 撰寫，覆核與修正由 Claude 執行。理由是作者不能覆核自己，且 Codex 當時無額度。
一次性對調，不改變常態分工。

C-72 前例仍然有效：**覆核未過就落地會被判 `LANDED_BEFORE_REQUIRED_REVIEW + REVISION_REQUIRED`
並整包 revert。** 本文件不構成覆核通過。**v1.38 的版本升級、freeze、seal 全部仍待覆核。**

---

## 1 · 使用者裁定（已生效）

| # | 裁定 | 影響 |
|---|---|---|
| U-1 | genesis cohort = `L3_PRIMARY_20M`（2,000萬，主）+ `L3_SECONDARY_50M`（5,000萬，輔） | NAV 餵 `adv_floor(port_value)`，兩格選出不同名單 |
| U-2 | Month 1 **順延到 2026-09**（decision 2026-09-30、execution 2026-10-01） | 依裁決草案 §2「miss the date → move forward」，不得事後補記 |
| U-3 | 9 月實際交易改用 L4a dual100 | 與 B0/L3 是兩條線，不得混用 |
| U-4 | 兩份新 TEJ 匯出「檢查過就接起來」 | 已執行 |
| U-5 | 未來以自動抓取為主、手動為輔 | ⚠ 與 R-W1-2 相反，**未執行**，見 §6 |

---

## 2 · 已關閉（12 項）

每項都先重現才修。所有新測試都做過突變檢驗。

### R1（`0ff279e6`）· 三個致命

- **B1** Phase A→B 契約綁了相位相依的 `market_state_sha256`（intent `5b670034…` vs exec
  `9e5a5d48…`）⇒ Phase B 結構上不可達。改綁 `decision_cutoff_state_sha256`（早已算好也 receipt，
  只是沒接上）。`market_state_sha256` 保留為 recorded-but-not-compared。
  COMPARED（17）/ RECORDED 為具名集合，`_assert_contract_field_sets()` **雙向**擋。
- **B2** `as_of` 可無聲落後（實測 17 個日曆日）。改為宣告日曆必須涵蓋到 `decision_date`。
  **剛好結束在 decision_date 仍接受** —— 執行 session 可以還不存在。
  同一裁剪在 adapter 層被第二次認證，已改寫；全樹確認無第三份。
- **B3** leaf 把機器絕對路徑烙進 hash 綁定欄位（5 個 family 中 4 個；calendar 倖免 ⇒ 部分壞掉更糟）。
  已還原 repo-relative，逐位元重現 base `216d62db` 的 payload hash。

### R2（`3b6e8bd4`）· 資料宣告與四道閘門

- **prices 宣告** `股價0817-0828.zip` 實際 **2026-08-18..08-28**（檔名錯）。記為
  `ROSTER_SNAPSHOT_DERIVED`，**不得引用來支持 `includes_delisted`**。實測：C1/C2 兩個 D-1
  閘門的輸入皆不變 ⇒ 這片切片結構上不可能移動任一閘門，而**不能移動閘門的切片不得被讀成通過了它**。
- **revenue 宣告** 完整 202607（2,002 檔）vs workbook 部分 406 檔且**重疊處不一致**
  （3003：658,000 → 657,875 千元）。以 `owns`/`yields` 解決，非 concat 順序。
  `build_monthly_revenue_pit` 不再 glob `*.xlsx`（舊 glob 會**靜默漏掉整個七月**）。
- **l3_readers 依宣告 format 分派**。`read_revenue` 原本對每個 entry 呼叫 `pd.read_excel`，
  會讓**前瞻路徑**崩潰（封存面板路徑把破綻裁掉看不見）。未註冊 format 具名 abort。
  實測驗證：202607 共 2,002 檔、`3003 = 657,875,000`（archive 修訂值）。
- **route_closure** owed 清單曾被縮成字面 `[]` 且誠實性測試被反轉 ⇒ `assert_route_is_sealable()`
  不可能失敗。現在從 freeze 記錄與七個 route 元件（AST 檢查）推導，雙向誠實。
- **snapshot 閘門測試** 曾 monkeypatch 掉自己的三個依賴，閘門刪掉照樣綠。現在建真的 leaf。
- **capture** `finally: rmtree` 在失敗時也刪證據，牴觸 `FAILED_CAPTURE_PRESERVATION`。
  失敗保全到 `failed_capture/<run_id>`；landing 目錄的空轉字串守衛換成重算 hash 的對帳。
- **順序測試** 只讀 `run_decision`，而三個操作整批搬進 `build_decision_intent`
  ⇒ **順序還在，測試停止看守**。現在既驗新家順序，也驗 `run_decision` 繞不過去。
- **CA 洞** claim 在未觀測執行 session 到期 ⇒ 該期永遠發不出 intent。
  「as_of 當下尚未到期」≠「來源無法重建」，兩者不再共用路徑。claim 維持 `owned`、
  非 tradable/spendable，**無第四狀態**。`_first_session_on_or_after` 逐位元未動。

### R3a（`e13c512e`）

- **S-1 cohort 沒帶過 genesis。** `checkpoint_record` 無 cohort 欄位、CONTINUATION 回 `{}`
  ⇒ 第 2 期起 `genesis_cohort_id` 為 `""`，20M 與 50M 兩軌只靠現金大小區分，而 NAV 正是
  `adv_floor(port_value)` 的輸入。`cohort_id` 加為 checkpoint **列**欄位（狀態 hash 不動），
  組裝前驗證，兩條具名無預設規則。CONTINUATION 須帶 `--lineage-cohort`。
  **重用 `genesis_cohort_id`，比較集合維持 17。**
- **S-2 來源改寫停止規則是死碼**（`assert_append_only_continuity` 零呼叫點）。
  現在自 preflight 逐 family 呼叫，基準是**前一次 run 自己的 manifest，由
  `--prior-source-manifest` 明示指定**——不用自動發現，因為「最新的 run 目錄」是在猜 lineage，
  **而猜出來的基準可以被猜成通過**。run 不得自我基準。首跑須明示
  `--no-prior-source-manifest`，記進每份 receipt，**限 GENESIS**。模組本身未改，缺陷在接線。
- **S-4** synthetic parity cohort admits 任何 NAV（含註冊格的金額）⇒ fixture 會選到真實格的母體。
  現在拒 sealed evidence、拒等於任何註冊格現金的 `c_ref`。
- **S-5** 決策紀錄用 `"wb"` 覆寫。改為 `O_CREAT|O_EXCL` 宣告路徑後仍委派位元組給
  `write_provenance_json`（內容逐位元不變、`core/b0_master_prereg.py` 未動）。七個寫入點，
  AST 測試釘住不得有新點溜回裸寫。
- **S-6** `_git` 出錯回 `""`。改為 raise；並 shape-check 40-hex，因為
  `rev-parse` 退出碼 0 不等於它真的答了一個 commit。
- **A-4** `build_flat_leaves` 對每個 flat family 硬寫 `source_family: "TEJ"`。日曆的位元組是
  `~/market_cache/taiex_daily.parquet`（FinMind seed + TWSE 增量），卻被標成 TEJ 且
  AUTHORITATIVE ⇒ R-W1-2 從它本該可驗證的 artefact 上變成不可驗證。改為逐 family 宣告，
  沿用引擎既有詞彙（`SOURCE_FAMILIES` / `AUTHORITY_LEVELS`，已在拒絕 `LIVE+AUTHORITATIVE`），
  缺宣告即 abort。只有 calendar leaf 的 hash 動。**⚠ 它揭露的問題見 §4 N-1。**
- **S-7** `SNAPSHOT_CONTRACT_VERSION` → V2，但交付物是 `PINNED_RECEIPT_KEYS`：
  依版本字串釘住兩組 key，並用 AST 確認 `build_receipt` 只有一個 `**built` splat
  （第二個 splat 會讓欄位溜出釘子）。只升版號等於把問題往後推一版。

### R3b（`5dbe47f2`）

- **S-8 封存合約與宣告集合已分歧且無人察覺。** 合約寫兩個 archive、`date_max 2026-08-17`，
  builder 宣告三個、到 2026-08-28，而 `assert_reads_the_sealed_source` **照樣通過**
  （它從 `data/b0/price_2019plus_new.parquet` 重算指紋，從不打開 zip）。今天無害**只因為**
  `panel_span()` 裁在凍結的 `window_end` —— 而那正是未來改 `window_end` 會失效的假設。
  現在 leaf 與 panel 兩邊都無條件對帳。硬 abort、無 allowance 可用的情形：合約指名的 archive
  位元組不符、合約依賴的 archive 未宣告、未知或**已用掉**的 allowance。
  分歧須有明示 allowance，鍵為 **`(locator, contract.content_sha256)`** —— 不是合約**名稱**
  （`register_price_source.py` 把名稱寫死，重組會沿用）。所以授予一次 allowance 等於把
  封存的 64-hex 指紋貼在一個精確檔名旁邊；重新註冊來源會作廢所有 allowance。
  **allowance 的理由不是散文**：`condition` 取自單一成員的封閉詞彙，且**每次 build 都對
  當下使用的 panel end 重驗**：`panel_end < covers[0]` 且 `panel_end <= contract.date_max`。
  今天記錄 `2026-04-01 < 2026-08-18`、`<= 2026-08-17`。**`window_end` 一移過 2026-04 就 abort。**
  為了讓裁切不能與 allowance 漂移，`panel_span()` 的尾端移進 leaf 模組，
  **沒有在 capture code closure 外新增任何程式相依**。
- **S-9 宣告的 `covers` 從不重驗**（正確 hash + 錯誤 span 不會被抓；而本語料已經有一個檔名錯的
  archive）。現在每次 build 對每個 archive 的**每個成員**做完整串流 min/max，
  日期欄**依標頭名稱**定位而非位置；宣告須與觀測完全相等，觀測結果隨 entry 與 receipt 走。
  成本已量測：3.5M 列 / 560MB 解壓，冷 22.0s、同行程二次 1.60s，依 `raw_sha256` memo，
  且 `build()` 在查表**前**先重算 hash 並對宣告斷言 ⇒ 命中必是位元組已證相同；不落盤，無快取檔可手改。
  **明文寫在程式碼裡：這是 span 檢查不是完整性檢查。** span 內部缺 session、roster 缺口、
  錯的 `leg`／`roster_basis`、以及任何數值，全部抓不到。

---

## 3 · 仍然開著 · 程式碼

| # | 事項 | 位置 |
|---|---|---|
| S-10 | revenue receipt 的 `rows_raw` 語義改變（現為「通過所有權切分後」），旁邊新增 `rows_read_from_sources`。下游若比對舊的 478,127 會看到 479,723 | `build_monthly_revenue_pit.py` |
| S-11 | `test_b0_l3_route_seal.py:135` 在 owed 清單為空時自我 skip。目前正確，但「閘門空 → 測試不跑」的形狀要盯著 | 該測試 |
| N-2 | **leaf payload 現在還相依於兩個「資料」檔**：`data/b0/trading_calendar.csv`（經 `panel_end_session`）與 `research/d1_price_universe/price_source_contract.json`。兩者都不在 `FLOOR_CAPTURE_CODE_CLOSURE`（那是**程式**閉包）⇒ 跨 clone 比對 leaf hash 的人必須知道 leaf 已不再只是 archive 的函數 | `build_prices_leaf.py` |
| N-3 | `build_price_panel.main()` 本體**從未被執行**（它會寫 `data/b0/price_panel.parquet`）。兩道新閘門是以函式形式對真實目錄驗過的 | `build_price_panel.py` |
| N-4 | `--prior-source-manifest` 是操作者提供的。已拒絕自我基準並記錄 `baseline_run_id`，但單次 run 內無法分辨「錯得合理」的前身。綁到 `--producer-run-id` 可關閉，但那會把來源 lineage 與投組 lineage 耦合 —— **是裁決不是修正** | `run_l3_prospective.py` |
| N-5 | `build_period(cohort_id="")` + GENESIS 會跳過 checkpoint 的 cohort 檢查。`main()` 一律具名，故僅直接呼叫者可達 —— 而 `tests/test_b0_l3_runner_end_to_end.py` 正是這樣做 | 同上 |
| N-6 | `zip_leg` 仍讀 `namelist()[0]`（單一成員假設）。S-9 的 span 檢查會讀所有成員，故一個伸出宣告 span 的第二成員會被發現，但完全落在 span 內的不會 | `build_price_panel.py` |

## 4 · 仍然開著 · 待裁決

| # | 事項 |
|---|---|
| A-1 | **L3 route seal 契約未裁決。** `ROUTE_SEAL_CONTRACT_STATUS = NOT_YET_RATIFIED`。`core/b0_l3_lineage_capture.py:237-242` 是**無條件 raise**，常數只出現在錯誤訊息字串裡；docstring 自陳「ratifying the contract is a deliberate edit here」 |
| A-2 | **正式 capture 的發佈刻意未實作**，`prepare_floor_capture` 仍無 production 呼叫者 |
| A-3 | valuation 未 harvest：`twse_/tpex_2026-08-28.json` 缺，store 月底制、止於 2026-07-31。C-48/C-49 禁止 TEJ 替代（harvester 可回抓任一 session） |
| A-5 | 每期須記錄 `only_live` / `only_authoritative` 名單（每 session 約 30 檔差異） |
| A-6 | adv20 單位精度：TEJ 千股捨入 vs FinMind 實際股數，差 <1000 股，直接餵 §4.2 ADV floor |
| A-7 | live 家族以 `0.0` 為「當日未成交」哨兵值 ⇒ 持倉以 0 標價令 NAV 靜默歸零 |
| A-8 | **C-71 的「A02 須與 A01 比對來源 hash、變了就停」前提已不成立。** 至今三個合法變動：宣告第三個 archive、修正 24 個 workbook vintage、S-8/S-9 的新 policy 與 `covers_verified`；`floor_capture_code_closure_sha256` 亦因修 capture 與 `build_prices_leaf.py` 而變。須重新裁定，不是繞過 |
| A-9 | **非交易日 credit date 實測可達。** CA ledger 46,433 列中，日曆區間內 `credit_tradable_date` **23** 筆、`cash_payment_date` **1** 筆落在非交易日（多為颱風停市）。若落在 `as_of` 與執行 session 之間，Phase A 記原始日期、Phase B snap 到執行日 ⇒ 契約閘門拒絕。落在 decision_date 當天不會。二選一：兩相都記宣告日期，或明文裁定該期別不受理並靠 `deferred_release_points` 讓它可數 |
| **N-1** | **日曆沒有權威來源。** A-4 修好之後，calendar leaf 正確讀作 `LIVE / SUPPLEMENTARY` —— R-W1-2 把權威給 TEJ，而日曆的位元組不是 TEJ，所以**沒有 TEJ 腿可以對帳**。日曆決定「什麼時候」。**L3 route 可否在「決策時輸入的唯一家族是 SUPPLEMENTARY」下被 seal，無人裁過。** 舊的假標籤沒有解決這件事，只是把它藏起來 |

## 5 · 尚未證明

- **`decision_cutoff_state_sha256` 的相位不變性**是程式碼推理 + 合成資料量測。
  **route 被 seal 之前必須補一組真實 intent→execute 配對做決定性檢查。**
- 「2026-03 可執行路徑逐位元 parity」：**決策路徑**已由影子重跑獨立驗證；
  **run 寫出的 artefact 不 parity**（receipt 增欄）。
- **`L3_P1_DRY_RUN_20260830.md` 裡那三個 payload sha256 不是證據**（帶 tempdir 路徑的
  validation leaf，且 staging 樹已被當時的 `finally` rmtree 刪除）。

## 5.1 · ⚠ canonical 套件在這棵 worktree 不可能全綠

13 個測試需要這棵樹沒有的未受版控 fixture：`outputs/universe_pool/c2_fullpool_*.csv`、
`beat_0050/data/benchmark/0050_tr.parquet`、`artifacts/` 的大部分（只有 3 個子目錄，主 repo 有 12）。
分佈：`test_identity_collector_blockers_round2`(7)、`test_research_lifelines`(3)、
`test_b0_l2_run_layout`、`test_b0_opening_state`、`test_b0_reopening_after_invalid`。
`test_repo_tree_cleanliness` 只是斷言那 13 個要過，樹乾淨時它自己會通過。

⇒ **v1.38 最終落地與 `seal(final_seal=True)` 必須在帶那些 fixture 的樹進行**，
或先把它們佈進這棵 worktree。**在這裡追求「套件全綠」是不可達的目標，別為它改測試。**

---

## 6 · U-5 與現行裁決衝突，尚未執行

「未來以自動抓取為主、手動為輔」與 **R-W1-2 方向相反**，不得在實作層默默對調權威。
前置至少三項：**W1-b**（live 家族改逐日累積、永不覆寫並宣告 availability convention；
O-E 已裁「只知道最新狀態的來源 `NOT_PIT_SAFE`」，現況 `price_valuation_daily/` 只從
2026-07-14 起）、**A-6 / A-7**（單位精度、哨兵零），以及 **N-1**（若日曆要有權威腿，
這正是同一個問題的另一面）。在那之前權威腿維持 TEJ 手動匯出。

## 7 · 每次手動匯出的固定要求

**股價匯出必須勾選包含歷史下市／併購／終止交易證券。** 驗收由資料判定：
`exits_observed > 0` 於 2019–2025 每一年、`2018-12-28` 群聚消失、source-only backstop 通過、
content hash 不在 quarantine（受污染指紋 `aeda65b9…` 依內容不依路徑）。
⚠ `股價0817-0828.zip` 不滿足這一條，已記為具名限制。

## 8 · 9/30 那次跑的操作變更（R3a 起）

- `CONTINUATION` **必須**帶 `--lineage-cohort`
- `intent` / `assemble` / `execute` **必須**擇一帶 `--prior-source-manifest` 或 `--no-prior-source-manifest`
  （後者限 GENESIS）
- `preflight` 回傳改為 7-tuple
- 首次 build 會多花約 22 秒做 S-9 的 span 驗證（同行程後續 1.6 秒）

## 9 · 已作廢的舊說法（別再轉述）

- `W1_L3_DataEdge_Gap_2026-08-26.md` §5 說股價 glob 會靜默納入 —— **錯**，當時已有 count guard
  （現已改為宣告清單比對）。同文說財報 `.csv` 讀不到 —— **已修**。
  真正靜默的是**月營收，方向是漏掉**（現已修）。
- `M3_L3_SpanDerivation_..._2026-08-27.md` 頂部的 `IMPLEMENTATION NOT AUTHORIZED` 是 v1.34
  之前的狀態，`core/b0_l3_price_span.py` 早已實作。
- **`source_max_session` 是日曆事實不是價格事實。** 2026-08-30 04:22 報 `2026-08-28`，
  同次 run 價格腿最大 session 是 `2026-08-17`。`assert_prices_are_on_calendar` 單向
  （prices ⊆ calendar），日曆跑在價格前面沒有閘門看得見。
  ⇒ **引用該 dry run 的任何數字前先讀本節。**

## 10 · 環境與流程備註

- **`git checkout --` 在這棵 worktree 會把 CRLF 寫回檔案**，即使 `.gitattributes` 對 repo 與
  工作樹都宣告 LF（看來 `core.autocrlf=true` 勝出）。這正是 C-47 遷移帳本在管的東西。
  **還原檔案後務必重新檢查位元組。**
- **不要在共用工作樹上跑會改動工作樹的 git 指令。** 2026-08-31 有一個並行代理跑了
  `git stash push -u`，把三個代理的未 commit 工作一起收走。損害為零（工作樹當時已領先
  `stash@{0}`，且另有樹外備份），復原是用 Python 逐檔比對 sha256 寫回、**沒有用 git**。
  `stash@{0}` 仍留在分支上未 drop。禁用清單：`stash` / `checkout --` / `restore` / `reset` /
  `clean` / `add` / `commit`。要 pristine 副本一律 `git show HEAD:<path>` 導到樹外。
- GPT 早前留下的兩個 pytest 暫存目錄（52MB）已**移出**樹外至
  `%TEMP%\claude\pytest_tmp_holding`，未刪除。§20.7 要求 untracked 乾淨。
- **33 個 pinned 規範模組中，恰好 4 個**相對 v1.37 freeze 漂移：`b0_corporate_actions`、
  `b0_route`、`b0_adapter_production`、`b0_l3_lineage_capture`。（先前寫「五個以上」是估算，
  已由逐檔重算 sha256 更正為 4。）**這是版本升級進行中的預期狀態**，`PRODUCTION_RUN`
  因此仍被 preflight 擋著。
