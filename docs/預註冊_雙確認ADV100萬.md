# 預註冊 · 雙確認 @ ADV≥100萬 的確認性檢定

**建立**:2026-07-30 02:1x —— **在 `realbody_scores_adv100w.parquet` 產出之前**。
建置於 01:45 啟動(2,139 檔 / 263,928 stock-months,估計 03:10 前後完成)。
**本文件凍結時,「真身 @ADV≥100萬」的任何數字都還不存在。** 這是預註冊唯一合法的時間窗。

**前置**:`docs/基準階梯_BenchmarkLadder.md` §113 指名本案為下一個驗證對象,並寫死兩個 blocker:
(a) 綜合分腿還是替身、(b) 從未走過預註冊驗證。(a) 正在補,(b) 就是本文件。

**教訓來源**:`預註冊_High52Lab.md`。high52 N=8 @100萬 的 in-sample 數字比本案更漂亮
(CAGR 22.50 / 夏普 0.77 / 虛無 p=0.000),**照樣被否定** —— 死在 H1 walk-forward
(OOS 夏普 0.52)與 H3c 選擇性偏誤(DSR p=0.342)。
**in-sample 有訊號 + walk-forward 垮掉 = 過擬合。** 本案必須走同一道閘門。

---

## 0. 待驗對象(定義凍結)

| 項 | 值 |
|---|---|
| 選股規則 | `real_composite` 逐月 Top-20% **∩** `c2` 逐月 Top-20% |
| 綜合分腿 | **真身** `real_composite`(生產 `core/scoring_manager`),取自 `realbody_scores_adv100w.parquet` |
| c2 腿 | `mean(value_ind%, revenue_yoy%, high52_prox%, 100 − momentum%)`,層內百分位 |
| 母體 | `listed_ok` **且 `adv20 ≥ 1e6`** |
| 報酬線 | `exec_ret.fwd_x` = open(訊號日+1) → open(下月訊號日+1),含息。**不得更換** |
| 成本 | 手續費 0.47% + 滑價 0.25%,來回 0.72%,按換手率計 |
| 加權 | 等權 |

`top_pct = 20` **只測這一個值**,不掃。

---

## 1. 現有證據(**全部是替身、全部 in-sample**,不構成結論)

`基準階梯_BenchmarkLadder.md` 的表:

| | 池 | 檔 | 換手% | CAGR | 夏普 | ①選股 | ②對齊隨機 | 虛無p | ③對0050 |
|---|---|---|---|---|---|---|---|---|---|
| 雙確認(**替身**) | 100萬 | 48 | 72.1 | 20.34 | **0.88** | +8.50 | +14.92 | 0.000 | **+0.19** |
| high52 N=8(已否定) | 100萬 | 8 | 94.1 | 22.50 | 0.77 | +10.67 | +20.31 | 0.000 | +0.08 |
| 0050 含息 | — | — | 0 | 14.18 | 0.68 | — | — | — | — |
| 等權母體 @100萬 | — | ~全部 | — | 11.84 | — | — | — | — | — |

替身滑價損益兩平 ≈ **0.70%**(來回)。

### 換身會讓數字變差多少 —— 2000萬 層已經量過

`現況盤點_2026-07-29.md` §3-3,同一個雙確認規則、只換綜合分腿:

| | CAGR% | 夏普 | MDD% |
|---|---|---|---|
| 替身雙確認 | 14.94 | 0.57 | −67.1 |
| **真身雙確認** | 16.29 | 0.59 | **−78.4** |

**CAGR +1.35pp,但 MDD 爛 11.3pp。替身低估尾端風險。**
→ 本案的先驗預期:**CAGR 可能不掉甚至變好,MDD 大概率變差**。H1 的門檻據此設定。

---

## 2. 假設(在看到任何真身@100萬 數字之前寫死)

### H1 — 換身不翻盤(前置閘,in-sample)
真身雙確認 @100萬 的全期夏普 **> 0.68**(0050),且基準階梯 ① 選股階 **> 0**。
> H1 只是「值不值得往下驗」的閘,**它是 in-sample,不構成任何結論**。
> H1 失敗 → 本案直接結案,不跑 H2~H5(替身數字是假象,與 2000萬 層的既有判定一致)。

### H2 — walk-forward(**主假設**,high52 就是死在這裡)
把「**選哪個 ADV 層**」交給協定:訓練窗 ≥60 月,每 12 個月重估一次,
只用訓練窗內夏普從 `{2000萬, 500萬, 100萬, 無門檻}` 挑一層,在其後 12 個月量測。
該協定的 OOS 夏普 **且** CAGR **皆優於同段 0050**。
> 依據:「100萬」是在看過四層結果之後挑的。不把這個挑選動作放進 OOS,
> 就是拿後見之明當預測 —— `預註冊_High52Lab.md` §6 的原話。

### H2b — 固定層的 OOS(輔助,拆解用)
固定用 100萬 層的雙確認,在 H2 的同一段 OOS 期間優於 0050(夏普且 CAGR)。
> H2 失敗但 H2b 通過 = 「這個池是對的,但挑池這件事無法即時複製」→ 見 §4 處置。

### H3 — 虛無對照(三組)
1. **對齊 footprint 的隨機**:持股數與換手率**都對齊**策略,50 次以上,取虛無 p。
2. **報酬置換**:保留真實選股(換手/滑價結構不變),打亂層內 `fwd_x` 對應。
3. **配置置換**:對「4 個 ADV 層」這個搜尋空間重複置換掃描,取最大夏普的分布。
   → 校正選擇性偏誤。**同時報解析式 DSR**(Bailey & López de Prado),trial 數見 §3。

### H4 — 滑價穩健(低 ADV 池的**主要威脅**)
來回滑價從 0.25% 拉到 **0.60%** 時,夏普仍 > 0050 的 0.68。
> 依據:替身損益兩平 0.70%;`基準階梯` §滑價敏感度 估真實值落在 0.3~0.5%。
> 零股撮合的溢折價**無歷史資料可回測**(`研究藍圖` §1),0.60% 是刻意留的安全邊際。

### H5 — 時代穩健
六時代(05-09 / 10-14 / 15-18 / 19-21 / 2022 / 23-26)中,
**至少 4 段** CAGR 勝該層等權母體,**且至少 3 段**夏普勝 0050。

---

## 3. 量測協定(跑之前定死)

- **面板**:`realbody_scores_adv100w.parquet`,由 `lab_paths.resolve_realbody(1e6)` 解析。
  面板門檻 **必須 ≤ 1e6**;覆蓋率 <99% 直接 raise(靜默縮池會讓母體對不上)。
- **主指標**:基準階梯三階(等權母體 / 對齊 footprint 隨機 / 0050),**不再用單一 0050 當及格線**。
- **次指標**:CAGR、夏普、MDD、月換手率、實際持股數、選中股的 `adv20` 中位數。
- **兩條路徑對帳**:淨值序列須由 `high52_lab.evaluate()`(矩陣路徑)與
  `honest_backtest.Engine`(持股字典路徑)各算一次,**逐月對帳,差異 >0.01pp 即視為 bug**。
  > `預註冊_High52Lab.md` §6.3:第一輪全部數字因一個 `np.einsum` 對 bool 累加飽和的
  > bug 作廢,是靠兩套獨立實作對帳才抓到的。**凡重要結論都要有第二條路徑對得起來。**

### 搜尋空間宣告(**這是全部,不得事後追加**)

| 維度 | 值 | 數 |
|---|---|---|
| ADV 層 | 2000萬 / 500萬 / 100萬 | **3** |
| top_pct | 20 | 1 |
| 綜合分腿 | 真身 | 1 |

→ **3 個 arm**。這個數字進 H3-3 的置換次數與 DSR 的 trial 數。

**「無門檻」層排除的理由(凍結前寫明)**:本次面板建到 ADV≥100萬,
`adv20 < 100萬` 的 stock-month **沒有真身分數**。拿它跑會觸發覆蓋率閘門 raise ——
那是對的行為,不是要繞過的障礙。因此真身層的搜尋空間是 3 不是 4。
替身時代「無門檻」層的數字**不可與本案並列**。
runner 會逐層印出真身覆蓋率,實際納入幾層以那份輸出為準;
**若某層覆蓋率不足而被排除,須在 §7 記錄,不得靜默跳過。**

**誠實揭露**:雙確認這個規則本身(composite ∩ c2、top20%)是先前工作的產物,
不是本次搜出來的。所以 DSR 的 N=4 **低估了真實的多重比較**。
本案不主張「N=4 已充分校正」,只主張「在本次宣告的搜尋空間內未見選擇性偏誤」。

---

## 4. 判定門檻與失敗處置(**先寫死**)

| 假設 | 通過條件 | 失敗處置 |
|---|---|---|
| H1 | 全期夏普 >0.68 且 ①>0 | **結案**,不跑 H2~H5 |
| **H2** | OOS 夏普 **且** CAGR 皆勝同段 0050 | 見下 |
| H2b | 同上,固定 100萬 層 | — |
| H3-1/2 | 虛無 p < 0.01 | 否定 |
| H3-3 | 觀測夏普 > 置換掃描 max 分布 **p95**,且 DSR p < 0.05 | 否定 |
| H4 | 滑價 0.60% 時夏普 > 0.68 | 否定 |
| H5 | ≥4 段勝等權母體 **且** ≥3 段夏普勝 0050 | 否定 |

### 事前宣告的三個出口

1. **H2 通過 + H3/H4/H5 全過** → 進實盤候選,走 `操作手冊_實盤執行SOP.md`,
   **先小額活體演練**(零股撮合風險無法回測,見 §5-3),不直接全額部署。
2. **H2 失敗但 H2b 通過** → 結論是「**池的選擇有效,但無法即時複製**」。
   處置:**不採用動態選池**;若 H2b 的優勢夠強,得另行預註冊「固定 100萬 池」的
   前瞻驗證(從凍結日往後累積,不再用歷史 in-sample 主張)。**不得直接當成通過。**
3. **H2 與 H2b 皆失敗** → 與 high52 同命,**記錄否定結果、收工**。
   **不得改門檻、不得換母體、不得換報酬線、不得追加 arm。**

---

## 5. 已知會使結論失效的事

1. **realbody 是快照**。`realbody_scores_adv100w.parquet` 用的是 2026-07-30 當下的
   `core/scoring_manager`(最後一次改分數邏輯是 07-15 v4.5)。**只要分數邏輯再改,面板就得重建**,
   否則量到的是舊分數。
2. **c2 的取樣池落差**(已知,非本次修):生產在當日 ~400 檔粗篩池內取百分位,
   研究在整個 ADV 流動池內取。同一支股的 c2 百分位不會完全相同。這是**池層**落差。
3. **零股撮合風險無歷史資料**(`研究藍圖` §1):分盤集合競價、冷門股可能無成交、
   零股 vs 整股日線的溢折價**不可回測**。H4 的 0.60% 是代理,不是證明。
   → 即使全部通過,§4 出口 1 仍要求先小額活體演練。
4. **「ADV≥100萬 池」這個名字會誤導**:該池 `adv20` 中位數其實是 2,069 萬/日。
   真正的低流動性尾巴有多少被選中,由 §3 的「選中股 adv20 中位數」回答 —— 該數字
   **若顯著低於替身時代的 11,652 萬,H4 的安全邊際要重新評估**。
5. **母體膨脹改變了 c2 的分母**:100萬 池 2,139 檔 vs 2000萬 池 ~520 檔。
   `c2` 與 `real_composite` 的百分位都在層內重算,兩個 Top-20% 的交集檔數會變 ——
   **持股數不是自由參數,是規則的產物**,不得事後調整以求好看。

---

## 6. 執行順序

1. ⏳ 等 `realbody_scores_adv100w.parquet` 落地(建置中,估 03:10)。
2. 驗收面板:列數 / 檔數 / 月數 / `real_composite` 分布 / 與 obs_alpha 的覆蓋率。
   → `python beat_0050/strategies/dual100_lab.py --part verify`
3. 跑 H1(前置閘)。**H1 失敗就停。**
4. H1 通過才跑 H2 / H2b / H3 / H4 / H5。
5. 結果(不論正負)寫進 `docs/開發日誌_DevLog.md` 與本文件 §7。

## 7. 結果

### 7.0 前置:量測基礎設施稽核(2026-07-30,Codex 靜態盤點 → 五輪修正)

> ⚠ **這一小節不是假設判定,是量測管線的稽核紀錄。** H1–H5 的正式結果仍在 §7.1 待填。
> 之所以先記在這:稽核修掉了一個**會動數字**的偏誤,§7.1 的數字必須用修正後的碼跑才算數。

- **修掉一個真的動數字的偏誤**:尾月共同時鐘不對齊,使四個受測策略各虛高
  **0.35–0.50pp CAGR / 0.02 夏普**。修正後 `dual100 --part h1` 前置閘**仍通過**
  (真身雙確認 @100萬 夏普 0.89 > 0050 的 0.68;①選股階 +9.30pp/年;兩路徑對帳 0.0000pp)。
  **結論方向未翻盤**,但 §7.1 的數字一律以修正後為準,不得沿用稽核前的舊值。
- **基準時位(開盤 vs 收盤錨)**經實測僅值 0.1pp/年(t≈0),非 5.6pp 級的偏誤;不影響判定。
- **母體完整性**:輸入(`obs_alpha` 過濾後)與輸出(`realbody_scores`)兩端都加了 `(stock_id, as_of)`
  唯一性硬檢查;真身覆蓋率閘門三個入口(`build`/`dual100`/`load_real_panel`)統一預設 **1.0**
  (零靜默損失)。放寬到探索模式(<1.0)一律進 run log 並須在本節標記。
- **正規面板隔離**:只有 cov=1.0 且 0 個 score_error 的完美建置能佔正規檔名;瑕疵一律
  `.EXPLORE`/`.INCOMPLETE`,`resolve_realbody()` 撿不到。
- **驗證紀錄**:研究 lifeline 測試 **25 passed / 0 failed**;完整 `tests/` 132 passed / 10 failed
  (那 10 個是 `core` 既有 baseline,`git stash` 已證與本次無關)。細節見
  `docs/開發日誌_DevLog.md §11`。

### 7.1 假設判定(H1–H5)

> ⬜ 未執行完整 `--part all`。本節在跑完之前保持空白 —— 先有門檻,才有數字。
> 執行時務必用**稽核後**的碼(見 §7.0)。H1 前置閘已知通過;H2–H5 尚未逐項落記。
