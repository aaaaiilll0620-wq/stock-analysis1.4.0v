# 工單 — 活體演練保護（第一梯隊）

**開立日：2026-07-19。** 出處：中線模組 B 全面盤點（14 代理 workflow，25 提案經三評審去重後之第一梯隊）。
**目的：保護 7/20 建倉起的 30 天實戰演練與 8/14 三方對帳**——它是 C2 唯一一次 live 成績單，
對帳證據鏈被污染就沒有第二次。本工單全部是工程／監控件，**不動任何訊號、排序、選股規則**；
唯一例外（WP3 告警帶判準）依單發射擊文化以一頁預註冊附錄凍結。

## 0. 時限地圖

| 錨點 | 日期 | 依賴本工單的項目 |
|---|---|---|
| 建倉日 | 2026-07-20（一）開盤 | 無阻斷件（plan 卡已就緒）；WP2 排程收尾與 WP4 搬家越早越好 |
| 首次三方對帳 | ≈2026-08-14（7/17 起 20 交易日） | WP1 淨額記帳＋輸入硬化＋對帳報告、WP3 告警帶**凍結**須在此前完成 |
| 首個 exit window | ≈2026-10 中（7/20 起 60 交易日） | WP1 賣出卡＋fills 賣出側 schema 須在此前就緒（schema 建議 8/14 前先補，事後補填破壞凍結件紀律） |

---

## WP1・演練賣出側＋淨額對帳閉環（M）

現況缺口：fills 只有買進側、報酬全是毛額、對帳結果只存在於 UI。賣出 0.3855% 與 20 日期望
+1.5% 同數量級，**毛額記帳會系統性高估演練成績、直接污染 8/14 判讀**。

| # | 任務 | 錨點 |
|---|---|---|
| 1-1 | fills schema 補賣出側欄位 `sell_date` / `sell_price` / `sell_reason`（或獨立 sells_{date}.csv，擇一定案） | app.py:577-601 |
| 1-2 | 輸入硬化：`fill_date` 由 TextColumn 改 `st.column_config.DateColumn`（現行 `len>=8` 過濾會把格式 typo **靜默踢出追蹤**，對帳樣本缺損不可察覺）；`fill_price` 加 sanity check（偏離凍結收盤逾帶寬數倍即紅字）；日期比較改 datetime 型別 | app.py:592, 600-601, 644 |
| 1-3 | 追蹤資料來源改「磁碟 fills_{date}.csv 優先」，消除 `_edited` 即時態「未存檔也追蹤、重整即遺失」歧義 | app.py:600 |
| 1-4 | 記帳改淨額：套實際元大零股 6 折費率（買 0.0855%／賣 0.3855% 含證交稅），啟用已收集未用的 `shares` 欄算實際 TWD 損益；允許回填券商實收手續費，記錄 **NT$1 最低手續費地板**的實際觸發（小資金專屬真實成本，只有 live 量得到） | app.py:620, 628 |
| 1-5 | 20 交易日成熟時自動落地 `reconciliation_{date}.md`：平均滑價、逐檔帶內/帶外、帶外檔數 vs §22 期望（≈1/5 屬正常）自動比對、實際 vs 理論 T+1、毛/淨並列；跨 cohort 滑價統計累積存檔（逐期累積「本人零股實際滑價分布」對照歷史跳空帶 ±1.1%, n=712） | app.py:663-668 |
| 1-6 | 硬編碼改參數並註明凍結出處：app 的「+1.5%±4%」、模擬器 plan 卡的「143%」 | app.py:667-668；portfolio_simulator_lab.py:646 |
| 1-7 | 再平衡賣出卡：`portfolio_simulator_lab.py --plan-exit`（或獨立小腳本）——讀最新含 c2_score 的凍結 pool＋fills 持倉，依已裁定規則（續留 top-8 零成本延續／掉出者次日開盤賣／整批）機械產出 `exit_plan_{date}.csv`：keep/sell 名單、賣出參考帶（重用 `overnight_gap_band()`）、預估賣出日 | portfolio_simulator_lab.py:578-592, 629-630 |
| 1-8 | entry/exit 日期改用實測交易日曆（當日收盤檔數≥500，ledger/模擬器已有同款）取代 `pd.BDay`，防台股長假使 60 交易日窗漂移 | portfolio_simulator_lab.py:624-626；shortlist_ledger.py:73-75 |
| 1-9 | 演練分頁加「預期痛感卡」（選配）：呈現已凍結統計——K=8 約每 3 季 1 季落後整籃>2%、每 5~6 季 1 季>5%；2022 情境 −12.3% vs 0050 −24.7%；帶外約 1/5 屬正常——壓低臨場棄守紀律的行為左尾 | 引用 devlog §20/§22 已凍結數字，非新判準 |

**驗收**：(a) 演練分頁毛/淨並列且淨額含實收費用；(b) 造一筆格式錯誤 fill_date 會被顯式警示而非靜默排除；
(c) 對帳日產出 reconciliation 檔可直貼 devlog；(d) 賣出卡對 mock 持倉輸出 keep/sell 與帶，人工複算一致。
**紀律**：免預註冊——賣出規則由 §22 預註冊 Q1/Q3 已裁定，本 WP 只是把「手動操作紀律」腳本化；
c2 排名一律讀存欄 `c2_score`（遵守 ledger CSV 精度先例，shortlist_ledger.py:94-95）。

---

## WP2・粗篩鏈 fail-loud＋心跳告警包（M）

現況缺口：`industry_value_ref` 當日缺列 → value_ind 全 NaN → **c2 因 skipna 默默退化成三因子平均，
shortlist 照常輸出零警告**（C2 最強的全天候腿無聲斷掉）；凍結件可被覆寫；7/18 兩排程未跑
33 小時無人察覺、漏收快照永久無法回補；app 無任何 stale 警示。

| # | 任務 | 錨點 |
|---|---|---|
| 2-1 | value_ind 守衛：ref 查無 as_of 當日列或池內 NaN 比率超閾（建議 20%）即 **abort 非零退出**（bat 鏈本就以 errorlevel 擋下游） | universe_screen_daily.py:178-181 |
| 2-2 | 四腳健康檢查：寫檔前列印 c2 四成分池內 NaN 比率落 log，任一腳覆蓋率 <95% 即 WARN＋非零退出 | universe_screen_daily.py:248-251 |
| 2-3 | 凍結件不可覆寫：pool_{date}.csv / shortlist 已存在即拒絕重寫（比照 backfill 的已存在跳過），凍結性改由**程式保證**而非排程冪等假設 | universe_screen_daily.py:254, 258 |
| 2-4 | as_of 新鮮度守衛：as_of 非預期最後交易日即 abort；營收腳 known 日期距 as_of >45 天即 WARN（≈8/10 七月營收發布後若 TEJ 未重匯會被抓到，**正落在演練期內**） | universe_screen_daily.py |
| 2-5 | 排程收尾：commit 實機已生效的 18:00 diff（register_daily_task.bat/ps1，防他機重建退回 20:30）；兩排程加 `-WakeToRun` 或第二觸發時刻（catch-up trigger）——7/18 實測證明 StartWhenAvailable **不涵蓋睡眠** | scripts/register_daily_task.ps1、register_market_snapshot_task.ps1 |
| 2-6 | 心跳＋watchdog：bat 鏈尾寫 SUCCESS 心跳檔；隔日比對最新 pool 日期 vs 前一交易日，落後即 Windows toast＋醒目 ALERT 檔 | scripts/market_snapshot_collect.bat |
| 2-7 | app 全域 stale banner：最新 shortlist 距今 >1 交易日即紅字警示（本機＋雲端），含最後成功日期 | app.py |

**驗收**：(a) 健康日改後與改前 shortlist **parity maxdiff=0**（用 ledger 既有 parity 模式驗證）；
(b) 手動移走 ref 當日列可觸發 abort；(c) 對已存在 pool 重跑會拒絕覆寫；(d) 模擬排程缺席一日，隔日有告警。
**紀律**：免預註冊——全部是「偵測與中止」，不改任何計算值。**明確不做**：skipna 改 min_count、
官方月營收 union 進營收腳——那是生產分數改動，須走 factor_experiments.py 雙閘門／parity-gated 另案（排 8/14 後）。

---

## WP3・ledger v2：四腳分解 IC＋排程＋告警帶凍結（S；評審最高分）

現況缺口：衰退監控只看 composite IC——C2 腳異質性極強（high52 是多頭限定塊、反動能腳在 2026
動能行情失血），**某條腿先死會被其他腳遮蔽到很晚**；ledger 純手動忘跑無提醒；期望帶只印終端無判定；
parity assert 一敗整支崩潰；ledger.csv 整檔覆寫無歷史。

| # | 任務 | 錨點 |
|---|---|---|
| 3-1 | 軌 1 加開四腳分解 IC：value_ind_pct_pool_pct / revenue_yoy_pool_pct / high52_prox_pool_pct / (100−momentum20_pool_pct) 各算 Rank IC——四欄自 7/20 新格式已凍結在 pool 檔內，零額外成本；輸出進 ledger.csv 與終端報告，附 alpha gate 分時代歷史期望帶對照 | shortlist_ledger.py:154-160；universe_screen_daily.py:248-251 |
| 3-2 | parity 檢查 fail-soft：由 assert 崩潰改為標記該 cohort FAIL 續跑（監控工具不能自己是單點） | shortlist_ledger.py:121 |
| 3-3 | ledger.csv 覆寫前留時間戳副本，保存健康度歷史軌跡 | shortlist_ledger.py:261 |
| 3-4 | 掛月度排程（Task Scheduler 一條，模式照抄 register_daily_task.ps1），收束 devlog §21「每月手動或掛排程」未決項 | — |
| 3-5 | **8/14 前**寫一頁預註冊附錄凍結告警帶判準（例：非重疊 20d IC 連續 N cohort 低於期望帶下緣 0.05、或 t 轉負 → 觸發**人工檢討**）；ledger 輸出加機器可讀 pass/warn 欄 | shortlist_ledger.py:47, 192-193 |

**驗收**：(a) 對既有 cohort 重跑，composite IC 與現版一致、新增四腳欄位齊全；(b) 排程註冊可查；
(c) 告警帶附錄先凍結、後上碼。
**紀律**：3-1~3-4 純監控免預註冊（只讀凍結存欄）；3-5 屬判準，**必須在 8/14 首個 live 對帳前凍結**，
否則球門會被 live 數字牽著走。觸發後行動＝人工檢討，**不自動改生產**（軌 2 top-N 開放題明文「解決前不動生產」照守）。

---

## WP4・研究資料基底搬家＋不可回補快照備份（S；存亡級左尾）

現況缺口：`obs_alpha.parquet` / `obs_dump_full.parquet` 住在**舊 session 的 Temp scratchpad**
（系統清理即蒸發），卻是四份預註冊文件（PortfolioSimulator / BasketDispersion / EventFlag / ModuleA_WeeklyGate）
共用的單一資料基底——滅失＝整條研究線斷炊，R3/R4 開跑前置。market_cache 每日快照單一副本、漏收永久無法回補。

| # | 任務 | 錨點 |
|---|---|---|
| 4-1 | obs 檔搬進專案穩定目錄（建議 `data/research_base/`，或 %USERPROFILE% 下非 OneDrive 同步目錄）；搬後抽樣 parity（列數/欄位/抽樣值逐檔一致）驗收 | — |
| 4-2 | 新增 `scripts/lab_paths.py` 共用常數，三支實驗室的硬編碼 Temp 路徑全改引用之；缺檔時給 friendly guard（印重生指令）而非裸 FileNotFoundError | alpha_gate_lab.py:30-35；portfolio_simulator_lab.py:33-43；basket_dispersion_lab.py:29-36 |
| 4-3 | `regen_obs_base.py` 一鍵重生：包裝兩步（tej_universe_screen_validation --dump-obs **須帶 2005 起訖 custom 區間**的 footgun → alpha_gate_lab --build），load 時印 max(as_of) 新鮮度提示 | — |
| 4-4 | market_cache / tej_cache 掛每週 robocopy 鏡像到第二位置（OneDrive 或外接碟）——「磁碟壞即歷史快照永久滅失」的唯一保護，成本近零 | — |

**驗收**：(a) 三支 lab 以新路徑跑通、舊路徑引用歸零；(b) parity 通過；(c) 鏡像排程可查。
**紀律**：純基建搬遷與備份，免預註冊，不動任何判準或實驗結論。

---

## 建議執行順序

1. **今天（7/19）**：WP2-5 排程收尾（commit 18:00 diff＋WakeToRun）＋ WP4-1/4-2 obs 搬家——兩件最便宜、且明天建倉前就生效。
2. **7/20 建倉後第一週**：WP2 其餘（fail-loud＋心跳＋stale banner）→ WP1-1~1-4（schema＋輸入硬化＋淨額）。
3. **8/14 前**：WP1-5/1-6（對帳報告落地）＋ WP3 全部（告警帶附錄先凍結再上碼）。
4. **10 月首個 exit window 前**：WP1-7/1-8（賣出卡＋交易日曆）；WP1-9 痛感卡選配隨時可插。

## 明確不在本工單（防範圍潛變）

- 營收腳快照 union（生產輸入源改動，parity-gated 另案，排 8/14 後）
- 實際費率＋K∈{8,10,12} 模擬器敏感度複跑（一頁預註冊附錄，排 8/14 後）
- ortho 基準換 C2＋holdout 射擊登記簿、C3 前瞻協定（挑戰賽閘門 phase 2）
- top-N 帶內去動能腳重排、regime 條件化節奏（訊號／規則層，各自完整預註冊，證據累積後立項）
