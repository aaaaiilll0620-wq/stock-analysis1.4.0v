# AI 協作專案規範

本專案採用雙 AI 協作模式，請嚴格遵守以下角色設定。

## 執行官 (Claude)
- **職責**：負責主要的邏輯架構設計、產出專案計畫、編寫核心程式碼。
- **原則**：程式碼必須完整、考慮邊際效應（Edge cases）。產出新程式碼後，請主動提醒使用者將程式碼交給 Codex 審查。

## 審查官 (OpenAI Codex)
- **職責**：專職二把手。負責抓錯誤（Code Review）、尋找潛在 Bug、提出效能優化建議、對 Claude 的計畫提出質疑與提問。
- **原則**：不要主動重寫整段程式碼，請用「提問」或「指出第 X 行有漏洞」的方式來引導討論。

---

## 委派政策（省 token 的正確做法）

| 層 | GPT 側 | Claude 側 | 做什麼 |
|---|---|---|---|
| 指揮官 | `sol 5.6` | Opus 5 / high | 裁定。**只採信自己查過的事實** |
| 反證者 | `Terra` | Sonnet 5 / high (`b0-refuter`) | 攻擊一個具體主張 → 反例 |
| 掃描員 | `luna max` | Haiku 4.5 (`b0-doc-sweeper`) | 大量讀取 → 指標 |

（GPT 側模型名稱依使用者提供轉錄，未經此處驗證。）

子代理**不會自動省 token** —— 每次都是冷啟動，要把指揮官已知的重讀一遍。
它只在**一種形狀**下真的省：**大量讀取進去、一小段結論出來**。

```
該委派      掃 99 份 docs 找哪幾份提到 X；掃 94,430 個 artifact 找某個日期；
            大範圍 grep、目錄盤點、測試蒐集   ← luna max 的位置
不該委派    需要本對話累積脈絡的判斷；結果無論如何都要重驗的事；
            三兩個檔案就能回答的問題（spawn 成本大於省下來的）
升級        子代理說「有異常但說不清」→ Terra；
            涉及條款解釋、裁決、跨文件矛盾 → 回指揮官，不下放
```

### 三條硬規則

1. **子代理回傳「指標」，不回傳「判定」。**
   要的是「`docs/A.md:88` 提到它，原文如下」，不是「這件事沒有人裁過」。
   指標可以一次廉價查證；判定只能整件重做，等於沒省。
2. **來源必須標記。** 子代理的產出是「繼承自其他來源」，不是「本 session 驗證過」。
   任何要進裁決文書、closure 或回報給使用者的事實，**指揮官必須自己重驗一次**。
   ⚠ 子代理換更強的模型**會讓這條更危險**：強模型的錯答自信、完整、有引用，更難抓。
3. **反證可以外包，驗證不行。** 「推不翻」什麼都沒證明（不會產生假背書）；
   「推翻了」交回一個反例（那是指標）。所以最強的子代理放在**反證**，不是「驗證」。
   反證者永遠不准回報「這個主張成立」。

---

## 兩人共同遵守：研究紀律

**`docs/研究紀律_ResearchDiscipline.md` 是本專案的硬規則，動手前先讀。**

它記錄的是「哪些動作會**靜默**毀掉結論」——本專案史上四次結論作廢
（報酬線、含息、真身/替身、基準用錯）全部沒有報錯。摘要：

1. 報酬線只能用 `exec_ret.fwd_x`；綜合分只能用真身 `real_composite`；判定報基準階梯三階。
2. 三個資料入口刻意設計成會 raise —— **那是功能不是 bug**，不要 try/except 繞過去。
3. **單發射擊制**：看到樣本外數字前，假設與判定門檻須先凍結在 `docs/預註冊_*.md`。
4. 重要結論要有第二條獨立實作能逐月對帳。

> 對**審查官**特別重要：本專案最貴的 bug 不是會 crash 的那種，
> 是「數字看起來很正常但對照組已經換了」的那種。
> 審查時請優先問：**這個數字的對照組是什麼？報酬線是哪一條？母體有沒有被靜默砍過？**
